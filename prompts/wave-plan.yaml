# Wave Plan Schema — drives scripts/wave-orchestrator.ps1
#
# Each wave entry is consumed sequentially. The orchestrator pauses for
# architect approval at three gates: after-review, after-builder-build,
# and before-issue-closure. State is tracked in `state.json` next to this file.
#
# Edit this file between waves to add new waves; the orchestrator picks up
# the next pending wave on each invocation.

waves:
  - id: "8.5"
    title: "AD-641 umbrella split"
    kind: meta              # meta | main | combo | bf
    dispatch_prompt: "prompts/WAVE-8.5-DISPATCH.md"
    expected_outputs:
      - "prompts/ad-641a-*.md"
      - "prompts/ad-641b-*.md"
      - "prompts/ad-641c-*.md"
      - "prompts/ad-641d-*.md"
      - "prompts/ad-641e-*.md"
      - "prompts/ad-641f-*.md"
      - "prompts/WAVE-8.5-SPLIT-SUMMARY.md"
    builder_required: false  # meta-prompts produce prompts only, not code
    issues_to_close: []      # AD-641 stays open until children land
    status: done

  - id: "9A"
    title: "AD-641 children — parallel-safe (a/b/f)"
    kind: main
    depends_on: ["8.5"]
    dispatch_prompt: "prompts/WAVE-9A-DISPATCH.md"
    prompts_already_drafted: true   # Wave 8.5 produced these; skip 'draft' stage
    prompt_paths:
      - "prompts/ad-641a-observability-bridge.md"
      - "prompts/ad-641b-ward-room-hebbian.md"
      - "prompts/ad-641f-engineering-chief-observability.md"
    builder_required: true
    issues_to_close: []
    status: done

  - id: "9B"
    title: "AD-641 children — cross-cutting (c/e)"
    kind: main
    depends_on: ["9A"]
    dispatch_prompt: "prompts/WAVE-9B-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-641c-ward-room-thread-priority.md"
      - "prompts/ad-641e-learned-shortcut-abstraction.md"
    builder_required: true
    issues_to_close: []
    status: done

  - id: "9C"
    title: "AD-641 child — high-risk (d) + AD-277 closure"
    kind: main
    depends_on: ["9B"]
    dispatch_prompt: "prompts/WAVE-9C-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-641d-crew-deliberation-protocol.md"
    builder_required: true
    issues_to_close: [277]   # AD-641 umbrella closes when 9C lands
    status: done

  - id: "10"
    title: "Workforce Cleanup — AD-501 + AD-500 (Combo B)"
    kind: main
    depends_on: ["9C"]
    dispatch_prompt: "prompts/WAVE-10-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-501-tasktracker-deprecation.md"
      - "prompts/ad-500-dutyscheduler-workitem-migration.md"
    builder_required: true
    issues_to_close: [88, 87]
    status: done

  - id: "11"
    title: "Tooling hygiene — phantom-API pre-check kwarg validation (AD-685)"
    kind: main
    depends_on: ["10"]
    dispatch_prompt: "prompts/WAVE-11-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-685-phantom-precheck-kwarg-validation.md"
    builder_required: true
    issues_to_close: []
    status: done

  - id: "12"
    title: "AD-477 Naval Organization v1 (Captain's Log + Plan of the Day)"
    kind: main
    depends_on: ["11"]
    dispatch_prompt: "prompts/WAVE-12-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-477-naval-organization-v1.md"
    builder_required: true
    issues_to_close: [71]
    status: done

  - id: "13"
    title: "Combo C — 7 trivial extensions (526d/572c/572d/573c/573e/573f/575c)"
    kind: main
    depends_on: ["12"]
    dispatch_prompt: "prompts/WAVE-13-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/combo-C-trivial-extensions.md"
    builder_required: true
    issues_to_close: [7]
    status: done

  - id: "14"
    title: "AD-487 Self-Distillation v1 (Map step only)"
    kind: main
    depends_on: ["13"]
    dispatch_prompt: "prompts/WAVE-14-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-487-self-distillation-v1.md"
    builder_required: true
    issues_to_close: [79]
    status: done

  - id: "15"
    title: "AD-685b — Phantom-API Pre-Check Method-Call AST Validation"
    kind: main
    depends_on: ["14"]
    dispatch_prompt: "prompts/WAVE-15-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-685b-method-call-validation.md"
    builder_required: true
    issues_to_close: []
    status: done

  - id: "16"
    title: "AD-525 Creative Expression v1 (Skills Inventory + Records Output)"
    kind: main
    depends_on: ["15"]
    dispatch_prompt: "prompts/WAVE-16-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-525-creative-expression-v1.md"
    builder_required: true
    issues_to_close: [100]
    status: done

  - id: "17"
    title: "AD-513 Phase 2 v1 (Crew Manifest Shell + Watch Filter + Ship Manifest)"
    kind: main
    depends_on: ["16"]
    dispatch_prompt: "prompts/WAVE-17-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-513-phase2-manifest-v1.md"
    builder_required: true
    issues_to_close: [14]
    status: done

  - id: "18"
    title: "AD-572e Task Awareness in Captain DM Context"
    kind: main
    depends_on: ["17"]
    dispatch_prompt: "prompts/WAVE-18-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-572e-task-awareness.md"
    builder_required: true
    issues_to_close: []
    status: done

  - id: "19"
    title: "AD-530 v1 Information Classification Enforcement (Disclosure Gate)"
    kind: main
    depends_on: ["18"]
    dispatch_prompt: "prompts/WAVE-19-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-530-classification-gate-v1.md"
    builder_required: true
    issues_to_close: [104]
    status: done

  - id: "20"
    title: "Combo D — Gap Pipeline Extensions (AD-539c + AD-539d)"
    kind: main
    depends_on: ["19"]
    dispatch_prompt: "prompts/WAVE-20-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/combo-D-gap-pipeline-extensions.md"
    builder_required: true
    issues_to_close: [106, 107]
    status: done

  - id: "21"
    title: "AD-522 v1 Statistical Process Control (Calibration + Western Electric Rules)"
    kind: main
    depends_on: ["20"]
    dispatch_prompt: "prompts/WAVE-21-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-522-spc-v1.md"
    builder_required: true
    issues_to_close: [97]
    status: done

  - id: "22"
    title: "AD-511 v1 Agent Autonomy Boundaries"
    kind: main
    depends_on: ["21"]
    dispatch_prompt: "prompts/WAVE-22-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-511-autonomy-boundaries-v1.md"
    builder_required: true
    issues_to_close: [93]
    status: done

  - id: "23"
    title: "Combo E — Cognitive Helpers (AD-508 Duty Scope + AD-478 Workspace Ontology)"
    kind: main
    depends_on: ["22"]
    dispatch_prompt: "prompts/WAVE-23-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/combo-E-cognitive-helpers.md"
    builder_required: true
    issues_to_close: [90, 72]
    status: done

  - id: "24"
    title: "AD-507 v1 Crew Development Framework (Curriculum Registry)"
    kind: main
    depends_on: ["23"]
    dispatch_prompt: "prompts/WAVE-24-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-507-curriculum-v1.md"
    builder_required: true
    issues_to_close: [89]
    status: done

  - id: "25"
    title: "AD-509 v1 Boot Camp Phase Tracker"
    kind: main
    depends_on: ["24"]
    dispatch_prompt: "prompts/WAVE-25-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-509-boot-camp-v1.md"
    builder_required: true
    issues_to_close: [91]
    status: done

  - id: "26"
    title: "BF-238 Post Budget Telemetry Monitoring"
    kind: bf
    depends_on: ["25"]
    dispatch_prompt: "prompts/WAVE-26-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/bf-238-post-budget-telemetry-monitoring.md"
    builder_required: true
    issues_to_close: [341]
    status: done

  - id: "27"
    title: "AD-657 v1 Dream Consolidation Trace Preservation"
    kind: main
    depends_on: ["26"]
    dispatch_prompt: "prompts/WAVE-27-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-657-dream-trace-preservation-v1.md"
    builder_required: true
    issues_to_close: [316]
    status: done

  - id: "28"
    title: "AD-658 v1 Cognitive Chain Harness Metrics"
    kind: main
    depends_on: ["27"]
    dispatch_prompt: "prompts/WAVE-28-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-658-chain-harness-metrics-v1.md"
    builder_required: true
    issues_to_close: [317]
    status: done

  - id: "29"
    title: "AD-683 v1 Ship State Snapshot for Cold-Start Onboarding"
    # Retarget rationale: AD-644 Phases 1-4 already shipped (commit f10369d).
    # Phase 5 (deprecation) is higher-risk and deferred to AD-644b. AD-683
    # is a tractable single-AD v1 that fits the streamlined wave cadence.
    kind: main
    depends_on: ["28"]
    dispatch_prompt: "prompts/WAVE-29-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-683-ship-state-snapshot-v1.md"
    builder_required: true
    issues_to_close: [313]
    status: done

  - id: "30"
    title: "AD-645b: EVALUATE Brief Alignment (AD-645 Phase 4)"
    kind: main
    depends_on: ["29"]
    dispatch_prompt: "prompts/WAVE-30-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-645b-evaluate-brief-alignment.md"
    builder_required: true
    issues_to_close: [287]
    status: done
    # Retarget rationale: original AD-645 v1 stub was drafted before Phases 1–3
    # shipped under #287. Phases 1–3 closed (PROGRESS.md:320). Phase 4 = AD-645b
    # is the last in-scope phase under #287; Phase 5 NATS schema deferred to
    # AD-641g per research doc Section 6. Closing #287 with Phase 4 is correct.

  - id: "31"
    title: "AD-659 v1 Cognitive Chain Self-Optimization Loop"
    kind: main
    depends_on: ["30"]
    dispatch_prompt: "prompts/WAVE-31-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-659-chain-self-optimization-v1.md"
    builder_required: true
    issues_to_close: [318]
    status: done

  - id: "32"
    title: "AD-660 v1 Agent Causal Reasoning Framework"
    kind: main
    depends_on: ["31"]
    dispatch_prompt: "prompts/WAVE-32-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-660-causal-reasoning-v1.md"
    builder_required: true
    issues_to_close: [319]
    status: done

  - id: "33"
    title: "AD-661 v1 Full Diagnostic Context for Agent Self-Improvement"
    kind: main
    depends_on: ["32"]
    dispatch_prompt: "prompts/WAVE-33-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-661-diagnostic-context-v1.md"
    builder_required: true
    issues_to_close: [320]
    status: done

  - id: "34"
    title: "AD-647 v1 Process-Oriented Cognitive Chains (Scout Report)"
    kind: main
    depends_on: ["33"]
    dispatch_prompt: "prompts/WAVE-34-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-647-process-oriented-chains-v1.md"
    builder_required: true
    issues_to_close: [291]
    status: done

  - id: "35"
    title: "AD-635 v1 Medical Diagnostic Data Access"
    kind: main
    depends_on: ["34"]
    dispatch_prompt: "prompts/WAVE-35-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-635-medical-diagnostic-v1.md"
    builder_required: true
    issues_to_close: [231]
    status: done

  - id: "36"
    title: "AD-686 v1 Oracle Absorbs SemanticKnowledgeLayer"
    kind: main
    depends_on: ["35"]
    dispatch_prompt: "prompts/WAVE-36-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-686-oracle-absorbs-semantic-v1.md"
    builder_required: true
    issues_to_close: [380]
    status: done

  - id: "37"
    title: "AD-687 v1 Knowledge Edge Store"
    kind: main
    depends_on: ["36"]
    dispatch_prompt: "prompts/WAVE-37-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-687-knowledge-edge-store-v1.md"
    builder_required: true
    issues_to_close: [381]
    status: done

  - id: "38"
    title: "AD-688 v1 Oracle Graph Integration"
    kind: main
    depends_on: ["37"]
    dispatch_prompt: "prompts/WAVE-38-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-688-oracle-graph-integration-v1.md"
    builder_required: true
    issues_to_close: [382]
    status: done

  - id: "39"
    title: "AD-689 v1 Edge Population from Existing Data"
    kind: main
    depends_on: ["38"]
    dispatch_prompt: "prompts/WAVE-39-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-689-edge-population-v1.md"
    builder_required: true
    issues_to_close: [383]
    status: done

  - id: "40"
    title: "AD-690 v1 Dream Step 10 Relationship Inference"
    kind: main
    depends_on: ["39"]
    dispatch_prompt: "prompts/WAVE-40-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-690-dream-step10-relationship-inference-v1.md"
    builder_required: true
    issues_to_close: [384]
    status: done
  - id: "41"
    title: "AD-691 v1 NL-to-Graph Query (Ship's Computer structural routing)"
    kind: main
    depends_on: ["40"]
    dispatch_prompt: "prompts/WAVE-41-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-691-nl-to-graph-query-v1.md"
    builder_required: true
    issues_to_close: [385]
    status: done

  - id: "42"
    title: "AD-692 v1 Classification Enforcement on Knowledge Graph"
    kind: main
    depends_on: ["41"]
    dispatch_prompt: "prompts/WAVE-42-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-692-classification-enforcement-v1.md"
    builder_required: true
    issues_to_close: [386]
    status: done

  - id: "43"
    title: "AD-695 v1 Ship Health Oracle Tier + Threshold Bridge Alerts"
    kind: main
    depends_on: ["42"]
    dispatch_prompt: "prompts/WAVE-43-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-695-ship-health-oracle-v1.md"
    builder_required: true
    issues_to_close: [389]
    status: done

  - id: "44"
    title: "AD-594a v1 Consultation Workspace (Session-Scoped Ship's Records)"
    kind: main
    depends_on: ["43"]
    dispatch_prompt: "prompts/WAVE-44-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-594a-consultation-workspace-v1.md"
    builder_required: true
    issues_to_close: [160]
    status: done

  - id: "45"
    title: "AD-661 Combo: b (Ship's Records) + c (remainder redistribution)"
    kind: combo
    depends_on: ["44"]
    dispatch_prompt: "prompts/WAVE-45-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-661bc-diagnostic-context-extensions.md"
    builder_required: true
    issues_to_close: [412, 413]
    status: done
  - id: "46"
    title: "AD-685c v1 Phantom-API Type-Shape Validation"
    kind: main
    depends_on: ["45"]
    dispatch_prompt: "prompts/WAVE-46-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-685c-phantom-type-shape-v1.md"
    builder_required: true
    issues_to_close: [406]
    status: done

  - id: "47"
    title: "AD-685d v1 Phantom-API Dataclass/Pydantic Field-Name Validation"
    kind: main
    depends_on: ["46"]
    dispatch_prompt: "prompts/WAVE-47-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-685d-phantom-field-name-v1.md"
    builder_required: true
    issues_to_close: [407]
    status: done

  - id: "48"
    title: "AD-647b v1 Process Chains Registry + BF-209 Bypass Removal"
    kind: main
    depends_on: ["47"]
    dispatch_prompt: "prompts/WAVE-48-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-647b-chain-registry-v1.md"
    builder_required: true
    issues_to_close: [404]
    status: done

  - id: "49"
    title: "AD-647c v1 Process Chains Bills/Watch Bill Integration (NATS deferred to AD-641g)"
    kind: main
    depends_on: ["48"]
    dispatch_prompt: "prompts/WAVE-49-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-647c-bills-integration-v1.md"
    builder_required: true
    issues_to_close: [405]
    status: done

  - id: "50"
    title: "AD-686b v1 Oracle Write-Path Migration"
    kind: main
    depends_on: ["49"]
    dispatch_prompt: "prompts/WAVE-50-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-686b-oracle-write-path-v1.md"
    builder_required: true
    issues_to_close: [414]
    status: done

  - id: "51"
    title: "AD-660b v1 Causal Reasoning Auto-Invocation + Emergence Integration"
    kind: main
    depends_on: ["50"]
    dispatch_prompt: "prompts/WAVE-51-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-660b-causal-auto-emergence-v1.md"
    builder_required: true
    issues_to_close: [411]
    status: done

  - id: "52"
    title: "AD-659b v1 Chain Optimizer Apply Approved Proposals"
    kind: main
    depends_on: ["51"]
    dispatch_prompt: "prompts/WAVE-52-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-659b-chain-optimizer-apply-v1.md"
    builder_required: true
    issues_to_close: [409]
    status: done

  - id: "53"
    title: "AD-659c v1 Chain Optimizer Counselor Monitoring + Decision Persistence"
    kind: main
    depends_on: ["52"]
    dispatch_prompt: "prompts/WAVE-53-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-659c-chain-optimizer-counselor-v1.md"
    builder_required: true
    issues_to_close: [410]
    status: done

  - id: "54"
    title: "AD-658a v1 Chain Trace Token Input/Output Split"
    kind: main
    depends_on: ["53"]
    dispatch_prompt: "prompts/WAVE-54-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-658a-chain-trace-token-split-v1.md"
    builder_required: true
    issues_to_close: [408]
    status: done

  - id: "55"
    title: "AD-456b v1 Security Infrastructure Runtime Sandboxing"
    kind: main
    depends_on: ["54"]
    dispatch_prompt: "prompts/WAVE-55-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-456b-runtime-sandboxing-v1.md"
    builder_required: true
    issues_to_close: [398]
    status: done

  - id: "56"
    title: "AD-456c v1 Per-Tier Credential Lookup"
    kind: main
    depends_on: ["55"]
    dispatch_prompt: "prompts/WAVE-56-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-456c-per-tier-credentials-v1.md"
    builder_required: true
    issues_to_close: [399]
    status: done

  - id: "57"
    title: "AD-456d v1 AuditLog SQLite Persistence"
    kind: main
    depends_on: ["56"]
    dispatch_prompt: "prompts/WAVE-57-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-456d-auditlog-sqlite-v1.md"
    builder_required: true
    issues_to_close: [400]
    status: done

  - id: "58"
    title: "AD-528b v1 Ground-Truth Active Rejection & Quarantine"
    kind: main
    depends_on: ["57"]
    dispatch_prompt: "prompts/WAVE-58-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-528b-active-rejection-v1.md"
    builder_required: true
    issues_to_close: [401]
    status: done

  - id: "59"
    title: "AD-528c v1 Ground-Truth Trust-Network Feedback"
    kind: main
    depends_on: ["58"]
    dispatch_prompt: "prompts/WAVE-59-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-528c-trust-feedback-v1.md"
    builder_required: true
    issues_to_close: [402]
    status: done

  - id: "60"
    title: "AD-458b v1 Pre-Flight Validation LLMTier + TokenBudget Checks"
    kind: main
    depends_on: ["59"]
    dispatch_prompt: "prompts/WAVE-60-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-458b-preflight-validation-v1.md"
    builder_required: true
    issues_to_close: [397]
    status: done

  - id: "61"
    title: "AD-459b v1 Saucer Separation Active Shedding Hooks"
    kind: main
    depends_on: ["60"]
    dispatch_prompt: "prompts/WAVE-61-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-459b-active-shedding-v1.md"
    builder_required: true
    issues_to_close: [396]
    status: done

  - id: "62"
    title: "AD-635b v1 Clinical Telemetry Anomaly Audit Trail Persistence"
    kind: main
    depends_on: ["61"]
    dispatch_prompt: "prompts/WAVE-62-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-635b-anomaly-audit-v1.md"
    builder_required: true
    issues_to_close: [391]
    status: done

  - id: "63"
    title: "AD-635c v1 Clinical Telemetry Circuit Breaker State History"
    kind: main
    depends_on: ["62"]
    dispatch_prompt: "prompts/WAVE-63-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-635c-circuit-breaker-history-v1.md"
    builder_required: true
    issues_to_close: [392]
    status: done

  - id: "64"
    title: "AD-635d v1 Clinical Telemetry REST Endpoints"
    kind: main
    depends_on: ["63"]
    dispatch_prompt: "prompts/WAVE-64-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-635d-clinical-rest-v1.md"
    builder_required: true
    issues_to_close: [393]
    status: done

  - id: "65"
    title: "AD-635e v1 Clinical Telemetry Shell Command"
    kind: main
    depends_on: ["64"]
    dispatch_prompt: "prompts/WAVE-65-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-635e-clinical-shell-v1.md"
    builder_required: true
    issues_to_close: [394]
    status: done

  - id: "66"
    title: "AD-635f v1 Clinical Telemetry Proactive Context Injection"
    kind: main
    depends_on: ["65"]
    dispatch_prompt: "prompts/WAVE-66-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-635f-clinical-proactive-context-v1.md"
    builder_required: true
    issues_to_close: [395]
    status: done

  - id: "67"
    title: "AD-573d v1 Dream-to-Working-Memory Pipeline (combo reframe → single AD)"
    kind: single
    depends_on: ["66"]
    dispatch_prompt: "prompts/WAVE-67-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-573d-dream-to-working-memory.md"
    builder_required: true
    issues_to_close: [8]
    status: done
    notes: |
      Wave-10 reframe applied at AD scoping. Of the original 5-AD combo
      (AD-573b/c/d/e/f), four are NOT outstanding work at HEAD fa6d83d:
        - AD-573b shipped Wave 8 (Combo A)
        - AD-573c shipped Wave 13 (Combo C, commit ffda515)
        - AD-573e wholesale-deferred to AD-573e-i (forcing function:
          cognitive/journal.py needs recency-ordered per-agent recall API)
        - AD-573f shipped Wave 13 (Combo C, commit ffda515)
      AD-573d alone is buildable; its forcing function (runtime.dream_scheduler
      exposing summaries) is satisfied by DreamScheduler.last_dream_report
      property at dreaming.py:2807. Issue #8 closes via partial-completion
      summary table in the close comment.

  - id: "68"
    title: "AD-572b-e Combo Captain Engagement Extensions (no-build close, 4→0 reframe)"
    kind: combo
    depends_on: ["67"]
    dispatch_prompt: "prompts/WAVE-68-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths: []
    builder_required: false
    issues_to_close: [109]
    status: done
    notes: |
      Wave-10 reframe applied at AD scoping (4→0). Every child of the
      AD-572 family is already resolved at HEAD 266391c:
        - AD-572b shipped Wave 8 (Combo A, commit 16c4ea4)
        - AD-572c shipped Wave 13 (Combo C, commit ffda515)
        - AD-572d wholesale-deferred to AD-572d-i (forcing function:
          interruptible-wait infra on proactive.py _think_loop at line 477;
          verified zero asyncio.Event/wait_for in proactive.py at HEAD;
          documented at DECISIONS.md:597 and roadmap.md:4582)
        - AD-572e shipped Wave 18 (commit 9ff7fed)
      No code shipped this wave. Issue #109 closes via partial-completion
      summary in the close comment. The per-AD combo prompt
      (prompts/ad-572b-e-captain-engagement-combo.md) was never drafted
      and is intentionally absent from prompt_paths.

  - id: "69"
    title: "AD-574b-c Combo DM Reply Extensions"
    kind: combo
    depends_on: ["68"]
    dispatch_prompt: "prompts/WAVE-69-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-574b-dm-sync-chat.md"
    builder_required: true
    issues_to_close: [110]
    status: done

  - id: "70"
    title: "AD-526c-h Combo Recreation System Extensions"
    kind: combo
    depends_on: ["69"]
    dispatch_prompt: "prompts/WAVE-70-DISPATCH.md"
    prompts_already_drafted: false
    prompt_paths:
      - "prompts/ad-526e-spectator-registry.md"
    builder_required: true
    issues_to_close: [101]
    status: done

  - id: "71"
    title: "AD-644b Phase 5 _build_prompt_text deprecation (no-build close)"
    kind: single
    depends_on: ["70"]
    dispatch_prompt: "prompts/WAVE-71-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths: []
    builder_required: false
    issues_to_close: [415]
    status: done
    notes: |
      Verify-first against HEAD fe71048 confirms _build_prompt_text is
      already gone from the codebase (sub_task.py is now 620 lines, was 3607).
      Likely removed during AD-644 Phases 1-4 (commit f10369d) or the AD-647
      chain restructure. No-build close.

  - id: "72"
    title: "AD-696 v1 Agentic Oracle Retrieval (on-demand Ship's Records query)"
    kind: single
    depends_on: ["71"]
    dispatch_prompt: "prompts/WAVE-72-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-696-agentic-oracle-retrieval-v1.md"
    builder_required: true
    issues_to_close: [416]
    status: done
    notes: |
      Full-scope v1 per GH #416: oracle_lookup QUERY op + ToolRegistry
      registration (READ, RecallTier.ORACLE gate) + ANALYZE intent signal
      (oracle_query token + oracle_query_text field) + chain dispatch seam
      between triage and execute phases (once per chain via
      _oracle_lookup_fired flag). Reuses existing _oracle_context rendering
      key. Deferred: AD-696b temporal decay, AD-696c query intent
      classification, AD-696d multi-turn retrieval. Baseline 11433 → +13
      target (window [+10, +14]).
  - id: "73"
    title: "AD-462f v1 Memory Architecture: Optimized Memory Representation (retrieval-as-pointers)"
    kind: single
    depends_on: ["72"]
    dispatch_prompt: "prompts/WAVE-73-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-462f-memory-refs-v1.md"
    builder_required: true
    issues_to_close: [58]
    status: done
    notes: |
      Full-closure v1 per GH #58. Pillars 1+2 (structured metadata, concept
      graphs) already shipped via prior ADs — pillar 1 by AD-567a/541/598/579b,
      pillar 2 by AD-688/692; v1 documents that coverage in roadmap and
      decisions. Pillar 3 (retrieval-as-pointers) ships here: MemoryRef
      dataclass in types.py, OracleService.query_refs/resolve_ref/format_refs
      with 256-entry OrderedDict LRU, oracle_refs QUERY op gated at
      RecallTier.ENHANCED (Lieutenant+, lower than AD-696's ORACLE-gated
      oracle_lookup), MEMORY_REFS_DISPATCHED event. Reverses the original
      AD-462f deferral note in decisions-era-4-evolution.md:2699.
      Deferred: AD-462f-1 ToolRegistry registration (same root cause as
      AD-696-1: init_communication signature lacks runtime parameter),
      AD-462f-b ANALYZE intent signal + chain dispatch seam, AD-462f-c
      cross-conversation ref persistence, AD-462f-d per-tier metadata
      contracts. Baseline 11447 → +14 target (window [+11, +15]).
  - id: "74"
    title: "AD-571b + AD-571c v1 Agent Tier Trust Separation (Operational Status + Hebbian Scope Reduction)"
    kind: single
    depends_on: ["73"]
    dispatch_prompt: "prompts/WAVE-74-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-571b-c-tier-trust-v1.md"
    builder_required: true
    issues_to_close: [21]
    status: done
    notes: |
      Closes GH #21 (umbrella AD-571a-c). AD-571a already shipped at HEAD
      4d0242a (AgentTier enum + AgentTierRegistry + crew_only filter on
      TrustNetwork + finalize wiring + 15 tests). Wave 74 ships AD-571b v1
      (OperationalStatus enum + ReliabilityMetrics + OperationalStatusTracker
      in substrate/operational_status.py, in-memory ring buffer, no SQLite,
      single v1 call site at HttpFetchAgent.handle_intent) and AD-571c v1
      (MeshConfig.hebbian_social_decay_rate + per-rel_type decay in
      decay_all() + utility-utility intent prune in record_interaction()).
      Deferred: AD-571b-i (20+ Rank.from_trust call-site sweep, Wave-10 6+
      rule), AD-571b-ii (HXI surfacing, HXI fragility per copilot-instructions),
      AD-571c-i (default flip of social_decay_rate to 0.999, forcing function
      AD-557 benchmark), AD-571c-ii (per-rel_type prune semantic review).
      Baseline 11463 → +16 target (window [+14, +20]).
  - id: "75"
    title: "AD-428b v1 Agent Skill Framework: Advanced Features (5-feature OSS v1)"
    kind: single
    depends_on: ["74"]
    dispatch_prompt: "prompts/WAVE-75-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-428b-skill-framework-advanced-v1.md"
    builder_required: true
    issues_to_close: [38]
    status: done
    notes: |
      Closes GH #38 (umbrella AD-428b "Advanced Features"). Five of nine
      AD-428b roadmap rows ship in v1: (1) composite skills + synergy
      bonuses (SkillDefinition.composite_skill_ids + synergy_partners,
      SkillProfile.has_composite_capability + synergy_bonus); (2)
      development goals (new agent_development_goals table + 3
      AgentSkillService methods + proactive.py:1796 surfacing); (3)
      Earned Agency proficiency-informed rank (new pure inspector
      proficiency_promotion_eligibility in earned_agency.py — does NOT
      modify Rank.from_trust per Wave-74 DLog #5 precedent); (4)
      skill-weighted routing OSS basic (HebbianRouter.set_skill_service
      + set_intent_skill_map + async score_with_skill_weight, new
      MeshConfig.intent_skill_map dict default empty); (5) dream
      consolidation reinforcement (DreamCycle ctor accepts skill_service
      + intent_skill_map, new _reinforce_skills_for_episodes helper at
      _consolidate_trust integration point with per-cluster dedup).
      Force-deferred (no choice): AD-428b-i (Holodeck-based assessment +
      assessment_criteria per level — Holodeck does not exist at HEAD,
      grep "class Holodeck" returns 0 hits); AD-428b-ii (Model-Skill
      Alignment + INNATE category — per-agent ModelDescriptor assignment
      incomplete per AD-463 partial status, ProviderABC/HebbianRouter
      integration deferred to AD-463b/c/d/e/f). Both forcing functions
      file new GH tracking issues at close-comment. Baseline 11480 →
      +18 target (window [+15, +22]).


  - id: "76"
    title: "AD-644 v1 Situation Awareness Architecture (no-build close)"
    kind: single
    depends_on: ["75"]
    dispatch_prompt: "prompts/archive/WAVE-76-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/archive/ad-644-situation-awareness-closure.md"
    builder_required: false
    issues_to_close: [285]
    status: done
    notes: |
      All 5 phases shipped earlier (Phases 1-4 in commit f10369d;
      Phase 5 deprecation in Wave 71 / AD-644b commit 8a20695).
      No-build close.

  - id: "77"
    title: "AD-523a/b/c Combo Ward Room Overhaul (3->1 reframe)"
    kind: combo
    depends_on: ["76"]
    dispatch_prompt: "prompts/archive/WAVE-77-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/archive/ad-523b-crew-notebooks-browser.md"
    builder_required: true
    issues_to_close: [98]
    status: done
    notes: |
      AD-523a shipped via BF-080 (verify-only). AD-523b shipped this wave
      (HXI Notebooks panel, +8 vitest). AD-523c superseded by AD-562
      Knowledge Browser (planned).

  - id: "78"
    title: "AD-569 v1 Behavioral Metrics Extensions (1-build + 5 verify-only + 1 defer)"
    kind: combo
    depends_on: ["77"]
    dispatch_prompt: "prompts/WAVE-78-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-569g-hxi-behavioral-dashboard.md"
    builder_required: true
    issues_to_close: [108]
    status: pending
    notes: |
      Closes GH #108 (umbrella AD-569 a-g). Reframe 7 -> 1-build:
      AD-569a/b/c/e shipped via AD-569 main entry (PROGRESS.md:363);
      AD-569d shipped via AD-583f/g satisfaction (decisions-era-4
      :3219); only AD-569g (HXI Behavioral Dashboard v1) builds in
      this wave - HXI-only panel on top of /api/behavioral-metrics
      (shipped under AD-569). AD-569f (Measurement Framework Infra)
      deferred with forcing function - research-grade psychometric
      layer with no present-day consumer. Vitest-only delta
      (+9 to +12 window). Baseline 11498 -> expected 11498 pytest
      (delta = 0); vitest gate covers the new panel separately.


  - id: "79"
    title: "AD-594d v1 Delivery Pipeline (full-scope)"
    kind: single
    depends_on: ["78"]
    dispatch_prompt: "prompts/WAVE-79-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-594d-delivery-pipeline-v1.md"
    builder_required: true
    issues_to_close: [163]
    status: done
    notes: |
      Closes GH #163 (AD-594d Delivery Pipeline). Full v1 scope in one wave
      per Captain rule (don't defer unless no choice). Module shape:
      FormatTransformer Protocol + 3 concrete transformers (Passthrough,
      MarkdownToHTML stdlib-only, JSONToMarkdown), DeliveryAdapter Protocol
      + LocalFileAdapter (allowed_roots-gated + rollback) + GitHubAdapter
      (httpx + env token + injectable http_post for tests), DeliveryPipeline
      orchestrator with approval gate (requires_approval=True ->
      pipeline.approve/reject), atomic vs partial, revision cycle via
      pipeline.revise -> workspace.transition_to. State-machine extension:
      COMPLETED -> CONSULTING and COMPLETED -> EXECUTING added (ARCHIVED
      still allowed). Pydantic ConsultationDeliveryConfig + finalize wirer.
      PDF rendering deferred behind FormatTransformer Protocol seam (mirrors
      AD-594a InputProcessor precedent). Baseline 11498 -> target 11523-11528
      (+25 floor). No HXI surface. No new EventType. No new Intent. OSS only.

  - id: "80"
    title: "AD-594c v1 Parallel Execution Dispatch (full-scope)"
    kind: single
    depends_on: ["79"]
    dispatch_prompt: "prompts/WAVE-80-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-594c-parallel-dispatch-v1.md"
    builder_required: true
    issues_to_close: [162]
    status: done
    notes: |
      Closes GH #162 (AD-594c Parallel Execution Dispatch). Full v1 scope in
      one wave per Captain rule (don't defer unless no choice). Module shape:
      WorkItemSpec frozen dataclass + PlanDecomposer Protocol +
      MarkdownPlanDecomposer (structured ATX-2 markdown; LLM-driven decomposers
      plug in behind the seam) + ConflictDetector (resource-overlap pairs,
      stateless) + ParallelDispatcher orchestrator (dispatch -> registers
      WorkItems via runtime.work_item_store + tags=["consultation",
      "workspace:<id>"], mirrors specs into workspace.add_work_item, transitions
      APPROVED -> EXECUTING; get_progress aggregates by status; check_completion
      -> EXECUTING -> COMPLETED idempotent; detect_blockers emits
      PARALLEL_DISPATCH_BLOCKED with per-spec dedup ring; revoke for teardown).
      Conflict resolution = synthetic depends_on edges injected in original
      spec list order. 3 new EventTypes (PARALLEL_DISPATCH_STARTED / _PROGRESS
      / _BLOCKED). Pydantic ConsultationDispatchConfig + finalize wirer
      (gated on runtime.consultation_workspaces AND runtime.work_item_store).
      LLM-driven semantic decomposition deferred behind PlanDecomposer Protocol
      seam (mirrors AD-594a InputProcessor + AD-594d FormatTransformer
      precedent). AD-594b (#161 consultation primitive) NOT a build dependency
      — verified zero coupling at HEAD. AD-581 Hybrid Dispatch wiring NOT in
      scope (assigned_to=spec.agent writes a string; ASA hand-off is AD-581's
      surface). Baseline 11528 -> target >= 11553 (+25 floor; 36 tests planned).
      No HXI surface. No new Intent. No router. OSS only.

  - id: "81"
    title: "AD-581 v1 Hybrid Dispatch (3 OSS sub-ADs in one build)"
    kind: single
    depends_on: ["80"]
    dispatch_prompt: "prompts/WAVE-81-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-581-hybrid-dispatch-v1.md"
    builder_required: true
    issues_to_close: [113]
    status: pending
    notes: |
      Closes GH #113 (AD-581 Hybrid Dispatch). 3 OSS sub-ADs in one wave per
      Captain rule (don't defer unless no choice). 581a DepartmentDispatcher
      (pure decision layer; Hebbian + ontology + cold-start floor +
      confidence threshold + runner-up margin), 581a WorkItemRouter (bridges
      WORK_ITEM_CREATED -> AD-654c Dispatcher; consumes AD-594c WorkItems),
      581b OrderState DECLINED/REFUSED + decline()/refuse() + StandingOrder
      Predicate Protocol seam + reassignment_callback hook, 581d
      HybridDispatchConfig + per-(intent, agent_id) success-rate ring buffer.
      4 new EventTypes (ORDER_DECLINED, ORDER_REFUSED, HYBRID_DISPATCH_DIRECT,
      HYBRID_DISPATCH_BROADCAST). Pydantic config + finalize wirer
      (_wire_hybrid_dispatch; gated on hebbian_router + ontology + dispatcher
      + registry; tier-2 log-and-degrade). Commercial sub-ADs 581c (ASA
      Bridge) and 581e (Project Team Dispatch) explicitly out of scope —
      they live in private commercial repo as extension points on top of
      OSS DepartmentDispatcher + OrderProtocol primitives. Standing-Order
      predicate and dream-cycle auto-tuning ship as Protocol seams; concrete
      implementations follow when consumer signal arrives. Baseline 11565 ->
      target >= 11595 (+30 floor; 30 tests planned). No HXI surface. No new
      Intent. No router. No LLM call. No commercial language.

  - id: "82"
    title: "AD-633 v1 Predictive Cognitive Branching (6 OSS sub-ADs concrete + 2 Protocol seams)"
    kind: single
    depends_on: ["81"]
    dispatch_prompt: "prompts/WAVE-82-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-633-predictive-branching-v1.md"
    builder_required: true
    issues_to_close: [228]
    status: pending
    notes: |
      Closes GH #228 (AD-633 Predictive Cognitive Branching umbrella). Six
      OSS sub-ADs ship concretely: 633a PredictionEngine (deterministic
      confidence scoring from Hebbian + thread activity + ontology dept +
      working-memory engagement; ConfidenceTier ZERO_COST/CHEAP/STANDARD/
      ANTICIPATORY; circuit-breaker hard gate), 633b SpeculationCache (TTL +
      FIFO bounded; emits PREDICTION_HIT/FLUSHED) + SpeculationExecutor
      (wraps SubTaskExecutor; speculation-tagged source; emits
      PREDICTION_ERROR_RECORDED on intent-divergence), 633c SpeculationBudget
      (per-agent rolling-window pool separate from operational AD-617;
      Standard-tier requires EarnedAgency >= EXECUTING; flush-rate feedback
      halves budget when 1-hour rolling flush rate >= 30%), 633d
      _decide_via_llm pre-LLM cache check (mirrors AD-585 _knowledge_ambient
      injection shape; tier-2 log-and-degrade; emits PREDICTION_MISS), 633e
      AccuracyTracker (per-agent ring buffer + AccuracyRates), 633h
      PREDICTION_ERROR_RECORDED event-emit (AD-557 listener integration is
      AD-633h-1 follow-on; EmergenceMetricsEngine has no record_signal API
      at HEAD). Two Protocol seams ship with NoOp default impls: 633f
      IdleSpeculationPolicy (concrete impl is AD-633f-1 once accuracy data
      shows >= 10% hit rate), 633g PreplayHook (concrete impl is AD-633g-1
      once dream pipeline exposes step registry). One sub-AD hard-deferred:
      633i Cognitive JIT compilation — consumer (AD-531-539 JIT) absent at
      HEAD; learned_shortcuts/protocol.py:18 documents 'cognitive_jit' as
      future identifier; v1 emits PREDICTION_HIT events JIT will subscribe
      to; no producer-without-consumer code shipped. 4 new EventTypes
      (PREDICTION_HIT, PREDICTION_MISS, PREDICTION_FLUSHED,
      PREDICTION_ERROR_RECORDED). New package
      src/probos/cognitive/predictive_branching/ with 6 modules (engine,
      cache, executor, budget, accuracy, policy). Pydantic
      PredictiveBranchingConfig (default-False, AD-695 precedent — speculation
      consumes real tokens). Finalize wirer _wire_predictive_branching
      invoked immediately after AD-632 SubTaskExecutor wiring; gated on
      hebbian_router + ontology; tier-2 log-and-degrade. Single
      _decide_via_llm modification at the head of the function. Baseline
      11580 -> target >= 11615 (+35 floor; 35 tests planned across 8 test
      classes — engine 8, cache 7, budget 6, accuracy 4, executor 4, policy
      seams 3, config+wiring 2, decision-pipeline integration 1). No HXI
      surface. No new Intent. No router. No LLM call inside engine. No
      commercial language (AD-633 umbrella is fully OSS — zero commercial
      sub-ADs).

  - id: "83"
    title: "AD-482 v1 Self-Improvement Pipeline (8 OSS sub-ADs concrete + 1 Protocol seam)"
    kind: single
    depends_on: ["82"]
    dispatch_prompt: "prompts/WAVE-83-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-482-self-improvement-pipeline-v1.md"
    builder_required: true
    issues_to_close: [76]
    status: pending
    notes: |
      Closes GH #76 (AD-482 Self-Improvement Pipeline umbrella). Eight OSS
      sub-ADs ship concretely: 482a StageContract (frozen dataclass +
      shape-only validators), 482b CapabilityProposal + ProposalStore (typed
      schema for "what was found, why it matters, how it fits"; in-memory
      append-only registry; terminal decisions emit lessons), 482c
      ApprovalGate (queue surface over ProposalStore with approve/reject
      including modifications support + audit log + rationale persistence;
      wraps not replaces SelfModificationPipeline._user_approval_fn), 482d
      EvolutionStore (ChromaDB-backed lessons collection separate from
      EpisodicMemory's "episodes" — name "self_improvement_lessons";
      time-decay weighting 0.5^(age/half_life) with 30-day default;
      log-and-degrade in-memory fallback when chroma absent), 482e
      PivotRefineDecision enum + IterationGuard (PROCEED/REFINE/PIVOT;
      iteration cap 5 default; artifact-version provenance), 482f
      QAAgentPool + Shapley aggregator (wraps existing SystemQAAgent
      template; pool size 3 default; consumes compute_shapley_values from
      consensus/shapley.py; synthetic Vote per QA agent; degrades to
      single-instance when spawner short), 482g AgentVersion +
      AgentVersionStore (in-memory parent-version lineage with per-version
      raw Beta(alpha,beta) trust + source_hash; emits AGENT_VERSION_PROMOTED),
      482h LocalDiskPersistence (writes promoted agents to
      src/probos/agents/designed/{agent_type}_v{N}.py + sidecar
      {agent_type}_v{N}.meta.yaml; Git PR creation is AD-482h-1 follow-on
      with subprocess git + GitHub MCP wiring as separate AD). One
      Protocol seam ships with NoOp default impl: 482i ShadowDeploymentPolicy
      (concrete impl is AD-482i-1 — needs parallel-pool comparator with
      scaler-aware shadow workers, AD-280 territory). ZERO hard-deferrals
      (Captain rule honored — every sub-AD with a consumer at HEAD ships
      concretely or as Protocol seam in v1). 6 new EventTypes
      (CAPABILITY_PROPOSAL_CREATED/APPROVED/REJECTED, PIVOT_REFINE_DECIDED,
      EVOLUTION_LESSON_RECORDED, AGENT_VERSION_PROMOTED). New package
      src/probos/cognitive/self_improvement/ with 6 modules (stage_contract,
      proposal, approval_gate, evolution_store, qa_pool, versioning) +
      __init__.py re-exports. Pydantic SelfImprovementConfig
      (default-False; AD-633 / AD-695 default-False precedent — pipeline
      spawns real QA agents, opens new ChromaDB collection, writes new
      files on promote). Finalize wirer _wire_self_improvement invoked
      immediately after AD-633 _wire_predictive_branching; gated on
      runtime._chroma_client (optional — degrades to in-memory) and
      runtime.spawner (optional — degrades to single-instance pool); tier-2
      log-and-degrade. Reuses existing SystemQAAgent template at
      runtime.py:619 (no new agent class), reuses compute_shapley_values
      at consensus/shapley.py:37 (no new Shapley impl), mirrors
      EpisodicMemory ChromaDB construction shape at episodic.py:732 (no
      new vector DB). 7 public typed runtime attributes (proposal_store,
      approval_gate, evolution_store, qa_agent_pool, agent_version_store,
      agent_persistence, shadow_deployment_policy) initialized to None in
      runtime.__init__ and set by wirer. SelfModificationPipeline ctor
      signature unchanged (Callable shape preserved for back-compat;
      ApprovalGate is parallel surface, not replacement). Baseline 11614
      -> target >= 11654 (+40 floor; 42 tests planned across 11 test
      classes — StageContract 4, CapabilityProposal 4, PivotRefine 3,
      ApprovalGate 6, EvolutionStore 6, QAAgentPool 7, Versioning 3,
      Persistence 4, ShadowSeam 2, Config+Wiring 2, Integration 1). No
      HXI surface (deferred AD-482-HXI follow-on). No new Intent. No
      router. No LLM call inside any v1 module. No commercial language
      (AD-482 umbrella is fully OSS — zero *(Commercial)* tags on any
      sub-AD letter in roadmap.md:3665-3732; pre-commit hook banned phrases (the e-word + tier and the private repo path token) confirmed 0 hits across dispatch, prompt, and this notes block).

  - id: "84"
    title: "AD-512 v1 Discovery-Based Capability Building (6 OSS sub-AD letters concrete)"
    kind: single
    depends_on: ["83"]
    dispatch_prompt: "prompts/WAVE-84-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-512-discovery-learning-v1.md"
    builder_required: true
    issues_to_close: [94]
    status: pending
    notes: |
      Closes GH #94 (AD-512 Discovery-Based Capability Building umbrella).
      Six OSS sub-AD letters ship as observational substrate following the
      AD-507/509/511 v1 precedent: 512a DiscoveryScenarioRegistry (read-only
      catalog of 8 scenarios across 5 capability categories — analysis,
      communication, coordination, construction, diagnosis; mirrors AD-507
      CoreKnowledgeCurriculumRegistry shape; emits DISCOVERY_SCENARIO_OFFERED
      on lookup), 512b StrengthMap (per-agent rolling aggregate;
      record_outcome / get_strengths / get_struggles / success_rate /
      to_episode_payload — payload shape only, substrate NEVER calls
      EpisodicMemory.store; emits DISCOVERY_OUTCOME_RECORDED +
      STRENGTH_MAP_UPDATED; importance=8 baked into payload helper for
      future Holodeck consumer), 512c suggest_routing pure function
      returning CrossFunctionalSuggestion(source, target, success, rel_type,
      rationale) tuple shape — substrate NEVER calls
      HebbianRouter.record_interaction (caller-driven by design; layer
      discipline rationale), 512d frame_as_growth + frame_as_discovery pure
      helpers (idempotent string transforms; "you can't" → "you have not yet
      developed"; Dweck growth-mindset framing), 512e
      CapabilityConfidenceScorer (per-(agent, capability) Beta(α,β)
      accumulator; stores RAW (alpha, beta) per ProbOS trust principle —
      mean = α/(α+β) is computed property; default prior Beta(1,1); emits
      CAPABILITY_CONFIDENCE_UPDATED), 512f ZPDCalibrator (Vygotsky Zone of
      Proximal Development band; compute_band anchors on
      confidence.mean + caller-supplied lower/upper offsets; select_scenarios
      filters by category + difficulty band; scaffolding hint
      high/medium/low/none from confidence mean; emits
      ZPD_SCENARIO_CALIBRATED). ZERO hard-deferrals — every capability
      listed in roadmap line 6407 ships as substrate. ZERO Protocol seams
      needed (consumer wave AD-486 Holodeck wires existing public APIs
      HebbianRouter.record_interaction at routing.py:177 + EpisodicMemory.store
      at episodic.py:942 — Protocol seam would be redundant). 5 new
      EventTypes (DISCOVERY_SCENARIO_OFFERED, DISCOVERY_OUTCOME_RECORDED,
      STRENGTH_MAP_UPDATED, CAPABILITY_CONFIDENCE_UPDATED,
      ZPD_SCENARIO_CALIBRATED) inserted adjacent to BOOT_CAMP_PHASE_ADVANCED
      at events.py:357. New sub-package
      src/probos/crew_development/discovery/ with 6 modules + __init__.py
      re-exports. Pydantic DiscoveryLearningConfig (default-True;
      AD-507/509/511 precedent — observational substrate, zero resource
      creation, zero I/O, zero LLM calls; model_validator(mode='after')
      enforces zpd_lower_bound < zpd_upper_bound). Finalize wirer
      _wire_discovery_learning invoked immediately after AD-509
      _wire_boot_camp_tracker at finalize.py:1311 — keeps Crew
      Development substrate together. 4 public typed runtime attributes
      (discovery_scenario_registry, strength_map,
      capability_confidence_scorer, zpd_calibrator) set by wirer; no
      runtime.__init__ declarations needed (mirrors AD-507/509/511 v1
      precedent). Hebbian writes and episodic encoding are caller
      responsibilities by design (AD-486 Holodeck wave's job) — out of
      scope, NOT deferred. PersonalOntologyProber (AD-487 v1 at
      cognitive/self_distillation/prober.py:66) is a future subscriber to
      STRENGTH_MAP_UPDATED but does NOT subscribe in v1 (AD-487b
      territory). Baseline 11673 -> target >= 11703 (+30 floor; 32 tests
      planned across 8 test classes — DiscoveryScenarioRegistry 4,
      StrengthMap 6, CrossFunctionalSuggestion 3, GrowthMindsetFraming 4,
      CapabilityConfidenceScorer 5, ZPDCalibrator 4, DiscoveryLearningConfig
      3, WiringIntegration 3). No HXI surface. No router. No new Intent.
      No LLM call inside any v1 module. No new agent class. No new pool.
      No persistence (in-memory only; AD-512h territory). No federation
      export (AD-512g territory). No HolodeckRunner module (AD-486 wave's
      job). No edits to AD-507/509/511 v1 substrate. No new AD numbers
      minted (sub-AD letters a-f organizational only — AD-507/509/511 v1
      precedent). No commercial language (AD-512 umbrella is fully OSS —
      zero *(Commercial)* tags on roadmap.md line 6407; pre-commit hook
      banned phrases (the e-word + tier and the private commercial repo
      path token) confirmed 0 hits across dispatch, prompt, and this
      notes block).
  - id: "85"
    title: "AD-473 v1 Mobile PWA (HXI installable shell — manifest + SW + InstallPrompt)"
    kind: single
    depends_on: ["84"]
    dispatch_prompt: "prompts/WAVE-85-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-473-mobile-pwa-v1.md"
    builder_required: true
    issues_to_close: [67]
    status: pending
    notes: |
      Closes GH #67 (AD-473 Mobile Companion umbrella). Three OSS sub-AD
      letters ship concretely: 473a Web App Manifest at
      ui/public/manifest.webmanifest (7 PWA-required fields, three icons —
      192/512/512-maskable), 473b hand-rolled service worker at
      ui/public/sw.js (~38 LOC; install/activate/fetch listeners; versioned
      CACHE_VERSION; skips /api/ and /ws; cache-first app-shell; no Workbox,
      no vite-plugin-pwa), 473c registration helper ui/src/pwa/register.ts
      (returns Promise<ServiceWorkerRegistration | null>; tier-2
      log-and-degrade; carries inline AD-473d Web Push integration-point
      comment) + InstallPrompt component ui/src/components/InstallPrompt.tsx
      (listens for beforeinstallprompt, surfaces stroke-based SVG install
      button matching HXI principle #3 — zero emoji; principle #5
      progressive disclosure — does NOT auto-render). Four future-AD
      letters parked with explicit forcing functions (NOT v1 deferrals):
      473d Web Push (needs Python VAPID + push-subscription endpoints +
      pywebpush dep — UI-only wave cannot ship), 473e Responsive HXI
      mobile layout (Captain UX decisions on chat/canvas/swipe pending —
      HXI principle #1 forbids guessing), 473f mDNS auto-discovery (needs
      Python zeroconf dep + cross-platform startup wiring — UI-only wave
      cannot ship publisher side), 473g native-app wrappers (roadmap
      already says "future stretch" — honoring existing framing).
      ui/index.html gains four <head> entries (manifest link, theme-color
      meta, two apple-mobile-web-app-* metas, apple-touch-icon link)
      adjacent to existing <link rel="icon">. ui/src/main.tsx gains import
      InstallPrompt + import registerServiceWorker + <InstallPrompt />
      sibling render + void registerServiceWorker() fire-and-forget. No
      Python source touched — pytest delta = 0 (gate must remain 11705).
      No new dependencies — pyproject.toml and ui/package.json untouched.
      Baseline vitest 292 (291 passing + 1 pre-existing WardRoomDmSync
      failure NOT introduced by Wave 85) -> target >= 305 (+14 floor; 14
      vitest tests across 4 describe blocks — manifest JSON shape 4,
      registerServiceWorker 4, InstallPrompt 5, viewport-meta verification
      1). No HXI canvas surface touched (CognitiveCanvas, animations,
      GlassLayer, App.tsx untouched). No new EventType, agent, pool,
      Intent, router edit, consensus change, trust scorer touch, episodic
      store touch. No new AD numbers minted (sub-AD letters a-g
      organizational only — AD-512 v1 precedent). No commercial language
      (AD-473 umbrella is fully OSS — zero *(Commercial)* tags on
      roadmap.md line 4205; pre-commit hook banned phrases confirmed 0
      hits across dispatch, prompt, and this notes block).

  - id: "86"
    title: "AD-474 v1 Voice (STT/TTS) — harden + hands-free (test backfill + continuous-listen + onSpeechEnd VAD)"
    kind: single
    depends_on: ["85"]
    dispatch_prompt: "prompts/WAVE-86-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-474-voice-stt-tts-v1.md"
    builder_required: true
    issues_to_close: [68]
    status: pending
    notes: |
      Closes GH #68 (AD-474 Voice Interaction umbrella). The browser-native
      TTS + STT substrate is already shipping live in HXI at HEAD 6e46444
      (ui/src/audio/voice.ts 90 LOC, ui/src/audio/speechInput.ts 78 LOC,
      voice picker in DecisionSurface.tsx, mic button in IntentSurface.tsx,
      voiceEnabled flag in store) but has zero vitest coverage. Three OSS
      sub-AD letters ship concretely: 474a vitest backfill (new files
      ui/src/__tests__/voice.test.ts ~11 tests, ui/src/__tests__/speechInput
      .test.ts ~9 tests in the first describe block — locks
      speakResponse/stopSpeaking/findPreferredVoice cascade/getAvailable
      Voices/setPreferredVoiceName/getCurrentVoiceName for voice.ts;
      isSpeechRecognitionSupported vendor-prefix paths, 3-arg
      startListening defaults, abort-before-restart, onresult/onerror/
      onend lifecycle, stopListening/isListening for speechInput.ts), 474b
      continuous-listen mode (extend startListening to 4-arg with optional
      ListenOptions: {continuous, interimResults, onSpeechEnd} — strictly
      back-compatible with the 3-arg call at IntentSurface.tsx:1467; new
      module-private _spawnRecognition + stopRequested guard + auto-restart
      on onend until stopListening; ~6 tests in second describe block),
      474c onSpeechEnd VAD callback (forwards opts.onSpeechEnd to
      recognition.onspeechend; fires before onend; per-utterance in
      continuous mode; ~4 tests in third describe block). Five future-AD
      letters parked with explicit forcing functions (NOT v1 deferrals):
      474d wake-word detection (needs Porcupine .ppn or OpenWakeWord ONNX
      model file 1-3 MB + audio capture loop — AD-474b continuous mode is
      its hook point; module-private activeOpts/activeOnResult/activeOnEnd/
      activeOnError fields are intentional groundwork marked "AD-474d hook
      point"), 474e SpeechRecognizer ABC + Whisper/Deepgram backends
      (needs Python openai-whisper >100 MB local model OR deepgram-sdk +
      API key + new /api/voice/transcribe endpoint — UI-only wave cannot
      ship), 474f Ship's Computer custom voice (needs Python
      VoiceProvider ABC + Piper/FishAudio/ElevenLabs dep + voice-clone
      artifacts — UI-only wave cannot ship), 474g macOS menubar PTT (needs
      Tauri/Electron desktop wrapper, its own AD class), 474h PWA mobile
      mic UX (cascades from AD-473e responsive HXI mobile layout, parked
      from Wave 85). No Python source touched — pytest delta = 0 (gate
      must remain 11705). No new dependencies — pyproject.toml and
      ui/package.json untouched. Baseline vitest 306 (305 passing + 1
      pre-existing WardRoomDmSync failure NOT introduced by Wave 85 or
      86) -> target >= 334 (+28 floor; 30 tests planned across three
      describe groups). No HXI canvas surface touched (CognitiveCanvas,
      animations, GlassLayer, App.tsx untouched). No edits to consumers
      (IntentSurface.tsx, DecisionSurface.tsx, useStore.ts) — the new
      ListenOptions parameter is optional and call-site-compatible. No
      new EventType, agent, pool, Intent, router edit, consensus change,
      trust scorer touch, episodic store touch. No new AD numbers minted
      (sub-AD letters a-h organizational only — AD-473 a-g (Wave 85) and
      AD-512 a-f (Wave 84) precedents). No commercial language (AD-474
      umbrella is fully OSS — zero *(Commercial)* tags on roadmap.md
      line 4207; pre-commit hook banned phrases confirmed 0 hits across
      dispatch, prompt, and this notes block).

  - id: "87"
    title: "AD-443 v1 Agent Mobility — Transfer Certificates + Memory Portability + chain portability + slot reassignment"
    kind: single
    depends_on: ["86"]
    dispatch_prompt: "prompts/WAVE-87-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-443-mobility-v1.md"
    builder_required: true
    issues_to_close: [42]
    status: pending
    notes: |
      Closes GH #42 (AD-443 Agent Mobility Protocol umbrella). The identity
      substrate AD-443 extends is fully shipped at HEAD 8252468
      (src/probos/identity.py 913 LOC: AgentBirthCertificate VC,
      ShipBirthCertificate VC, LedgerBlock, AgentIdentityRegistry with
      ConnectionFactory-backed storage, _IDENTITY_SCHEMA tables, verify_chain
      local-validation pattern at line 829, export_chain at line 872 produces
      the JSON shape import_chain will consume), and the federation substrate
      is fully shipped (src/probos/federation/bridge.py FederationBridge with
      handle_inbound dispatching on message.type string at line 137,
      transport.py send_to_peer + send_to_all_peers + deliver_response
      message_id correlation, mock_transport.py in-process round-trip,
      FederationMessage type:str at types.py:664 accepts new wire types
      non-breakingly). Five OSS sub-AD letters ship concretely: 443a
      TransferCertificate W3C VC dataclass + issue_transfer_certificate
      (new module src/probos/mobility.py ~250 LOC, identity.py
      transfer_certificates table, ~10 tests), 443b import_chain +
      verify_remote_chain + foreign_chains table (identity.py +~80 LOC, ~12
      tests), 443c MemoryPolicy StrEnum (CLEAN_ROOM default / SELECTIVE /
      FULL) + apply_memory_policy filter + FederationConfig.memory_policy
      Pydantic field with field_validator + config/standing_orders/federation
      .md "Mobility & Memory Portability (AD-443)" addendum after the AD-541
      Memory Reliability Hierarchy section, ~10 tests), 443d reassign_slot +
      foreign_birth_certificates table + transparent get_by_slot foreign
      fallback + birth-provenance preservation per AD-499 (~10 tests), 443e
      FederationBridge transfer_request/transfer_response/chain_request/
      chain_response wire types added to handle_inbound dispatch + new
      handlers _handle_chain_request and _handle_transfer_request + outbound
      helpers request_chain and request_transfer + identity_registry handle
      injected via optional bridge constructor parameter (existing call
      sites unaffected) + MockFederationTransport 2-ship round-trip end-to-
      end test (~10 tests). Six classes total ~57 tests floor. Three future-
      AD letters parked with explicit forcing functions (NOT v1 deferrals):
      443f live cross-process ZMQ multi-instance demo (needs ops-tooling for
      second-instance launch + observability — in-process MockFederation
      Transport round-trip in v1 already proves protocol correctness end-to-
      end), 443g trust-weighted automatic acceptance (depends on AD-479
      federated capability map — not yet shipped), 443h A2A Agent Card
      export of Transfer Certificates (depends on AD-480 — not yet shipped).
      Three commercial-repo carve-outs (NOT deferrals — out-of-repo by
      design at roadmap.md:4095 neutral language "Commercial features
      (Global Instance Registry, fleet dashboard, compliance) tracked in
      the commercial repository"): Global Instance Registry, fleet
      dashboard, fleet-wide compliance auditing. Reframe rationale: the
      original PROGRESS.md line-100 deferral (verify_remote_chain +
      slot-reassignment paths require AD-479) is rejected on verify-first
      against HEAD because (1) verify_remote_chain is pure local hash-chain
      validation given a JSON snapshot, (2) slot reassignment is a local
      SQL update against slot_mappings, (3) AD-479 capability map is for
      "which peer can do X?" intent forwarding, not point-to-point peer-
      addressed mobility messages, (4) MockFederationTransport is the same
      harness already validating intent_request/response correctness today.
      No UI surface touched — vitest unchanged at 306 (305 passing + 1
      pre-existing WardRoomDmSync failure carried since pre-Wave-85). No
      runtime.py boot-ordering changes — AgentIdentityRegistry and
      FederationBridge already wire via infrastructure.py/organization.py.
      No edits to BaseAgent, IntentMessage, IntentResult, TaskDAG, acm.py,
      episodic.py, consensus, trust, attention, dreaming, decomposer,
      prompt builder, shell, panels, HXI. No new EventType, no new pool, no
      new agent, no new Intent, no router edit, no Hebbian touch. No new AD
      numbers minted (sub-AD letters a-h organizational only — AD-474 a-h
      Wave 86, AD-473 a-g Wave 85, AD-512 a-f Wave 84 precedents). No
      commercial language — pre-commit hook 11 banned patterns confirmed 0
      literal hits across dispatch + prompt + this notes block (placeholder
      forms used: "the e-word + tier", "the private commercial-repo path
      token", etc.; the audit text itself does NOT contain literal forms).
      Baseline pytest 11705 -> target >= 11760 (+55 floor; ~57 tests
      planned across six classes TestTransferCertificate ~10,
      TestChainImportVerify ~12, TestSlotReassignment ~10, TestMemoryPolicy
      ~10, TestFederationTransferMessages ~10, TestStandingOrdersFederation
      Tier ~5).

  - id: "88"
    title: "AD-481 v1 Extension-First Architecture — Sealed Core + 8 extension types + skill manifest + toggle + profiles + /extensions slash"
    kind: single
    depends_on: ["87"]
    dispatch_prompt: "prompts/WAVE-88-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-481-extension-first-v1.md"
    builder_required: true
    issues_to_close: [75]
    status: pending
    notes: |
      Closes GH #75 (AD-481 Extension-First Architecture umbrella). All eight
      extension points roadmap.md:3604-3611 enumerates already live at HEAD
      e39e262: AgentRegistry at src/probos/substrate/registry.py:17,
      ToolRegistry at src/probos/tools/registry.py:49 (AD-423a/b),
      SkillRegistry at src/probos/skill_framework.py:427 (AD-428),
      ChannelAdapter ABC at src/probos/channels/base.py:34 (Phase 24),
      IntentBus at src/probos/mesh/intent.py:72, ModelProvider analogue via
      LLMTier config.py:230 (AD-463), EventHook via runtime _emit_event +
      add_event_listener (AD-637d), PerceptionPipeline registry not yet
      shipped (manifest-only declaration in v1). What's missing: the meta-
      layer above the eight points + the on-disk skill.yaml manifest format +
      sealed-core boundary list + extension toggle/profile/persistence
      surface + /extensions slash command. Eight OSS sub-AD letters ship
      concretely: 481a Extension protocol substrate (Extension ABC +
      ExtensionType / ExtensionRiskLevel / ExtensionState StrEnums +
      ExtensionManifest Pydantic + EXTENSION_API_VERSION 1.0.0 semver
      constant + ExtensionsConfig Pydantic, master extensions.enabled flag
      default-False per AD-695 precedent), 481b ExtensionRegistry
      (register/get/list/enable/disable/remove + lifecycle dispatch + LOW
      auto-approve / MEDIUM staged PENDING_APPROVAL / HIGH refused with
      forcing function to AD-482 BuildSpec gate), 481c ExtensionDiscovery
      (filesystem scan over src/probos/extensions/{agents,channels,hooks,
      skills,tools}/ + ExtensionManifest validation + semver major-version
      compatibility check), 481d ExtensionStateStore (extension_states
      SQLite table via ConnectionFactory + load-on-startup state restoration),
      481e Skill manifest format (skill.yaml SkillManifest Pydantic +
      load_skill_from_manifest pure adapter to existing SkillDefinition,
      no breaking change to SkillRegistry, register_from_manifest helper
      added above existing register_skill at line 505), 481f Sealed Core
      boundary (config/sealed_modules.yaml path globs + is_sealed_path
      helper in src/probos/extensions/sealed_core.py + warn-only Builder
      pre-write check at four sites cognitive/builder.py:2585/2604/
      2724/2729, default-False enforce_sealed_core flag per AD-695, hard-
      block deferred to AD-481l), 481g Extension Profiles (three preset
      YAMLs config/extension_profiles/{minimal,developer,full}.yaml +
      ExtensionProfile Pydantic + apply_profile returning enable-list),
      481h /extensions slash command (list/enable/disable/remove/profile/
      info subcommands in commands_extensions.py + two-line shell.py
      wiring at lines 104+290 mirroring AD-596d /skill precedent). 5 future-
      AD letters parked w/ explicit forcing functions: 481i HXI extension
      toggle panel UI (control surface lands at 481h slash command - UI
      follow-up own vitest-budget wave), 481j probos init --profile wizard
      prompt (depends on AD-484c onboarding wizard partial at HEAD per
      roadmap.md:7042), 481k auto-installation of declared skill
      dependencies (touches pip subprocess + needs sandboxing under AD-456
      security_infra - manifest schema declares dependencies field so
      producers record contract, 481k consumer resolves), 481l Builder
      hard-block on sealed paths (default-False flag flip - AD-695
      grandchild precedent - depends on AD-482 v1 RedTeam baseline before
      flipping enforcement), 481m Marketplace publishing/discovery wire
      protocol (depends on AD-480 A2A + AD-479 federation hardening,
      neither shipped - manifest is on-disk substrate that 481m publishes).
      4 commercial-repo carve-outs out-of-repo per roadmap.md:3478 +
      :3595: commercial Agent Marketplace, centralized extension
      distribution / CDN, hosted/managed extension trust scoring +
      revocation registry, paid extension catalog + billing surface.
      Reframe rationale: roadmap line-7033 framing implicitly framed
      AD-482 as a precondition consumer; verify-first against HEAD shows
      the eight extension points already exist - AD-481 is "build the
      meta-layer above the registries", not "build the registries" -
      meta-layer is purely additive substrate landing non-breakingly with
      default-False enforcement flags. Builder pre-write check is pure
      observation hook in v1 (logger.warning only, gated by default-False
      config flag and default-False BuildSpec flag). skill.yaml ships on-
      disk format only, not auto-installation. /extensions slash command
      is same pattern as /skill (AD-596d) - new commands_extensions.py
      module + two-line shell.py wiring. Profiles are three YAML files +
      one Pydantic + one helper function. extension_states SQLite table
      follows established ConnectionFactory pattern (cloud-ready storage
      convention preserved). No UI surface touched - vitest unchanged at
      306 (305 passing + 1 pre-existing WardRoomDmSync failure carried
      since pre-Wave-85). No runtime.py boot-ordering changes - registry
      wires via existing finalize phase. No edits to BaseAgent,
      IntentMessage, IntentResult, TaskDAG, acm.py, episodic.py,
      consensus, trust, attention, dreaming, decomposer, prompt builder.
      No new EventType, no new pool, no new agent, no new Intent, no
      router edit, no Hebbian touch. No new AD numbers minted (sub-AD
      letters a-m organizational only - AD-443 a-h Wave 87, AD-474 a-h
      Wave 86, AD-473 a-g Wave 85, AD-512 a-f Wave 84 precedents). No
      banned-pattern language - pre-commit hook 11 banned patterns
      confirmed 0 literal hits across dispatch + prompt + this notes block
      (placeholder forms used: "the e-word + tier", "the private
      commercial-repo path token", etc.; the audit text itself does NOT
      contain literal forms). Baseline pytest 11762 -> target >= 11842
      (+80 floor; ~80 tests planned across eight classes
      TestExtensionProtocol ~10, TestExtensionRegistry ~12,
      TestExtensionDiscovery ~10, TestExtensionStateStore ~10,
      TestSkillManifest ~8, TestSealedCore ~10, TestExtensionProfiles ~8,
      TestSlashExtensionsCommand ~12).

  - id: "89"
    title: "AD-480 v1 Federation Protocol Adapters — MCP server + A2A both directions"
    kind: single
    depends_on: ["88"]
    dispatch_prompt: "prompts/WAVE-89-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-480-federation-mcp-a2a-v1.md"
    builder_required: true
    issues_to_close: [74]
    status: pending
    notes: |
      Closes GH #74 (AD-480 Federation Protocol Adapters umbrella). The
      substrate AD-480 extends is fully shipped at HEAD 03937cb: outbound
      MCP client at AD-449 (MCPClient/MCPSession/MCPBridge/MCPToolAdapter +
      MCP_BRIDGE_INVOKE/FAILED EventTypes + JSONRPC_VERSION 2.0 +
      MCP_PROTOCOL_VERSION 2025-03-26 constants), federation transport
      (FederationBridge handle_inbound dispatch on message.type string at
      bridge.py:145, FederationRouter.select_peers at router.py:29,
      MockFederationTransport in-process round-trip, FederationMessage
      type:str at types.py:664), IntentBus.broadcast(federated=False) loop
      guard at mesh/intent.py:502, TrustNetwork.create_with_prior at
      consensus/trust.py:195 + record_outcome at line 209, IntentDescriptor
      at types.py:609 with decomposer catalog at runtime.py:2216,
      AgentIdentityRegistry.get_ship_certificate at identity.py:609
      returning ShipBirthCertificate with vessel_name + ship_did, existing
      /federation slash at commands_status.py:100. Nine OSS sub-AD letters
      ship concretely: 480a FederationMCPServer (inbound MCP server -
      JSON-RPC 2.0 over Streamable HTTP, initialize/tools/list/tools/call,
      ASGI via Starlette+uvicorn, governance pass-through via
      IntentBus.broadcast federated-False), 480b Capability-to-MCP-tool
      translator (every IntentDescriptor projected as MCP tool entry with
      JSON-Schema-ish input from descriptor.params), 480c AgentCard
      dataclass (A2A 0.2.0 schema with vessel_name flowing into name +
      ship_did flowing into provider.url per AD-441 / AD-499 connection at
      roadmap.md:7013), 480d FederationA2AServer (inbound A2A server -
      GET /.well-known/agent.json + JSON-RPC tasks/send + tasks/get
      synchronous-only - tasks/sendSubscribe / tasks/cancel /
      tasks/pushNotification/* return -32601 Method not found), 480e
      A2AClient (outbound - discover/send_task/get_task/close, structured
      analogously to existing MCPClient, EgressPolicy-gated), 480f
      FederationPeer + FederationPeerRegistry (protocol literal
      zmq|mcp|a2a discriminator + parallel structure to
      FederationRouter._peer_models - additive, no behavior change for
      single-node or ZeroMQ-only deployments), 480g probationary trust
      wiring (TrustNetwork.create_with_prior peer_id alpha=1.0 beta=3.0 on
      first registration + record_outcome on every forwarded intent +
      destructive intents always require full consensus regardless of
      accumulated trust), 480h config additions
      (FederationMCPServerConfig + FederationA2AConfig + A2APeerConfig +
      FederationPeerTrustConfig - all default-False per AD-695 + W82 +
      W88 default-False precedent), 480i /federation peers slash
      subcommand (extends existing cmd_federation dispatch on first arg -
      no new slash registration, existing /federation panel preserved
      verbatim) + render_federation_peers_panel helper. Nine classes
      total ~72 tests floor. Four future-AD letters parked with explicit
      forcing functions (NOT v1 deferrals): 480j A2A SSE streaming
      tasks/sendSubscribe (depends on observability streaming +
      backpressure substrate unbuilt at HEAD), 480k inbound MCP/A2A
      OAuth 2.1 (depends on AD-449d parked at HEAD per PROGRESS.md:98 -
      v1 accepts opaque bearer tokens via static auth_token config -
      mismatch returns -32600 Invalid Request), 480l cross-protocol
      Hebbian routing (depends on AD-479 federation hardening unshipped
      at HEAD per roadmap.md:3201 - v1 ships trust integration at 480g -
      Hebbian feedback loop is consumer story), 480m A2A push-
      notification callbacks tasks/pushNotification/* (depends on
      outbound webhook delivery substrate unbuilt at HEAD - v1 returns
      -32601 Method not found). Four commercial-repo carve-outs (NOT
      deferrals - out-of-repo by design at roadmap.md:3478 + :3595 +
      :4111 neutral language): hosted multi-tenant MCP/A2A directory
      service, fleet-wide MCP marketplace + paid catalog + billing
      surface, managed cross-fleet A2A trust scoring + signed revocation
      registry, paid pre-built vendor-specific MCP server packs (per
      AD-450 ERP carve-out + AD-449 commercial pack carve-out at
      roadmap.md:4111). Reframe rationale: roadmap line-3247 framing
      (which implicitly framed full Google A2A spec including streaming
      + push notifications + multi-modality as preconditions) revisited
      and rejected on verify-first against HEAD - the substrate AD-480
      extends already exists at HEAD, AD-480 is not "build the federation
      layer" but "build the cross-ecosystem federation surface above the
      existing transports", synchronous-only A2A is feasible and correct
      for v1 (Google A2A spec explicitly distinguishes tasks/send sync
      from tasks/sendSubscribe streaming - sync covers single-shot
      delegation + capability invocation + hailing-frequencies-open
      patterns), MCP server reuses JSON-RPC + Streamable HTTP machinery
      from AD-449 client verbatim (same constants, same envelope, same
      error codes), trust integration reuses TrustNetwork.create_with_
      prior verbatim (no new trust schema, no new dampening config, no
      new tier - external peers are first-class citizens of existing
      Beta(alpha,beta) network with same probationary prior the self-
      mod path uses), FederationPeer discriminator is purely additive
      (FederationRouter.select_peers signature unchanged, parallel
      registry structure, ZeroMQ peers auto-registered via existing
      gossip path, zero behavior change for ZeroMQ-only deployments),
      /federation peers is one-line dispatch extension on existing
      cmd_federation (no new slash registration, no new shell.py
      wiring). No UI surface touched - vitest unchanged at 306 (305
      passing + 1 pre-existing WardRoomDmSync failure carried since
      pre-Wave-85). No runtime.py boot-ordering changes -
      federation_peer_registry initialized eagerly in __init__ adjacent
      to existing self.federation_bridge None init, MCP/A2A servers
      None until startup wires them per default-False enable flags. No
      edits to BaseAgent, IntentMessage, IntentResult, IntentDescriptor,
      TaskDAG, acm.py, episodic.py, consensus, attention, dreaming,
      decomposer, prompt builder, cognitive/standing_orders.py,
      cognitive/builder.py, cognitive/architect.py, HXI, existing
      MCPClient/MCPSession/MCPBridge/MCPToolAdapter (AD-449 surface
      preserved verbatim), existing FederationBridge / FederationRouter
      / FederationTransport / MockFederationTransport /
      NATSFederationTransport (Phase 9 + Wave 87 surface preserved
      verbatim). No new EventType (480a/480d emit existing
      MCP_BRIDGE_INVOKE/FAILED with neutral semantics - "an MCP method
      was invoked" whether server or client - and emit via runtime
      _emit_event for free-form A2A audit log), no new pool, no new
      agent, no new Intent, no router transport rewrite, no Hebbian
      touch, no new SQLite tables (trust scores reuse existing trust_
      scores table, peer registry is in-memory only - operator-driven
      re-registration on boot via static config). No new AD numbers
      minted (sub-AD letters a-m organizational only - AD-481 a-m Wave
      88, AD-443 a-h Wave 87, AD-474 a-h Wave 86, AD-473 a-g Wave 85,
      AD-512 a-f Wave 84 precedents). No banned-pattern language - pre-
      commit hook 11 banned patterns confirmed 0 literal hits across
      dispatch + prompt + this notes block (placeholder forms used:
      "the e-word + tier phrase", "the private commercial-repo path
      token", "the e-word overlay phrase", "the e-word-prefixed repo
      token", "monthly-price regex", "per-month abbreviation regex",
      "rev-proj phrase", "the recurring-revenue acronym", "outcome-
      style pricing phrase", "the GTM-pattern phrase", "the patterns-
      to-absorb phrase"; the audit text itself does NOT contain
      literal forms). Baseline pytest 11843 -> target >= 11913 (+70
      floor; ~72 tests planned across nine classes
      TestFederationPeerRegistry ~6, TestAgentCard ~8, TestA2AClient
      ~8, TestA2AServerInbound ~10, TestMCPServerInbound ~12,
      TestProbationaryTrustWiring ~6, TestFederationRouterPolymorphism
      ~6, TestFederationConfigSchema ~4, TestSlashFederationPeersCommand
      ~6, TestStartupWiring ~6).
  - id: "90"
    title: "AD-462 Biological Memory Model — Umbrella Close (no-build, tracker reconciliation)"
    kind: single
    depends_on: ["89"]
    dispatch_prompt: "prompts/WAVE-90-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-462-biological-memory-umbrella-close.md"
    builder_required: true
    issues_to_close: [111]
    status: pending
    notes: |
      No-build close of GH #111 (AD-462 umbrella). All six sub-ADs
      shipped at HEAD 89d4fa7: AD-462a absorbed by AD-567b
      (salience-weighted recall), AD-462b absorbed by AD-567d
      (ActivationTracker), AD-462c (RecallTier enum), AD-462d
      (SocialMemoryService — also absorbed by AD-567f), AD-462e
      (OracleService cross-tier unified query), AD-462f (MemoryRef
      retrieval-as-pointers, Wave 73 commit f5bd612). Pillar 1
      biological memory staging is the conceptual frame realised across
      EpisodicMemory.recent_for_agent (sensory buffer at episodic.py:
      1815), ChromaDB store (short-term), and ProcedureStore + Records
      Store consolidated by DreamingEngine (long-term, dreaming.py:72,
      78, 105, 114) — no separate sub-AD. Wave 73 Builder shipped AD-
      462f code but skipped the tracker updates required by the W73
      dispatch — at HEAD roadmap.md:4168 still says AD-462 *(planned)*,
      roadmap.md:4177 still says AD-462f *(planned)*, decisions-era-4-
      evolution.md:2699 still says "AD-462f DEFERRED", decisions-era-4-
      evolution.md:2690 still says "AD-462f (concept graphs) deferred".
      Wave 90 reconciles by flipping the umbrella status to *(complete
      via AD-462a-f)*, refreshing the AD-462f sub-AD bullet to
      *(COMPLETE — Wave 73, 2026-05-05)* with a one-line summary,
      flipping the era-4 table row at :2699, updating the era-4 prose
      at :2690, and appending a new ### AD-462f closure paragraph
      between the existing AD-462c/d/e cluster and the AD-570b heading
      at :2701. Carry-forward children AD-462f-1 (ToolRegistry
      registration), AD-462f-b (ANALYZE intent signal + chain dispatch
      seam), AD-462f-c (cross-conversation ref persistence), AD-462f-d
      (per-tier metadata contracts) are W73 deferrals NOT W90 deferrals
      — their forcing functions live in prompts/archive/WAVE-73-
      DISPATCH.md lines 31, 49, 64, 90. The umbrella close cites them
      so #111 readers can trace the story; W90 mints zero new GH
      issues. No code touched, no tests added, no pytest delta (target
      11916 -> 11916). No commercial leak — AD-462 is purely OSS
      cognitive architecture (10-bit bottleneck principle, biological
      memory staging, ACT-R activation model, ward-room social memory,
      oracle cross-tier query, retrieval-as-pointers projection); zero
      tier/pricing/SaaS surface anywhere in the umbrella. Pre-commit-
      hook 11 banned-pattern audit on dispatch + prompt + this notes
      block: 0 literal hits across all forms (Wave 87/88/89 placeholder
      convention applied — audit prose uses descriptor-only language,
      e.g. "tier-noun phrase" / "private commercial-repo path token" /
      "monthly-price regex" without invoking the literal forms).
      Reframe rationale: there is genuinely nothing to defer because
      there is nothing to add — the entire umbrella is already
      shipped. Captain rule "don't defer unless no choice" satisfied
      vacuously. Wave 90 is structurally identical to Wave 71 (#415
      AD-644b) and Wave 76 (#285 AD-644 SA architecture) — both clean
      no-build closes flipping tracker entries to reflect work that
      already shipped under earlier ADs. Builder cycle: read prompt,
      apply 6 SEARCH/REPLACE pairs grouped into 3 MODIFY blocks (2
      pairs in roadmap.md, 3 sequential pairs in decisions-era-4-
      evolution.md, 1 pair in wave-plan.yaml — the wave-plan one is
      this entry attached to the W89 tail), no test runs needed since
      no source touched, full gate as belt-and-braces should pass at
      11916 unchanged, gh issue close 111 with the canonical paragraph
      provided in Section 4 of the per-AD prompt.

  - id: "91"
    title: "AD-479 v1 Federation Hardening — capability-aware routing + federated recall + designed-agent sharing + cluster health + multicast discovery + TLS surface"
    kind: single
    depends_on: ["90"]
    dispatch_prompt: "prompts/WAVE-91-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-479-federation-hardening-v1.md"
    builder_required: true
    issues_to_close: [73]
    status: pending
    notes: |
      AD-479 Federation Hardening v1 — closes GH #73. HEAD 4f18c7d
      (Captain reference 86a9fe5 origin/main, equivalent for verify-
      first since the BF between them does not touch federation files).
      Baseline pytest 11897 -> target >= 11960 (+63 floor; ~71 tests
      planned across nine classes: TestCapabilityAwareSelectPeers ~6,
      TestTrustWeightedRanking ~8, TestFederationHebbianRouting ~8,
      TestRecallFederated ~10, TestShareDesignedAgent ~8,
      TestFederationTLSConfig ~5, TestClusterHealthMonitor ~8,
      TestMulticastDiscovery ~8, TestSlashFederationRoutingCommand ~6,
      plus ~4 startup/runtime wiring tests).

      Nine concrete v1 sub-AD letters: 479a capability-aware
      FederationRouter.select_peers (replaces Phase-9 placeholder
      "return all available_peers" at router.py:32 with peer_has_
      capability filter; bootstrap fall-through preserves W87/W89 zero-
      data behavior); 479b trust-weighted peer ranking (optional
      trust_network on FederationRouter; min_trust_score gate; per-
      result outcome update wired from FederationBridge.forward_intent
      via new _record_zmq_peer_outcome helper; trust record id
      namespaced federation_peer:{node_id} to avoid AD-480f mcp-peer:/
      a2a-peer: collision); 479c FederationHebbianMap (intent x peer
      keyed Hebbian weights mirroring AD-271/274 HebbianRouter
      ConnectionFactory pattern at routing.py:39; persists to separate
      federation_hebbian_weights SQLite table per Hard-stop W91-3); 479d
      recall_federated IntentDescriptor + FederationRecallAgent core
      agent (read-only no-consensus, broadcasts via existing bridge.
      forward_intent, aggregates Episode snapshots, dedupes by
      episode_id); 479e share_designed_agent (extends AD-443e
      transfer_request payload with optional designed_agent_payload
      field; receiver runs CodeValidator.validate_text gate before
      AgentDesigner.register_designed_template_from_payload; new
      EventType FEDERATION_DESIGNED_AGENT_RECEIVED); 479f
      FederationTLSConfig Pydantic model on FederationConfig (default-
      False enabled, cert/key/ca/verify_peer fields; NATS path passes
      through to nats-py TLS context via existing NATSBus.config.tls
      injection; ZMQ CURVE parked as AD-479l forcing function); 479g
      FederationClusterMonitor (polls bridge._router._peer_models every
      gossip_interval_seconds * 3 and flips peers older than peer_
      unreachable_seconds=60.0 to unreachable; select_peers filters
      unreachable peers; new EventTypes FEDERATION_PEER_UNREACHABLE +
      FEDERATION_PEER_RECOVERED; auto-recovery is gossip arriving
      again); 479h MulticastDiscovery (opt-in default-False per
      AD-695/W82/W88 precedent; raw UDP multicast announce + listen;
      new FederationBridge.add_peer public method + transport.add_peer
      extension on Mock + NATS + ZMQ transports; new EventType
      FEDERATION_PEER_DISCOVERED; networking-bound tests gated by
      pytest.mark.skipif(not _multicast_available()) per Hard-stop
      W91-2); 479i /federation routing slash subcommand (extends
      AD-480i cmd_federation dispatcher with routing arg; new
      panels.render_federation_routing_panel showing capability /
      trust / Hebbian / unreachable per peer; existing /federation no-
      arg + /federation peers AD-480i preserved verbatim).

      Four future sub-AD letters with crisp upstream blockers (NOT
      scope cuts): 479j mDNS cross-LAN discovery via optional zeroconf
      package (forcing function: cross-LAN multi-instance demo wave;
      v1 raw UDP multicast covers local broadcast domain only); 479k
      cross-protocol Hebbian routing for MCP/A2A peers (forcing
      function: AD-480l revival paired with AD-479c substrate now that
      W91 has shipped it; the W89 archive line 19 explicitly parked
      AD-480l depending on AD-479c shipping); 479l ZeroMQ CURVE
      encryption + TLS hot rotation (forcing function: ZMQ becomes
      production transport again after AD-637e moved default federation
      traffic to NATS; v1 surfaces FederationTLSConfig + wires NATS
      path that handles 100% of NATS-based deployments); 479m
      distributed ChromaDB sharding for federated recall scale-out
      (forcing function: scale-out wave once federation has >100 ships
      where v1 round-trip-full-Episode-summaries-per-peer hits NATS
      payload limits; for <=100 ships and k<=20 the v1 approach works).

      Three commercial-repo carve-outs (out-of-repo by roadmap design,
      NOT v1 deferrals): hosted multi-tenant cross-fleet trust scoring
      service (per roadmap.md:4111 cross-fleet line), fleet management
      dashboard (per roadmap.md:4095 fleet dashboard line), fleet-wide
      compliance + the recurring-revenue acronym surface + the
      patterns-to-absorb phrase + the GTM-pattern phrase + outcome-
      style pricing phrase tied to centralized agent marketplace (per
      roadmap.md:3478 + :3595 + :4111). Tracked in the private
      commercial-repo path token. Audit prose in dispatch + per-AD
      prompt + this notes block uses descriptor-only language.

      Reframe rationale: every roadmap.md:7027 component above un-
      shipped substrate ships in v1. The four AD-479j/k/l/m deferrals
      all have crisp upstream blockers documented above. Captain rule
      "don't defer unless no choice" satisfied — the unblocked-
      substrate carve-out is empty. Every roadmap.md:3202 - :3207
      component (peer discovery / cross-node memory / cross-node agent
      sharing / smart capability routing / federation TLS-auth /
      cluster management) is addressed by at least one shipped 479
      sub-AD letter. Substrate already at HEAD verified by 16 grep-
      evidence lines in the per-AD prompt's Section 12 Verified
      Against Codebase footer. AD-637e NATS federation transport is
      COMPLETE at HEAD - W91 TLS work flows through NATS + parks ZMQ
      CURVE because NATS is the production transport now. AD-443e
      mobility chain transfer wire types already exist - W91 479e is
      a thin extension adding designed_agent_payload field + Code
      Validator gate, NOT a new wire protocol. AD-271/274 HebbianRouter
      ConnectionFactory pattern reused verbatim - W91 479c uses the
      same default_factory + sqlite_factory.default_factory + table-
      with-CREATE-IF-NOT-EXISTS pattern, separate table name per
      Hard-stop W91-3.

      EventType audit: 4 new types collision-free against HEAD
      (Select-String -Path src/probos/events.py -Pattern '^\s+([A-Z_]+)\s*='
      returned no FEDERATION_* matches): FEDERATION_PEER_UNREACHABLE,
      FEDERATION_PEER_RECOVERED, FEDERATION_PEER_DISCOVERED,
      FEDERATION_DESIGNED_AGENT_RECEIVED.

      AD numbering: highest stem at HEAD remains AD-696 (Wave 72,
      verified 2026-05-06 by Select-String over PROGRESS.md +
      DECISIONS.md + roadmap.md returning 696/695/694). AD-479 pre-
      allocated by docs/development/roadmap.md:3202 + :7027. Sub-AD
      letters a-m organizational catalog markers only, mirroring
      AD-480 a-m Wave 89, AD-481 a-m Wave 88, AD-443 a-h Wave 87
      precedents - no new AD numbers minted.

      Hard-stop conditions specific to W91: W91-1 CodeValidator
      rejecting every test designed-agent payload in 479e (Builder
      must NOT loosen validator gate, surface back to Architect);
      W91-2 UDP multicast bind failing on test runner (skip
      networking tests, do not disable feature); W91-3 SQLite schema
      collision with AD-271/274 hebbian_weights table (Builder must
      use separate federation_hebbian_weights table). Standard W87/
      W89 hard-stops also apply.

      Banned-pattern audit on dispatch + per-AD prompt + this notes
      block: 11 patterns checked, 0 literal hits per pattern.
      Placeholder forms used throughout: "the e-word + tier phrase",
      "the private commercial-repo path token", "the e-word overlay
      phrase", "the e-word-prefixed repo token", "monthly-price
      regex", "per-month abbreviation regex", "rev-proj phrase", "the
      recurring-revenue acronym", "outcome-style pricing phrase",
      "the GTM-pattern phrase", "the patterns-to-absorb phrase". Pre-
      commit-hook simulation Select-String -Path prompts/WAVE-91-
      DISPATCH.md, prompts/ad-479-federation-hardening-v1.md -Pattern
      <pattern> -SimpleMatch returns zero hits per pattern.

      4 review passes recorded in this draft session: P1 (initial
      draft against HEAD 4f18c7d - structure mirrored on W89 + W90),
      P2 (verify-first sweep - all 16 grep-evidence lines confirmed
      against HEAD, AD-637e NATS-completeness flagged and 479f scope
      revised to NATS-only with 479l forcing function), P3 (dispatch
      reframe table - confirmed 9-letter v1 + 4-letter forcing-
      function deferred + 3 commercial carve-outs + 0 hard-deferrals
      satisfies Captain rule), P4 (banned-pattern audit on full text
      + per-section consistency check between dispatch + prompt +
      this entry; AD numbering verified 696 highest, no collision).

      Builder execution: read prompt top-to-bottom, apply nine
      sections in order (479a -> 479b -> 479c -> 479d -> 479e -> 479f
      -> 479g -> 479h -> 479i), focused per-section pytest gate after
      each section, full parallel gate before commit. Tracker updates
      land in roadmap.md (AD-479 entry flipped from planned to partial
      with sub-AD letter breakdown) + decisions-era-4-evolution.md
      (new ### AD-479 section appended before AD-636) + this wave-plan
      entry. gh issue close 73 with the canonical paragraph in
      Section 9 of the per-AD prompt (Section 11 Acceptance Criteria
      item 10).

  - id: "92"
    title: "AD-607 v1 Memory Security Framework — extraction & poisoning defense (retrieval / response / privacy layers + store-time prompt-injection + DP aggregation + /security memory)"
    kind: single
    depends_on: ["91"]
    dispatch_prompt: "prompts/WAVE-92-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-607-memory-security-framework-v1.md"
    builder_required: true
    issues_to_close: [183]
    status: pending
    notes: |
      AD-607 Memory Security Framework v1 — closes GH #183. HEAD 255c52a
      (matches Captain reference origin/main exactly; no upstream BFs).
      Baseline pytest 11963 -> target >= 12035 (+72 floor; ~75 tests
      planned across ten classes: TestRecallAnomalyValidation ~8,
      TestProvenanceIntegrity ~7, TestAnchorMismatch ~6,
      TestMemoryLeakageGuard ~8, TestOracleAccessPolicy ~10,
      TestFederationInboundSanitization ~8, TestFederationOutboundPrivacy ~8,
      TestPromptInjectionStoreGate ~8, TestDifferentialPrivacyAggregation ~6,
      TestSecurityMemorySlashCommand ~6, plus ~3 startup/runtime wiring
      tests on the new MemorySecurityRegistry).

      Ten concrete v1 sub-AD letters: 607a validate_recall_result anomaly
      gate (new module memory_security.py; observational by default;
      hooked into EpisodicMemory.recall + recall_by_anchor_scored +
      OracleService._query_episodic; emits MEMORY_RECALL_ANOMALY); 607b
      validate_provenance integrity check (Episode.agent_ids non-empty
      + source in known MemorySource enum + correlation_id non-empty
      when source=direct; emits MEMORY_PROVENANCE_GAP); 607c
      score_anchor_mismatch (reuses AD-567c anchor_dimension_weights
      inverted as anomaly signal; emits MEMORY_ANCHOR_MISMATCH above
      anchor_mismatch_threshold default 0.7); 607d check_memory_leakage
      response-based guard (slotted alongside AD-589
      IntrospectiveFaithfulness in cognitive_agent.py post-decision
      block; flags responses with >=20-char overlap with foreign-shard
      episodes; emits MEMORY_LEAK_SUSPECTED; observational v1, no
      response mutation); 607e MemoryAccessPolicy enum
      (PERMISSIVE/OWN_SHARD_ONLY/OWN_SHARD_PLUS_PUBLIC) on
      OracleService.query via new caller_sovereign_id + access_policy
      kwargs (default PERMISSIVE preserves AD-462c cross-shard recall
      behavior); 607f federated-recall inbound sanitization
      (FederationRecallAgent.act runs validate_recall_result +
      validate_provenance + new validate_inbound_classification on
      every incoming peer episode unconditionally; emits
      FEDERATION_EPISODE_REJECTED; receiver always owns its boundary
      so no opt-out); 607g federated-recall outbound privacy filter
      (FederationConfig.memory_access_policy: public | shared_trust |
      private; default shared_trust drops private-classified episodes
      while honoring AD-479b peer trust ranking shipped by W91; public
      mode applies AD-607i DP redaction); 607h store-time prompt-
      injection detection (MemorySecurityGate evaluate_store mirrors
      AD-610 _storage_gate slot pattern at episodic.py:949; pattern
      set covers ignore_previous + role_swap + tool_spoof +
      system_prompt_leak shapes; emits MEMORY_INJECTION_SUSPECTED;
      enforce_store=False default observational); 607i aggregate_with_dp
      pure helper (blanks Episode.user_input + dag_summary when fewer
      than min_cohort_size=3 unique sovereign_ids contributed; wired
      ONLY through 607g federated-outbound public path per W92-4
      hard-stop — local consumers see episodes verbatim); 607j
      /security memory slash subcommand (MemorySecurityRegistry
      24h-window counter aggregator surfaces 7-EventType counts;
      extends cmd_security via the AD-479i /federation routing
      precedent; existing /security subcommands preserved verbatim).

      Three future sub-AD letters with crisp upstream blockers (NOT
      scope cuts): 607k ML-based extraction-attack classifier
      (forcing function: 90 days of MEMORY_RECALL_ANOMALY accumulation
      + AD-466-style red-team campaign generates labeled adversarial
      query corpus); 607l hardware-attested memory bank via TEE / SGX
      / SEV-SNP (forcing function: hosted deployment that opts into a
      TEE platform; outside zero-config OSS boot promise); 607m
      cross-fleet privacy-budget tracker (forcing function: cross-
      fleet operations becoming a deployed surface beyond single-
      cluster federation; this is the hosted-multi-tenant carve-out
      path, not a public-runtime concern).

      Three commercial-repo carve-outs (out-of-repo by roadmap design,
      NOT v1 deferrals): hosted memory-security-as-a-service offering
      centralized adversarial-query corpus + ML-classifier model
      distribution (per roadmap.md:3478 + :4111), fleet-wide
      compliance + audit trail aggregation across customer fleets
      (per roadmap.md:4095 + :4111), customer-isolation memory-
      classification overlay built on the v1 MemoryAccessPolicy enum
      (per roadmap.md:3595 + :4111). Tracked in the private
      commercial-repo path token. Audit prose in dispatch + per-AD
      prompt + this notes block uses descriptor-only language only.

      Reframe rationale: every survey-Section-8 + roadmap.md:5266
      design-bullet ships in v1. The three AD-607k/l/m deferrals all
      have crisp upstream blockers (labeled-adversarial-corpus / TEE-
      platform-infra / cross-fleet hosted surface) documented above.
      Captain rule "don't defer unless no choice" satisfied — the
      unblocked-substrate carve-out is empty. Substrate already at
      HEAD verified by 16+ grep-evidence lines in the per-AD prompt's
      Section 12 Verified Against Codebase footer. AD-479
      FederationRecallAgent shipped Wave 91 — W92 607f/g extend it
      without touching the transport layer. AD-589 IntrospectiveFaith-
      fulness pipeline shipped already — W92 607d slots alongside as
      a sibling check, NOT a replacement. AD-610 _storage_gate slot
      pattern at episodic.py:949 is the canonical extension point for
      607h — same evaluate() contract, separate concern.

      EventType audit: 7 new types collision-free against HEAD
      (Select-String -Path src/probos/events.py -Pattern
      'MEMORY_RECALL_ANOMALY|MEMORY_PROVENANCE_GAP|MEMORY_ANCHOR_MISMATCH|MEMORY_LEAK_SUSPECTED|MEMORY_INJECTION_SUSPECTED|FEDERATION_EPISODE_REJECTED|FEDERATION_RECALL_DP_REDACTED'
      returned no matches): MEMORY_RECALL_ANOMALY,
      MEMORY_PROVENANCE_GAP, MEMORY_ANCHOR_MISMATCH,
      MEMORY_LEAK_SUSPECTED, MEMORY_INJECTION_SUSPECTED,
      FEDERATION_EPISODE_REJECTED, FEDERATION_RECALL_DP_REDACTED.

      AD numbering: highest stem at HEAD remains AD-696 (Wave 72,
      verified 2026-05-07 by Select-String over PROGRESS.md +
      DECISIONS.md + decisions-era-*.md + roadmap.md returning
      696/695/694). AD-607 pre-allocated by docs/development/
      roadmap.md:5266 + decisions-era-4-evolution.md:4005. Sub-AD
      letters a-m organizational catalog markers only, mirroring
      AD-479 a-m Wave 91, AD-480 a-m Wave 89, AD-481 a-m Wave 88,
      AD-443 a-h Wave 87 precedents — no new AD numbers minted.

      Hard-stop conditions specific to W92: W92-1 prompt-injection
      patterns produce >5% FP on legitimate test fixtures (Builder
      surfaces back to Architect, do not loosen patterns); W92-2
      OWN_SHARD_ONLY breaks an existing test that depended on cross-
      shard recall (Builder makes failing test pass explicit
      access_policy= kwarg, do not change default away from
      PERMISSIVE); W92-3 federation inbound rejects every test peer
      episode in tests/test_ad479_federation_hardening.py (Builder
      updates fixture quality to satisfy provenance integrity, do
      not loosen validator); W92-4 DP redaction blanks Episode.user_input
      that local consumer tests assert on (Builder wires DP redaction
      ONLY through 607g federated outbound path, NOT through
      Oracle.query or local recall). Standard W87/W89/W91 hard-stops
      also apply.

      Banned-pattern audit on dispatch + per-AD prompt + this notes
      block: 11 patterns checked, 0 literal hits per pattern.
      Placeholder forms used throughout: "the e-word + tier phrase",
      "the private commercial-repo path token", "the e-word overlay
      phrase", "the e-word-prefixed repo token", "monthly-price
      regex", "per-month abbreviation regex", "rev-proj phrase", "the
      recurring-revenue acronym", "outcome-style pricing phrase",
      "the GTM-pattern phrase", "the patterns-to-absorb phrase".
      Pre-commit-hook simulation Select-String -Path
      prompts/WAVE-92-DISPATCH.md, prompts/ad-607-memory-security-
      framework-v1.md -Pattern <pattern> -SimpleMatch returns zero
      hits per pattern.

      4 review passes recorded in this draft session: P1 (initial
      draft against HEAD 255c52a — structure mirrored on W91), P2
      (verify-first sweep — all 16+ grep-evidence lines confirmed
      against HEAD; FederationRecallAgent at agents/federation_recall_agent.py:23
      confirmed shipped W91; AD-589 IntrospectiveFaithfulness post-
      decision block confirmed shipped via cognitive_agent.py +
      memory_probes.py consumers), P3 (reframe table — confirmed
      10-letter v1 + 3-letter forcing-function deferred + 3
      commercial carve-outs + 0 hard-deferrals satisfies Captain
      rule), P4 (banned-pattern audit on full text + per-section
      consistency check between dispatch + prompt + this entry; AD
      numbering verified 696 highest, no collision; 7 EventType names
      verified collision-free).

      Builder execution: read prompt top-to-bottom, apply ten
      sections in order (607a -> 607b -> 607c -> 607d -> 607e -> 607f
      -> 607g+607i -> 607h -> 607j; 607g and 607i ship together in
      Section 7) with focused per-section
      pytest gate after each section, full parallel gate before
      commit. Tracker updates land in roadmap.md (AD-607 entry
      flipped from planned to complete with 10-letter sub-AD
      breakdown) + decisions-era-4-evolution.md (new ### AD-607
      section appended before AD-636) + this wave-plan entry. gh
      issue close 183 with the canonical paragraph in Section 10E of
      the per-AD prompt (Section 11 Acceptance Criteria item 13).

  - id: "93"
    title: "AD-628 v1 Crew Skill Readiness Monitoring + Training Officer Role — skill telemetry events + readiness profile + TRAINO crew agent + drill calendar + onboarding mentor + readiness reporter + LIMDU protocol + /readiness slash command"
    kind: single
    depends_on: ["92"]
    dispatch_prompt: "prompts/WAVE-93-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-628-crew-skill-readiness-v1.md"
    builder_required: true
    issues_to_close: [223]
    status: pending
    notes: |
      AD-628 Crew Skill Readiness Monitoring + Training Officer Role v1
      — closes GH #223. HEAD c6c38ec (Captain reference c6c38ec
      origin/main, exact match — no upstream BF commits between Captain
      HEAD and draft HEAD). Baseline pytest 12041 -> target >= 12107
      (+66 floor; ~70 tests planned across nine classes:
      TestSkillTelemetryEvents ~10, TestSkillReadinessProfile ~8,
      TestTrainingOfficerCrewProfile ~6, TestDrillCalendar ~10,
      TestOnboardingMentorAnnouncer ~6, TestReadinessReporter ~10,
      TestLIMDUProtocol ~10, TestReadinessSlashCommand ~6,
      TestStartupWiring ~4).

      Eight concrete v1 sub-AD letters: 628a skill telemetry events
      (six new EventType values SKILL_LOADED/BLOCKED/EXERCISED/
      REGRESSION/DECAY/ACQUIRED + AgentSkillService.set_event_emitter
      setter mirroring AD-526d _emit_event_fn precedent +
      emission inside acquire_skill/update_proficiency/record_exercise/
      check_decay mutation methods at skill_framework.py:624/689/
      712/722 — tier-2 log-and-degrade); 628b AgentSkillReadinessProfile
      + AgentSkillReadinessService in new module
      cognitive/skill_readiness.py — pure read-side aggregator over
      existing SkillFramework + qual_skill_bridge.SkillAdvancement
      history + new internal SkillRegressionEvent ring; 628c Training
      Officer crew agent — config-only crew profile per AD-339
      standing-orders precedent (new training_officer.yaml + chief_
      training post + Tucker assignment in organization.yaml; 15th
      assignment); 628d DrillCalendar in new module
      cognitive/drill_calendar.py — qualification-test scheduling
      queue layered atop AD-477 QualificationHarness, NO Holodeck
      dependency; 628e mentor-announcer hook into AD-515
      AgentOnboardingService via register_mentor_announcer setter +
      invocation just before the AD-499 self-naming event-log block at
      agent_onboarding.py:233; 628f ReadinessReporter in new module cognitive/
      readiness_reporter.py with DepartmentReadinessReport +
      ShipReadinessReport + C-rating mapping (C1/C2/C3/C4 thresholds
      0.85/0.70/0.50); 628g LIMDUService in new module
      cognitive/limdu.py — joint-authority via two required
      keyword-only parameters medical_callsign + traino_callsign;
      auto-populated remediation plan via DrillCalendar; new
      LIMDU_RECOMMENDED EventType; consumes AD-506a CognitiveZone
      enum; 628h /readiness slash command in commands_status.py per
      cmd_status precedent — minted in v1 to keep operator surface
      honest.

      Three future sub-AD letters parked w/ forcing functions: 628d-1
      Holodeck Scenario Selection (forcing function: AD-486 Birth
      Chamber + AD-539b Holodeck scenarios ship the Holodeck primitive
      — verified absent at HEAD by grep returning zero implementation
      hits), 628e-1 Birth Chamber Mentor Embodiment (forcing function:
      AD-486 ships Birth Chamber substrate), 628i Durable LIMDU +
      DrillCalendar persistence (forcing function: 30 days of
      in-memory operation reveals access patterns OR
      hosted-deployment opt-in justifies durable schema). Three
      commercial-repo carve-outs out-of-repo per roadmap.md:4095/4111
      — fleet-wide cross-ship readiness rollup, customer-facing
      PQS-board examination service, outcome-style
      readiness-improvement consulting overlay.

      Reframe: TWO letters reframed mid-spec to avoid AD-486/AD-539b
      substrate gap — 628d Holodeck-scenario framing replaced by
      qualification-test scheduling on top of shipped AD-477
      QualificationHarness; 628e Birth-Chamber-embodied mentorship
      replaced by DM-style announcement at the existing post-naming-
      ceremony block. Neither reframe drops scope from issue #223
      sub-AD list a-g. Eight letters ship in v1 + three forcing-
      function future-AD letters. Captain rule "don't defer unless no
      choice" satisfied — every issue-#223 sub-AD letter ships and
      every deferral has a crisp upstream blocker.

      Banned-pattern audit: 0 literal hits across all 11 patterns on
      WAVE-93-DISPATCH.md + ad-628-crew-skill-readiness-v1.md +
      this wave-plan entry. Pre-commit-hook simulation
      Select-String -SimpleMatch returns zero per pattern.
      Descriptor-only language used throughout the audit prose itself.

      4 review passes caught: (P1) AD-628c crew-agent registration
      assumption — initial draft proposed src/probos/agents/
      training_officer.py file; corrected to config-only YAML pattern
      after grep showed 14 existing crew profiles all use the
      crew_profiles/{role}.yaml + organization.yaml posts/assignments
      pattern with NO src/probos/agents/{role}.py file;
      (P2) AD-628d Holodeck dependency assumption — initial draft
      followed AD-628 spec verbatim with "TRAINO Holodeck Integration"
      framing; corrected to qualification-test scheduling after grep
      confirmed AD-486 + AD-539b substrate is genuinely absent at HEAD
      (zero implementation hits, only deferral-pointer comments);
      (P3) AD-628g joint-authority enforcement — initial draft
      proposed runtime role-validation; simplified to required
      keyword-only parameters at the call signature level (joint
      authority enforced at type level, not runtime; per-callsign role
      validation deferred to calling-agent layer);
      (P4) verify-first footer line-number drift — final pass
      grep-confirmed all 16 cited line numbers against HEAD c6c38ec.

      Builder execution: read prompt top-to-bottom, apply eight
      sections in order (628a -> 628b -> 628c -> 628d -> 628e ->
      628f -> 628g -> 628h) with focused per-section pytest gate
      after each section, full parallel gate before commit.
      Tracker updates land in roadmap.md (AD-628 entry flipped from
      SCOPED to complete with 8-letter sub-AD breakdown) +
      decisions-era-4-evolution.md (existing ### AD-628 SCOPED entry
      converted to COMPLETE with implementation-section table
      appended) + this wave-plan entry. gh issue close 223 with the
      canonical paragraph in Section 12 of the per-AD prompt.

  - id: "94"
    title: "AD-579 v1 Memory Architecture: Tiered Context Loading & Temporal Knowledge Validity — umbrella close — no-build tracker reconciliation flipping Memory Anchoring Build order line + AD-579 umbrella section header + AD-579 umbrella entry status field from planned/deferred to complete and inserting Issues anchor after Absorption paragraph (all three sub-ADs AD-579a/b/c shipped pre-W94)"
    kind: single
    depends_on: ["93"]
    dispatch_prompt: "prompts/WAVE-94-DISPATCH.md"
    prompts_already_drafted: true
    expected_outputs:
      - "prompts/ad-579-memory-architecture-umbrella-close.md"
    builder_required: true
    issues_to_close: [37]
    status: draft
    notes: |
      AD-579 v1 umbrella close — single AD, no-build, no new sub-AD letters,
      no new GH issues, no source/test changes. HEAD at draft 4649e61.
      Baseline pytest 12113 -> target 12113 (delta = 0).

      Verify-first against HEAD 4649e61 confirms all three sub-ADs shipped
      pre-W94: AD-579a Pinned Knowledge Buffer (PROGRESS.md:383, DECISIONS.md:1201,
      PinnedKnowledgeConfig at config.py:1372 + MemoryConfig.pinned_knowledge at
      config.py:2755, 12 focused tests in baseline); AD-579b Temporal Validity
      Windows (PROGRESS.md:385, DECISIONS.md:1208, TemporalValidityConfig at
      config.py:1381 + MemoryConfig.temporal_validity at config.py:2756,
      valid_from/valid_until comments at types.py:386 + types.py:461, 10
      focused tests in baseline); AD-579c Validity-Aware Dream Consolidation
      (PROGRESS.md:222, DECISIONS.md:1215, 8 focused tests in baseline). 30
      tests already in the 12113 baseline. Sub-AD bullets at
      roadmap.md:5996-5998 already read *(complete, OSS, ...)* — only the
      umbrella header/entry status field and the Memory Anchoring lineage
      Build order paragraph reference remain stale.

      Three SEARCH/REPLACE pairs in one MODIFY block on
      docs/development/roadmap.md: (1) :4360 Memory Anchoring Build order
      paragraph "Deferred: AD-579a/b/c (tiered memory loading + temporal
      validity, MemPalace absorption)." flips to "Complete: AD-579a/b/c
      checkmark (tiered memory loading + temporal validity, MemPalace
      absorption — Wave 94 umbrella close)." with explicit checkmark glyph;
      (2) :5984 + :5986 umbrella section header *(planned, OSS)* and
      umbrella entry status field *(planned, OSS, depends: ...)* both flip
      to *(complete, OSS, ...)* in a single SEARCH/REPLACE pair anchored on
      the header line + blank line + entry first-line bold + " — Two
      complementary enhancements" trailing-substring uniqueness; (3) :6000
      inserts a new "Issues: hash-37 (AD-579 umbrella close, Wave 94 — ...)."
      line between the Absorption italic paragraph and the existing ---
      separator, mirroring the AD-612 catalogue convention at :5969
      "Issues: hash-193 (AD-612).".

      Reframe rationale: original user-facing request to draft "AD-579 v1
      build" reframed to no-build umbrella close after verify-first. Captain
      rule "don't defer unless no choice" satisfied vacuously - nothing to
      defer, nothing to add, all three sub-ADs already shipped. Structurally
      identical to W90 (#111 AD-462 biological memory umbrella close), W76
      (#285 AD-644 SA architecture, no-build close), W71 (#415 AD-644b
      deprecation, no-build close).

      Forward-looking carve-outs in existing AD-579{a,b,c} bullet text remain
      forward-looking: AD-579a "Future ADs may wire Counselor/dream
      auto-pinning and event emission"; AD-579b "No automatic validity
      inference, notebook validity, anchor-recall filtering, retroactive
      propagation, or dream consolidation changes"; AD-579c "No LLM validity
      inference, validity UI/API, retroactive stored-episode propagation, or
      micro_dream changes". W94 mints zero new GH issues for these — same
      shape as W90's AD-462f-1/b/c/d Wave 73 carry-forward children which
      W90 explicitly chose not to convert into new GH issues.

      AD numbering: highest AD stem at HEAD remains AD-696 (Wave 72,
      verified 2026-05-07 in W92 dispatch notes block; W93 AD-628 did not
      bump highest). W94 mints zero new AD numbers.

      No commercial leak: AD-579 is purely OSS cognitive-architecture work
      (tiered context loading, pinned knowledge buffer, temporal validity
      windows, validity-aware dream consolidation). Zero tier-noun /
      pricing-token / SaaS-overlay surface in the umbrella, in either of
      the two new prompt files, or in this notes block. Banned-pattern
      audit on WAVE-94-DISPATCH.md + ad-579-memory-architecture-umbrella-close.md
      + this notes block: 11 patterns checked, 0 literal hits per pattern.
      Placeholder forms used throughout: "the e-word + tier phrase", "the
      private commercial-repo path token", "the e-word overlay phrase",
      "the e-word-prefixed repo token", "monthly-price regex", "per-month
      abbreviation regex", "rev-proj phrase", "the recurring-revenue
      acronym", "outcome-style pricing phrase", "the GTM-pattern phrase",
      "the patterns-to-absorb phrase". Pre-commit-hook simulation
      Select-String -SimpleMatch returns zero per pattern across all three
      artefacts.

      Hard-stops specific to W94: W94-1 any of the 3 SEARCH blocks fails
      to match (anchor drift since draft) -> Builder surfaces back to
      Architect, do not improvise; W94-2 pytest gate returns anything other
      than 12113 (tracker-only changes cannot move count; any delta means
      unrelated regression entered between draft and build); W94-3
      pre-commit hook flags banned literal in any of the modified surfaces;
      W94-4 Builder elects to ship a fresh AD-579{d,e,...} sub-AD letter
      "while we're here" -> out of scope; W94-5 Builder elects to mint a
      new GH issue for any of the AD-579 forward-looking carve-outs ->
      out of scope, umbrella close cites them not re-tracks them; W94-6
      Builder elects to add a ### AD-579 paragraph to
      decisions-era-4-evolution.md -> out of scope, canonical decision
      record lives at DECISIONS.md:1201-1224; W94-7 Builder elects to flip
      the AD-579{a,b,c} sub-AD bullets at roadmap.md:5996-5998 -> already
      correct, touching them is unnecessary churn.

      4 review passes recorded in this draft session: P1 (initial draft
      against HEAD 4649e61 — structure mirrored on W90 no-build umbrella
      close); P2 (verify-first sweep — confirmed all 3 stale anchors at
      the asserted line numbers, confirmed all 3 sub-AD bullets already
      read *(complete, OSS, ...)*, confirmed all 3 DECISIONS.md entries
      already read Status: Implemented, confirmed PinnedKnowledgeConfig +
      TemporalValidityConfig present in config.py at the asserted lines,
      no decisions-era-4-evolution.md AD-579 paragraph exists therefore
      none to update); P3 (reframe table — confirmed no-build is correct
      path, no sub-AD letter mintable without inventing forcing function,
      Captain rule "don't defer unless no choice" satisfied vacuously);
      P4 (banned-pattern audit on full text + per-section consistency
      check between dispatch + prompt + this entry; AD numbering verified
      696 highest no collision; SEARCH/REPLACE block uniqueness verified
      by including the "Memory Anchoring" Build order context for Pair 1,
      the " — Two complementary enhancements" substring for Pair 2, the
      AD-612-style "Issues:" placement context for Pair 3).

      Builder execution: read prompt top-to-bottom, apply 3 SEARCH/REPLACE
      pairs in 1 MODIFY block on docs/development/roadmap.md, append W94
      entry to prompts/wave-plan.yaml. Verify git diff --stat shows
      exactly 2 modified trackers (roadmap.md, wave-plan.yaml) plus the 2
      new prompt files. Pre-commit hook runs naturally on commit. Full
      pytest gate belt-and-braces (expected 12113 passed). Commit with
      "AD-579: Memory Architecture umbrella close — tracker reconciliation
      (no-build, +0 tests)". Archive both prompts. gh issue close 37 with
      the canonical paragraph in Section 4 of the per-AD prompt.
  - id: "95"
    title: "AD-652 Cognitive Code-Switching — Umbrella Close (no-build, tracker reconciliation)"
    kind: single
    depends_on: ["94"]
    dispatch_prompt: "prompts/WAVE-95-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-652-cognitive-code-switching-umbrella-close.md"
    builder_required: true
    issues_to_close: [302]
    status: pending
    notes: |
      No-build close of GH #302 (AD-652 umbrella). AD-652 is an
      architectural design principle adopted 2026-04-20 (DECISIONS.md:
      2253-2280) governing the cognitive chain pipeline — six rules:
      unified pipeline, contextual modulation (Halliday field/tenor/mode),
      structured format overlays (billet instructions), variable chain
      depth, character-driven self-monitoring, process-specific chains.
      AD-652 is a principle that governs OTHER ADs, not a discrete module.
      All six principles are fully realised at HEAD b02a9a4 across already
      shipped child ADs: AD-632 SubTaskExecutor unified-pipeline substrate
      at cognitive/sub_task.py:172; AD-649 derive_communication_context at
      cognitive_agent.py:59 with 5 channel registers (private_conversation,
      bridge_briefing, casual_social, ship_wide, department_discussion)
      setting observation _communication_context at cognitive_agent.py:
      2223 — channel/recipient → field/mode register inference; AD-639
      chain trust band at cognitive_agent.py:2055-2062 — tenor weighting
      of personality block in EVALUATE/REFLECT; AD-650 analytical_reasoning
      narrative field on ANALYZE composition brief — depth modulation;
      AD-651 StepInstructionRouter slicing composed standing orders by
      category markers — billet instructions as cognitive scaffolding;
      AD-651a [PROPOSAL] block syntax injected into compose plus Findings/
      Assessment/Recommendation duty report format — first practical
      billet application; AD-647 / AD-647c process chains framework
      (QUERY/TRANSFORM/STORE/NOTIFY step types) distinct from
      communication chain — variable depth plus process-specific
      composition; AD-653 Layer 1 speak_freely intended_action with
      trust-gated authorization (>=0.7 auto-granted, 0.4-0.7 flagged,
      <0.4 denied) and Counselor REGISTER_SHIFT_GRANTED/DENIED event
      subscription — character-driven register shifting. Downstream
      consumers AD-655 contrastive memory, AD-656 department cognitive
      profiles, AD-657 dream trace preservation, AD-658 chain harness
      metrics, AD-659 v1 chain self-optimization, AD-660 causal reasoning
      all complete and reference AD-652 modulation parameters. The AD-658
      archive prompt itself (prompts/archive/ad-658-chain-harness-metrics
      -v1.md:11-19) documents the realised-not-shipped status: AD-652
      dependency satisfied as-is, no wiring changes required. Wave 95
      reconciles four trackers: DECISIONS.md:2253-2280 (status: Design
      Principle adopted -> Realised, append Realised in subsection),
      docs/development/roadmap.md:7105 (bullet: Design Principle, OSS,
      Issue #302 -> Realised, OSS, Issue #302 with realisation list),
      PROGRESS.md:331 (DESIGN PRINCIPLE adopted -> REALISED with one-line
      realisation summary), prompts/wave-plan.yaml (this entry). Total: 4
      SEARCH/REPLACE pairs across 4 MODIFY blocks. There is no AD-652
      paragraph in decisions-era-4-evolution.md (grep returns zero matches)
      — W95 does NOT touch era-4 content; canonical decision record lives
      at DECISIONS.md:2253-2280. No code touched, no tests added, no
      pytest delta (target 12130 -> 12130). No source changes, no test
      changes, no notebook/qualification/journal changes. No new GH issues
      minted — downstream consumer ADs remain attached to the Meta-Harness
      Research Wave's own tracking; W95 cites them as realisation evidence
      not as new deferrals. No commercial leak — AD-652 is purely OSS
      cognitive-architecture work (cognitive science research foundation:
      Levelt, Halliday, Giles, Snyder, Weick/Sutcliffe; HRO research;
      cognitive chain pipeline modulation via field/tenor/mode parameters;
      structured format overlays via billet instructions; trust-gated
      register shifting). Zero tier-noun phrase / pricing-token regex /
      SaaS-overlay descriptor surface anywhere in the umbrella.
      Banned-pattern audit on WAVE-95-DISPATCH.md + ad-652-cognitive-code-
      switching-umbrella-close.md + this notes block: 11 patterns checked,
      0 literal hits per pattern. Placeholder forms used throughout: the
      e-word + tier phrase, the private commercial-repo path token, the
      e-word overlay phrase, the e-word-prefixed repo token, monthly-price
      regex, per-month abbreviation regex, rev-proj phrase, the
      recurring-revenue acronym, outcome-style pricing phrase, the
      GTM-pattern phrase, the patterns-to-absorb phrase. Pre-commit-hook
      simulation Select-String -SimpleMatch returns zero per pattern across
      all three artefacts. Reframe rationale: there is genuinely nothing
      to defer because there is nothing to add — AD-652 is a principle not
      a module, all six principles realised at HEAD. Captain rule "don't
      defer unless no choice" satisfied vacuously. Wave 95 structurally
      identical to W71 (#415), W76 (#285), W90 (#111 AD-462), and W94
      (#37 AD-579) no-build umbrella closes. 4 review passes recorded:
      P1 (initial draft against HEAD b02a9a4 — structure mirrored on
      W90/W94 patterns; Realised in mapping verified against DECISIONS.md
      principles 1-6); P2 (verify-first sweep — 8 grep-anchored claims
      confirmed: DECISIONS.md:2253-2280, roadmap.md:7105, PROGRESS.md:331,
      cognitive_agent.py:59 + 2223 + 2055-2062, decisions-era-4-evolution
      .md zero AD-652 matches, AD-658 archive prompt :11-19, AD-651/651a
      /653/647/647c/650 all Complete, AD-655/656/657/658/659/660 all
      Complete or v1 complete); P3 (reframe table — no reframe needed,
      principle is fully realised, only trackers lag); P4 (banned-pattern
      audit + SEARCH/REPLACE block uniqueness verified across all 4
      MODIFY blocks). Builder execution: read prompt top-to-bottom, apply
      4 SEARCH/REPLACE pairs across 4 MODIFY blocks. Verify git diff
      --stat shows exactly 4 modified trackers (DECISIONS.md, roadmap.md,
      PROGRESS.md, wave-plan.yaml) plus the 2 new prompt files. Verify
      git diff decisions-era-4-evolution.md shows zero changes.
      Pre-commit hook runs naturally on commit. Full pytest gate
      belt-and-braces (expected 12130 passed). Commit with "AD-652:
      Cognitive Code-Switching umbrella close — tracker reconciliation
      (no-build, +0 tests)". Archive both prompts. gh issue close 302
      with the canonical paragraph in Section 5 of the per-AD prompt.

  - id: "96"
    title: "AD-521 v1 SWE/Build Pipeline Separation — Model A"
    kind: single
    depends_on: ["95"]
    dispatch_prompt: "prompts/WAVE-96-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-521-swe-build-pipeline-separation-v1.md"
    builder_required: true
    issues_to_close: [96]
    status: pending
    notes: |
      Source-modifying v1 of AD-521 (DECIDED 2026-03-29 at
      decisions-era-4-evolution.md:1427-1467). Five sections applied
      top-to-bottom: (1) new module src/probos/build_pipeline.py
      (~180 lines, BuildPipeline Ship's Computer service with
      execute_approved_build + parse_file_blocks instance methods,
      constructor injection of runtime, defensive getattr lookups for
      pre_flight_runner / emit_event / llm_client at invocation time);
      (2) cognitive/builder.py rename — class BuilderAgent renamed to
      SoftwareEngineerAgent, module-level BuilderAgent =
      SoftwareEngineerAgent alias preserves the 16 test files importing
      the old name, class docstring updated to reflect SWE crew role;
      (3) runtime.py wiring — self.build_pipeline declared in __init__
      alongside warm_boot / dream_adapter (runtime.py:567-569), the
      BuildPipeline import added alongside other Ship's Computer service
      imports, instantiation in start() with no ordering constraint vs
      pre_flight_runner because BuildPipeline reads the runtime
      attribute via getattr at invocation time (same pattern as the
      current cognitive/builder.py:2548 lookup); (4) new test file
      tests/test_ad521_swe_pipeline_separation.py with 12 boundary
      tests across BuildPipeline service shape (4), SoftwareEngineerAgent
      class identity (3), BuilderAgent alias (1), module-level shim (1),
      runtime wiring (2), crew identity continuity (1); (5) tracker
      reconciliation — decisions-era-4-evolution.md:1467 status flip
      from DECIDED to v1 COMPLETE, docs/development/roadmap.md:6628
      bullet flip from decided to v1 complete with realisation summary,
      PROGRESS.md INSERT new status note after AD-652 line at :331
      (zero existing AD-521 matches at HEAD), wave-plan.yaml W96 entry
      appended.

      Crew identity continuity preserved: agent_type "builder" unchanged
      (16+ allow-list / spawner / standing-orders / skill-framework
      sites untouched), callsign Scotty unchanged, pool name "builder"
      unchanged at runtime.py:697, standing-orders mapping
      builder→engineering unchanged at cognitive/standing_orders.py:42,
      _WARD_ROOM_CREW "builder" entry unchanged at crew_utils.py:15.

      The agentic-loop tooling (AD-543 ToolCall protocol + ToolExecutor,
      AD-544 native tool suite, AD-545 agentic loop, AD-546
      BuildPipeline integration, AD-547 session compaction, AD-548
      trust-tier permissions, AD-549 visiting-builder migration) is the
      next wave (GH #13). AD-521 v1 unblocks AD-546 which depends on
      it explicitly per docs/development/roadmap.md:6768.

      The closed-source SWE-tier overlay (AD-452 class-extension
      plug-in point) is out-of-repo. v1 ships zero closed-source
      content — the OSS class structure is the architectural surface
      and the overlay slots in via the placeholder-tier licensing AD
      that lives in the private commercial-repo path token surface.
      Banned-pattern audit on this dispatch + per-AD prompt + this
      notes block: zero hits across all 11 patterns (the e-word + tier
      phrase, the private commercial-repo path token, the e-word
      overlay phrase, the e-word-prefixed repo token, monthly-price
      regex, per-month abbreviation regex, rev-proj phrase, the
      recurring-revenue acronym, outcome-style pricing phrase, the
      GTM-pattern phrase, the patterns-to-absorb phrase). Pre-commit
      hook simulation Select-String -SimpleMatch returns zero per
      pattern across all artefacts.

      4 review passes recorded: P1 (initial draft against HEAD 08bfc7f
      — five-section structure, 12-test plan, AD-521 spec mapping
      verified against decisions-era-4-evolution.md:1427-1467); P2
      (verify-first sweep — caught two phantom claims: PROGRESS.md
      had zero AD-521 matches so Section 5c is INSERT not flip,
      pre_flight_runner is set in startup/finalize.py:1644 not
      runtime.__init__ so getattr defensive pattern is canonical and
      no ordering constraint matters; both fixed in dispatch); P3
      (reframe table — build v1 not defer, AD-543-549 deps tracked
      under GH #13, Captain rule satisfied non-vacuously by AD-546
      forcing function); P4 (banned-pattern audit + SEARCH/REPLACE
      block uniqueness check across 6 MODIFY blocks; AD numbering
      verified AD-696 highest no collision; the 16-test-file import
      path coverage cross-checked).

      Builder execution: read prompt top-to-bottom, apply 6
      SEARCH/REPLACE pairs across 6 MODIFY blocks plus the 2 new file
      creates (build_pipeline.py + test_ad521_swe_pipeline_separation
      .py). Verify git diff --stat shows 4 modified trackers (DECISIONS
      era-4, roadmap.md, PROGRESS.md, wave-plan.yaml) plus 1 modified
      runtime.py plus 1 modified cognitive/builder.py plus 2 new
      files plus this prompt + dispatch (which Builder will archive
      after commit). Pre-commit hook runs naturally on commit. Full
      pytest gate (expected ≥12142 passed; minimum gate is +12).
      Commit with "AD-521 v1: SWE/Build Pipeline Separation — Model A
      (extract BuildPipeline service, rename BuilderAgent →
      SoftwareEngineerAgent + alias, runtime wiring, +12 tests)".
      Archive both prompts. gh issue close 96 with the canonical
      paragraph in Section 6 of the per-AD prompt.

  - id: "97"
    title: "AD-476 v1 Specialized Builders ΓÇö Cognitive Division of Labor for SWE"
    kind: single
    depends_on: ["96"]
    dispatch_prompt: "prompts/WAVE-97-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-476-specialized-builders-v1.md"
    builder_required: true
    issues_to_close: [70]

  - id: "98"
    title: "AD-543/544/545/546/547/548/549 v1 Native SWE Harness ΓÇö Agentic Tool Loop (combined; closes #13)"
    kind: combo
    depends_on: ["97"]
    dispatch_prompt: "prompts/WAVE-98-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-543-549-native-swe-harness-v1.md"
    builder_required: true
    issues_to_close: [13]
    status: pending

  - id: "99"
    title: "AD-486 v1 Holodeck Birth Chamber — Graduated Cognitive Onboarding (closes #24)"
    kind: single
    depends_on: ["98"]
    dispatch_prompt: "prompts/WAVE-99-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-486-holodeck-birth-chamber-v1.md"
    builder_required: true
    issues_to_close: [24]
    status: pending

  - id: "100"
    title: "AD-539b v1 Holodeck Scenario Generation from Skill Gaps (closes #12)"
    kind: single
    depends_on: ["99"]
    dispatch_prompt: "prompts/WAVE-100-DISPATCH.md"
    prompts_already_drafted: true
    prompt_paths:
      - "prompts/ad-539b-holodeck-scenario-generation-v1.md"
    builder_required: true
    issues_to_close: [12]
    status: pending
