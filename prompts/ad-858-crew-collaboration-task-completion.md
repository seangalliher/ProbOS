# AD-858..862 — Crew Collaboration: coordinated multi-agent task completion (decompose → fan-out → adversarial-verify → converge → synthesize)

**Status:** Draft for review (Architect-authored, verify-first against HEAD)
**Mode:** Architect spec. Builder executes ONE AD = ONE commit with a gate between each. Do NOT build the whole epic in one pass.
**Northstar:** Crew agents complete *hard* work the way a ship's crew does — a single goal is decomposed into a sub-task DAG, fanned out across **persistent, trusted, memory-bearing** crew agents, each result is **independently/adversarially verified** before it folds in, the crew **iterates until results converge**, and the verified parts are **synthesized into one completion** with per-agent attribution recorded against the durable trust ledger.
**Numbering:** Highest reserved AD = **AD-857** (Crew Self-Unblock epic). This epic = **AD-858–862**. Reconciled in `docs/development/roadmap.md` (Crew Autonomy table).
**Depends on:** **AD-856** (AgenticLoop as the dispatchable-work-item executor) — each subtask in this epic runs through that executor. Build 856 first.

---

## Why this epic (the gap)

ProbOS already owns the **substrate** for coordinated multi-agent work, but it has never been wired into a single "complete this hard task by collaboration" loop. The connective tissue and two genuinely-missing pieces (a semantic LLM decomposer and an adversarial **convergence gate**) are this epic.

| Existing primitive | Location (verified HEAD) | What it already does | What it does NOT do |
|---|---|---|---|
| `ParallelDispatcher` (AD-594c) | `consultation/dispatch.py:283`, `dispatch()` at `:405` | Decomposes a plan → `WorkItemSpec` DAG, detects resource conflicts, injects serialization edges, creates `WorkItem`s with `depends_on` + `assigned_to`, tracks progress/blockers | Decomposer is **markdown-only** (`MarkdownPlanDecomposer:189`); no LLM/semantic decomposition of a single goal |
| `PlanDecomposer` Protocol | `consultation/dispatch.py:162` | Pluggable decomposer seam — docstring says *"LLM-driven semantic decomposers plug in here under a separate AD"* | The LLM decomposer itself does not exist yet — **this is the hook for AD-858** |
| `WorkItem` WBS containment | `workforce.py:571` (`parent_id`), `depends_on` | Parent/child + dependency DAG already modeled | No driver that runs a parent as "collect verified children → synthesize" |
| `RedTeamAgent` | `agents/red_team.py:25`, `verify()` at `:66` | **Deterministic** re-execution + compare for specific tool intents (`read_file`/`stat_file`/`run_command`/`http_fetch`/`write_file`); reports discrepancies to trust + consensus | Does NOT do **semantic** task-result verification (judging whether a synthesized answer/work product is correct) |
| `compute_shapley_values` | `consensus/shapley.py:37` | Per-agent Shapley attribution over `list[Vote]` for a consensus outcome | Not yet applied to multi-agent *task* contribution |
| `DAGExecutor` | `cognitive/decomposer.py:668` | Parallel intent execution respecting deps | Operates on a single-request `TaskDAG`, not a crew of dispatched work items |
| `AgenticLoop` (AD-545) | `cognitive/swe_harness/agentic_loop.py:47` | Multi-turn LLM↔tool executor (the AD-856 per-subtask executor) | — (reused as the per-subtask engine) |

**Net:** an agent today can complete *one* dispatched work item (AD-856), and a markdown plan can be fanned out (`ParallelDispatcher`), but there is no path that takes **one hard goal → crew fan-out → adversarial verify → converge → one synthesized completion** with attribution. That is the differentiator the market leaders are converging on (parallel subagents + adversarial verification), and ProbOS's wedge is doing it with **persistent, trusted, memory-bearing** crew instead of ephemeral subagents. This epic builds that loop **on top of** the existing dispatcher, not beside it (DRY — do NOT author a second dispatcher).

---

## AD-858 — LLM-driven semantic plan decomposer (single goal → sub-task DAG)

**Build.** A new `class LLMPlanDecomposer` implementing the existing `PlanDecomposer` Protocol (`consultation/dispatch.py:162`), in `src/probos/consultation/llm_decomposer.py`:
- Takes a single natural-language goal (the parent work item's title+description) and emits a `list[WorkItemSpec]` — sub-tasks with `depends_on` edges, suggested `agent`/`work_type`, and `resources` — using the tiered LLM client (`cognitive/llm_client.py`, **standard** tier; deep only if a config flag opts in).
- **Each emitted sub-task carries a declared `expected_output` contract** — a short natural-language statement of what a *correct, complete* result for that sub-task must contain (the acceptance criterion), generated by the decomposer alongside the task description. This is the connective tissue for AD-860: the verifier judges a result **against its declared contract**, not against a vague "find a flaw" prompt — a declared acceptance criterion is the difference between a convergence gate that converges and one that wanders. Add `expected_output: str | None` to `WorkItemSpec` (VERIFY the dataclass at `consultation/dispatch.py` / `workforce.py` first; add as an **optional** field defaulting `None` so `MarkdownPlanDecomposer` and all existing call sites are unaffected — backward-compatible, Open/Closed). If the LLM omits it, honest-degrade to `None` (verifier falls back to free-text critique), do NOT fail decomposition.
- Output must be **schema-validated** before return: every `depends_on` references an emitted `spec_id` (no dangling edges), the graph is **acyclic** (reject + honest-degrade to a single-spec passthrough if the LLM emits a cycle), and spec count is bounded by a config cap (default 12 — Safety Budget; do not let the LLM fan out unbounded).
- It is a **drop-in** for `MarkdownPlanDecomposer`: `ParallelDispatcher(decomposer=LLMPlanDecomposer(...))`. Do NOT modify `ParallelDispatcher`'s dispatch logic — only provide a new decomposer. Conflict detection + serialization edges already run downstream and are reused unchanged.
- **SYNC→ASYNC BRIDGE (blocking — verified by Architect review).** The `PlanDecomposer` Protocol method is **synchronous** — `def decompose(self, markdown_text: str) -> list[WorkItemSpec]` — and `ParallelDispatcher.dispatch()` (async) calls it **synchronously** at `dispatch.py:423` (no `await`). The LLM client's `complete()` is **async**. Therefore `LLMPlanDecomposer.decompose` MUST stay synchronous (to satisfy the Protocol AND the existing sync call site) and MUST run the async LLM call on a **separate thread with its own loop** — NEVER `asyncio.run()` / `loop.run_until_complete()` on the calling loop (that raises `RuntimeError: asyncio.run() cannot be called from a running event loop`). Do NOT make the Protocol async and do NOT touch `dispatch.py:423`. Bridge shape:
  ```python
  import asyncio, concurrent.futures
  from probos.types import LLMRequest
  def decompose(self, markdown_text: str) -> list[WorkItemSpec]:
      req = LLMRequest(prompt=..., system_prompt=..., tier=self._tier)  # tier defaults "standard"
      with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
          resp = ex.submit(lambda: asyncio.run(self._client.complete(req))).result()
      # parse resp.content (JSON) → validate (no dangling edges, acyclic, ≤cap) → list[WorkItemSpec]
  ```
  Real signatures to pin: `async def complete(self, request: LLMRequest, *, priority: Priority = Priority.NORMAL) -> LLMResponse`; tier selected via `LLMRequest(tier="standard")`; result text on `LLMResponse.content`.
- Config-gated: extend the existing `ConsultationDispatchConfig` Pydantic model (`config.py:4446`) with `decomposer: Literal["markdown","llm"] = "markdown"`, `max_subtasks: int = 12`, `decomposer_tier: str = "standard"`. Defaults keep existing behavior unchanged until opted in (default `markdown`).

**Acceptance.** `tests/test_ad858_llm_decomposer.py` (≥8, fake LLM client returning canned JSON — NO real network): goal→multi-spec DAG with valid deps; each spec carries an `expected_output` contract when the LLM supplies one; LLM omits `expected_output`→degrades to `None`, not a crash; dangling-dep edge rejected/repaired; cycle→honest-degrade to single passthrough spec; spec cap enforced; conforms to `PlanDecomposer` Protocol (structural); empty/garbage LLM output→single passthrough, not a crash; `MarkdownPlanDecomposer` output still valid with `expected_output=None` (backward-compat); **`decompose()` invoked from INSIDE a running event loop returns specs without raising `RuntimeError` (the sync→async bridge test — this is the one that catches the blocking defect; without it the bridge ships green)**. Verify Engineering-Principles compliance.

**Do NOT build:** the crew executor wiring (859), the verify gate (860), synthesis (861), UI. Do NOT modify `ParallelDispatcher` or `MarkdownPlanDecomposer`.

---

## AD-859 — Crew fan-out executor (AD-859a reusable executor + AD-859 dep-gated crew driver)

> **Architect verify-first correction (NO-GO on the original section — re-scoped).** The original section assumed a *dispatch-and-collect* seam on `WorkItemRouter` (`:104`) that runs the AD-856 loop and returns a result. **That seam does not exist.** Verified at HEAD: `WorkItemRouter.on_work_item_created(event) -> None` is a fire-and-forget event listener (`work_item_router.py`) — it broadcasts a `task_event` and returns nothing. The AD-856 path (`cognitive_agent._run_agentic_dispatch`, `cognitive_agent.py:1199`) is reachable only event-driven, **returns `str | None` (only `agentic_result.final_text`)**, discards the `AgenticResult` (and thus `tool_calls` / `stopped_reason`), persists **no** tool trace, and only ever transitions the work item to **`in_progress`** — never `done`. There is also **no** readiness/ready-children helper to reuse (`ParallelDispatcher.dispatch()` creates ALL children at once and emits `WORK_ITEM_CREATED` per child; nothing gates on deps). So the original "reuse the router + reuse readiness semantics" plan is architecturally impossible and a fake-router test would ship green-but-wrong. **Fix: split into a prerequisite refactor (AD-859a) that turns the AD-856 inline loop into a reusable, result-returning, trace-persisting executor, then build `CrewTaskExecutor` (AD-859) on top of THAT — awaiting it directly with its own `depends_on`-gated scheduling.** Two commits, gate between.

### AD-859a — Extract the AD-856 loop into a reusable result-returning executor (prerequisite refactor)

**Build.** Refactor the AD-856 dispatch loop out of `CognitiveAgent` so it can be called by BOTH the existing AD-839 handler AND the crew executor, returning a structured result instead of a bare string:
- New `@dataclass WorkItemAgenticOutcome` (in `src/probos/cognitive/agentic_dispatch.py`, beside `DispatchToolExecutor`): `final_text: str`, `stopped_reason: str`, `denied_tools: list[str]`, `tool_trace_ref: str | None` (content-addressable SHA ref to the serialized `AgenticResult.tool_calls` in `AttachmentStore`, NOT inline — AD-731 rule).
- New `class WorkItemAgenticExecutor` (same module): holds the wiring currently built inline in `_run_agentic_dispatch` (build `DispatchToolExecutor`, register mesh-intent tools, gather grants→tool defs, construct `AgenticLoop`, `await loop.run(...)`), then **persists the trace** (`runtime.attachment_store.write(content_hash=<sha256 of the JSON-serialized tool_calls>, blob=..., mime="application/json", origin="crew_trace")` — VERIFY `AttachmentStore.write` signature at `attachments/store.py` first; honest-degrade `tool_trace_ref=None` if `attachment_store` is unwired), and returns a `WorkItemAgenticOutcome`. Capability-gap surfacing for `denied_tools` stays the **caller's** responsibility (so the AD-839 handler keeps its existing gap-driver call; the crew executor does its own).
- Refactor `CognitiveAgent._run_agentic_dispatch` to call the new executor and preserve its **existing `str | None` contract** (return `outcome.final_text or ""`) — pure extraction, zero behavior change on the AD-839 path. Do NOT change `_handle_work_item_dispatch`'s call site, the feature-gate, or the fallback semantics.

**Acceptance.** `tests/test_ad859a_agentic_executor.py` (≥6, fake LLM + fake registry + real-or-fake AttachmentStore): executor returns `final_text` + `stopped_reason` mapped from `AgenticResult`; `denied_tools` captured from `DispatchToolExecutor`; `tool_trace_ref` is a ref (not inline bytes) when a store is wired; `tool_trace_ref=None` honest-degrade when store unwired; `_run_agentic_dispatch` still returns `str | None` and the AD-839 path is unchanged (regression); gap surfacing unchanged for denied tools. Verify Engineering-Principles compliance.

**Do NOT build:** the crew executor (859), verify gate (860). Do NOT author a second AgenticLoop — this is an EXTRACTION of the existing one.

### AD-859 — Crew fan-out executor (each subtask runs the AD-859a executor, results collected with provenance)

**Build.** A `class CrewTaskExecutor` in `src/probos/cognitive/crew_executor.py` that, given a parent work item already dispatched into child specs by `ParallelDispatcher`, drives the children directly:
- Read children via `WorkItemStore.list_work_items(parent_id=<parent_id>)` (VERIFY signature at `workforce.py` first). Do its **own** `depends_on`-gated scheduling (there is no readiness helper to reuse — confirmed): a child becomes runnable only when every id in its `depends_on` has reached `done` in the store. This IS the topological scheduling the executor owns; keep it small and explicit.
- For each runnable child, resolve its `assigned_to` agent and run it through the **AD-859a `WorkItemAgenticExecutor`** (awaited directly — NOT via the fire-and-forget router), then on success `transition_work_item(child_id, "done", source=...)` so dependents unblock.
- Collect each child's result into a `@dataclass SubtaskResult`: `work_item_id`, `spec_id`, `agent_id` (the **persistent** agent identity — durable provenance ephemeral subagents cannot carry), `output` (from `outcome.final_text`), `status` (derived from `outcome.stopped_reason` / failure), `tool_trace_ref` (from `outcome.tool_trace_ref` — already a ref), `started_at`/`finished_at`.
- Bounded concurrency (config `max_parallel_subtasks`, default conservative — extend the existing `AgenticDispatchConfig` or a new `CrewConfig`; default keeps fan-out small per Safety Budget) so a wide fan-out doesn't exhaust the LLM tier. Hold all task references (no fire-and-forget; `asyncio.create_task` refs retained per async-hygiene rule).
- A failed child surfaces its status in its `SubtaskResult` without aborting siblings (it just doesn't unblock its dependents). Do NOT silently mark a failed child `done`.
- New events: `CREW_TASK_STARTED`, `SUBTASK_COMPLETED` (append in `events.py` after the verified existing `WORK_ITEM_*` block at `events.py:98-102`; confirm neither exists — they don't).

**Acceptance.** `tests/test_ad859_crew_executor.py` (≥6, **fake `WorkItemAgenticExecutor` + real `WorkItemStore`** — NOT a fake router): independent children run in parallel up to the cap; a child waits until its `depends_on` children are `done` before starting; each `SubtaskResult` carries the persistent `agent_id` + a trace **ref** (not inline bytes); a failed child surfaces status without aborting siblings and does NOT unblock its dependents; concurrency cap respected; `CREW_TASK_STARTED`/`SUBTASK_COMPLETED` emitted. Verify Engineering-Principles compliance.

**Do NOT build:** the verify gate (860), synthesis (861), UI. Do NOT author a second dispatcher or a second AgenticLoop (reuse the AD-859a executor; reuse `ParallelDispatcher` for fan-out).

---

## AD-860 — Adversarial verification + convergence gate (the differentiator)

> **Architect verify-first corrections (NO-GO on the original section — three blockers fixed).** (A) **The `expected_output` anchor was never persisted.** AD-858 added `expected_output` to `WorkItemSpec` (`consultation/dispatch.py:64`), but `ParallelDispatcher.dispatch()` does NOT copy it into the persisted `WorkItem` metadata (`dispatch.py:451-457` builds metadata with `workspace_id`/`spec_id`/`resources`/`plan_version` only — `expected_output` is dropped). `SubtaskResult` carries `spec_id`/`work_item_id` but no contract, and there is no spec store to look it up by `spec_id`. So the "anchored to the declared contract" path — the whole point of the gate — is dead code and EVERY subtask would hit the free-text fallback. **Fix (in AD-860 scope, additive/backward-compatible):** add one key at `dispatch.py:~452` — `metadata["expected_output"] = spec.expected_output` (persistence, not dispatch *logic* — respects the "don't fork the dispatcher" boundary); the verifier resolves the contract via `wi = await store.get_work_item(result.work_item_id); expected = (wi.metadata or {}).get("expected_output")`. (B) **The convergence loop cannot go "back through AD-859."** `CrewTaskExecutor` exposes no single-child-with-critique seam (`run(parent_id)` re-runs the whole DAG; `_run_child(child)` is private and takes only a `WorkItem`), and the `duty` state machine has NO `done→in_progress` transition (`done` is terminal, `workforce.py:86`), so a completed child can't be re-opened. **Fix:** point the convergence re-dispatch at the PUBLIC AD-859a engine — `await WorkItemAgenticExecutor.run(agent_id=<producer>, instructions=<orig>, task_text=<orig_task + "\n\nCRITIQUE:\n" + verdict.critique>, runtime=runtime, ...)` (async, public, state-machine-independent). Declare the executor (or its `llm_client`) as an AD-860 constructor dependency. (C) **`record_outcome` real signature + sync.** Verified verbatim: `def record_outcome(self, agent_id, success, weight=1.0, intent_type="", episode_id="", verifier_id="", source="verification") -> float` (`consensus/trust.py:217`) — it is **SYNCHRONOUS (do NOT `await`)** and inserts `episode_id`/`verifier_id` between `intent_type` and `source`, so a positional 5th arg lands in `episode_id`. Call it with KEYWORDS only. Verified-clean (do NOT re-flag): `Vote{agent_id, approved, confidence=0.0, reason="", timestamp}` (`types.py:160`) maps cleanly `accepted→approved`/`confidence→confidence`/`critique→reason`/`verifier_agent_id→agent_id`; `LLMClient.complete` is async so the judge `await`s it directly (NO AD-858 ThreadPool bridge); `registry.all()` minus the producer id gives the independent verifier.

**Build.** The gate that makes results *trustworthy*, modeled on the `RedTeamAgent` **pattern** but for **semantic** task results (the existing `RedTeamAgent` only does deterministic tool re-execution — do NOT claim it covers this; this is new):
- **First, the one-line persistence fix (Defect A):** at `consultation/dispatch.py:~452`, add `metadata["expected_output"] = spec.expected_output` to the metadata dict the dispatcher already builds. Additive only; `MarkdownPlanDecomposer` emits `expected_output=None` so existing behavior is unchanged. Do NOT touch any other dispatch logic.
- A `class SubtaskVerifier` in `src/probos/cognitive/crew_verifier.py` that, for each `SubtaskResult`, runs an **independent** agent (a *different* crew member than the producer — independence is the point) to **refute** the result: an LLM-judge prompt (`await llm_client.complete(LLMRequest(prompt=..., system_prompt=..., tier="standard"))`) that tries to find a flaw, missing requirement, or unsupported claim. Resolve the declared contract via `wi = await store.get_work_item(result.work_item_id); expected = (wi.metadata or {}).get("expected_output")`. When `expected` is non-None, anchor the judge prompt to it — "does this result satisfy *this declared acceptance criterion*?" — a concrete, checkable target; when `None`, fall back to the free-text critique prompt (honest-degrade). Output: `@dataclass VerificationVerdict{ accepted: bool, confidence: float, critique: str, verifier_agent_id: str }`.
- **Convergence loop:** if a verdict is `accepted=False`, re-run the subtask through the **public AD-859a `WorkItemAgenticExecutor.run(...)`** with the critique appended to `task_text` (NOT "back through AD-859" — that seam does not exist), up to a config `max_convergence_rounds` (default 2 — Safety Budget; do not loop unbounded). Converged = accepted, or max rounds reached (then escalate the subtask / mark it unverified, do NOT silently accept a refuted result). Take the executor (or `llm_client`) as a constructor dependency.
- **Reuse the consensus path for attribution, not a parallel one:** each verifier's verdict is recorded against the **trust ledger** via the SYNCHRONOUS `TrustNetwork.record_outcome` — call it with KEYWORDS: `trust_network.record_outcome(verifier_agent_id, success=verdict.accepted, intent_type="crew_verification", verifier_id=<producer_id>, source="crew_verification")` (do NOT `await`; do NOT pass a positional 5th arg) — so good verifiers and good producers both earn trust over runs (the persistent-crew wedge). Build `Vote(agent_id=verifier_agent_id, approved=verdict.accepted, confidence=verdict.confidence, reason=verdict.critique)` so AD-861 can call `compute_shapley_values` — do NOT invent a new attribution scheme and do NOT call `compute_shapley_values` here (that is AD-861).
- Independence rule enforced: the verifier agent_id MUST differ from the producer agent_id (pick from `registry.all()` excluding the producer id); if no independent agent is available, honest-degrade to "unverified" status with a logged reason (do NOT let an agent verify itself).

**Acceptance.** `tests/test_ad860_crew_verifier.py` (≥8, fake LLM judge + real-or-fake WorkItemStore + real TrustNetwork): accepted verdict folds through; refuted verdict triggers re-run via the AD-859a executor with the critique in `task_text`; verdict anchored to `expected_output` when the WorkItem metadata carries it; `expected_output` absent→free-text-critique fallback path (a GENUINE branch now that persistence is fixed); convergence stops at `max_convergence_rounds` then escalates/unverified (not silent-accept); verifier ≠ producer enforced; no-independent-agent→unverified degrade; verdicts recorded against the trust ledger via the sync keyword call; verdicts map to the real `Vote` shape. Verify Engineering-Principles compliance.

**Do NOT build:** synthesis (861), UI. Do NOT modify `RedTeamAgent` (it stays the deterministic tool verifier; this is the semantic sibling). Do NOT call `compute_shapley_values` (AD-861). Do NOT add a `done→in_progress` transition to the duty state machine (re-run via the AD-859a executor instead).

---

## AD-861 — Result synthesis + Shapley attribution → parent completion

> **Architect verify-first corrections (NO-GO on the original section — two blockers fixed; all APIs exist).** (1) **Verifier Votes cannot be built from bare `SubtaskResult`s.** The original input contract was "parent + accepted `SubtaskResult`s," but `SubtaskResult` (`crew_executor.py`) carries only `work_item_id/spec_id/agent_id/output/status/tool_trace_ref/started_at/finished_at` — **no verifier id, no confidence**. Verifier identity/confidence/accepted live in `VerificationVerdict` inside the AD-860 `ConvergenceOutcome` wrapper (`crew_verifier.py:74` — `result: SubtaskResult` + `verdict: VerificationVerdict`, both confirmed at HEAD). **Fix:** AD-861's input contract is a `list[ConvergenceOutcome]` from AD-860, NOT bare `SubtaskResult`s. Producer Vote ← `oc.result.agent_id`; verifier Vote ← reuse the existing `SubtaskVerifier.verdict_to_vote(oc.verdict)` static helper (`crew_verifier.py:242`) which already maps `verifier_agent_id/accepted/confidence/critique` → `Vote`. Skip outcomes with empty `verdict.verifier_agent_id` (the honest-degrade "unverified" case — no verifier Vote, producer Vote only). (2) **`update_work_item(status="done")` bypasses the AD-498 state machine.** `update_work_item` (`workforce.py:1108`) is a raw SQL UPDATE with NO `validate_transition`; the sibling AD-859 path uses the validated `transition_work_item(child.id, "in_progress", source="crew_executor")` (`crew_executor.py:195`). Forcing `status="done"` via `update_work_item` is a governance-gate bypass. **Fix:** use `transition_work_item(parent_id, "done", source="crew_synth")` for the status change (Builder confirms the parent work_type declares a `→done` transition — children already transition so a state machine exists; treat a `None` return as honest-degrade, not silent success), then `update_work_item(parent_id, metadata={...})` for provenance ONLY. Verified-clean (use as-is): `compute_shapley_values(votes: list[Vote], approval_threshold: float, use_confidence_weights=True) -> dict[str,float]` (`shapley.py:37`, keys by `agent_id`); `Vote{agent_id, approved, confidence=0.0, reason="", timestamp}` (`types.py:159`); `Episode` carries `shapley_values`/`trust_deltas`/`agent_ids`/`outcomes` (`types.py:469`); episodic API is the ASYNC **`runtime.episodic_memory.store(episode)`** (`episodic.py:1186`) — NOT `store_episode` — and `runtime.episodic_memory` is `None` when disabled (`runtime.py:682`) so GUARD before calling; `record_outcome` is sync+keyword (same as AD-860); `AttachmentStore.write(content_hash, blob, mime, *, origin=...)` async (AD-731). `CREW_TASK_COMPLETED` does not exist yet (`events.py:103-104` has only the AD-859 pair) — append `CREW_TASK_COMPLETED = "crew_task_completed"  # AD-861` after `SUBTASK_COMPLETED`.

**Build.** Fold the verified subtask results into one parent completion:
- A `class CrewSynthesizer` in `src/probos/cognitive/crew_synth.py` with **constructor injection mirroring `SubtaskVerifier`** (`__init__(*, llm_client, work_item_store, trust_network, episodic_memory, attachment_store, runtime, ...)` — no global lookups): given the parent work item + its `list[ConvergenceOutcome]` (from AD-860), synthesize the parent's final output (LLM, standard tier: `await llm_client.complete(LLMRequest(prompt=..., system_prompt=..., tier="standard"))`) from the **accepted** outcomes (`oc.verdict.accepted`), then transition the parent to done via `transition_work_item(parent_id, "done", source="crew_synth")` (NOT `update_work_item(status=...)` — that bypasses the state machine), and write provenance (which subtasks + which verifiers contributed) via `update_work_item(parent_id, metadata={...})` after storing the full provenance blob as a content-addressable ref (`attachment_store.write(content_hash=<sha256>, blob=..., mime="application/json", origin="crew_synth_provenance")`, AD-731 — NOT inline). Honest-degrade if `transition_work_item` returns `None`.
- **Attribution:** build `Vote`s — one producer Vote per accepted outcome (`Vote(agent_id=oc.result.agent_id, approved=oc.verdict.accepted, confidence=oc.verdict.confidence, reason=...)`) plus one verifier Vote per outcome with a non-empty `verdict.verifier_agent_id` (via `SubtaskVerifier.verdict_to_vote(oc.verdict)`) — and call `compute_shapley_values(votes, approval_threshold, use_confidence_weights=True)` (`consensus/shapley.py:37`) to compute each crew member's marginal contribution. Pin `approval_threshold` from the runtime quorum policy (`QuorumPolicy().approval_threshold`, ~0.6) — do NOT hardcode a bare literal. Note Shapley keys by `agent_id`: if the same agent appears as both producer and verifier in the vote set, dedupe/merge intentionally (do not let one Vote silently overwrite the other). Record each agent's result against the trust ledger via the SYNC keyword `record_outcome(...)` (no await), and store an **episode** for the whole collaboration via the async `episodic_memory.store(Episode(...))` (guard `episodic_memory is not None`), populating `shapley_values`/`agent_ids`/`outcomes` so the Hebbian "which collaborators work well together" payoff lands (learning-loop rule).
- New event `CREW_TASK_COMPLETED = "crew_task_completed"  # AD-861` (append in `events.py` after `SUBTASK_COMPLETED`), emitted with the parent id + attribution summary.

**Acceptance.** `tests/test_ad861_crew_synth.py` (≥6, real WorkItemStore + real TrustNetwork + fake LLM): accepted outcomes synthesize into parent `done` **via `transition_work_item`** (assert the validated path, not a raw update); Shapley attribution computed across producers+verifiers (sums/keys correct, same-agent dedupe honored); episode stored for the collaboration (guarded when `episodic_memory` is None — no crash); synthesis provenance is a **ref** not inline bytes; partial (some outcomes refuted/unverified) synthesizes from accepted-only with a recorded caveat; `CREW_TASK_COMPLETED` emitted. Verify Engineering-Principles compliance.

**Do NOT build:** UI (862). Do NOT author a new attribution algorithm (reuse `compute_shapley_values`). Do NOT use `update_work_item` for the status change (use `transition_work_item`). Do NOT call the episodic store without a None-guard.

---

## AD-862 — HXI crew-collaboration surface (forward marker, dual-surface)

**Build (UI + thin API).** Per HXI Principles #4 (motion encodes state) and #6 (the canvas IS the information), surface the live collaboration so the Captain can watch fan-out → verify → converge without a command:
- `GET /api/crew-tasks/{parent_id}` returning the live tree (parent + subtasks + each subtask's status/verifier verdict/convergence round) — thin wrapper over the AD-859/860/861 state.
- HXI: a crew-collaboration view showing the parent and its fanned-out children, each child pulsing while running, flashing on verify, settling on converge (motion = state). Vitest component test required (UI-test rule — HXI has broken from untested UI changes before).

**Acceptance.** ≥3 API tests (happy/parent-not-found/in-progress) + ≥1 vitest component test. Verify Engineering-Principles compliance.

**Do NOT build:** changes to the decompose/execute/verify/synthesize logic.

---

## Build order & gates

`AD-858 → 859 → 860 → 861 → 862`, each: focused tests green → full gate → commit → **stop, review**. AD-859 (touches dispatch/runtime wiring) and AD-861 (touches the parent-completion path) get a corruption pre-check (`git diff --numstat`, PowerShell sort) before commit per the working-tree-integrity memory.

**Test invocation (CWD hazard):** `Set-Location -LiteralPath d:\ProbOS` then
`d:/ProbOS/.venv/Scripts/pytest.exe d:/ProbOS/tests/test_ad858_llm_decomposer.py --rootdir d:/ProbOS -q -n 0 -p no:cacheprovider`.

## Relationship to adjacent epics (do NOT duplicate)
- **AD-853–857 (Crew Self-Unblock)** is how a *single* agent gets unblocked mid-task (request a grant/skill/build). **This epic is how *multiple* agents complete *one hard task together*.** They compose: a subtask in AD-859 that hits a capability gap files an AD-854 `CapabilityRequest` and blocks/resumes via AD-855 — self-unblock is the per-subtask recovery path inside crew collaboration. Keep the boundary clean: 853–857 = capability acquisition; 858–862 = collaboration/verification/synthesis.
- **AD-856 (AgenticLoop executor)** is the per-subtask engine — this epic depends on it; build 856 first. Do NOT re-implement the loop.
- **`ParallelDispatcher` (AD-594c)** is the fan-out substrate — reused unchanged except for the swappable decomposer (858). Do NOT fork it.
- **`RedTeamAgent`** stays the *deterministic* tool verifier; AD-860 is the *semantic* sibling. Two verifiers, different jobs.

## Verify-first reminders for the Builder
- `TrustNetwork.record_outcome` signature is ASSUMED — grep the real method (per memory: real shape is `record_outcome(agent_id, success, weight, intent_type, source)`) before citing; do NOT invent `observe(...)`.
- `WorkItemRouter` dispatch entry point (`mesh/work_item_router.py:104`) — confirm the exact method name + params before wiring AD-859.
- `compute_shapley_values(votes, approval_threshold, use_confidence_weights)` takes `list[Vote]` — confirm the `Vote` dataclass fields before constructing them in AD-861.
- `update_work_item` / `create_work_item` signatures — confirm against `workforce.py` (the dispatcher calls `create_work_item(title, description, work_type, priority, depends_on, assigned_to, tags, metadata, created_by)`; mirror that shape).
- Only the events verified in `events.py` exist — add new event constants, do NOT assume others.
- Treat this spec as a lead, not ground truth: grep/read every cited API before editing (subagent/spec reports are leads — recurring memory lesson). The `ParallelDispatcher` already does fan-out + conflict serialization — read `consultation/dispatch.py:283-520` fully before adding anything, to avoid duplicating dispatch logic.
