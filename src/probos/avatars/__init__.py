"""ProbOS avatar pipeline (AD-721d, AD-721i).

`dsl.py` — `AvatarDSL` Pydantic model (the agent-authored appearance artifact).
`blender_renderer.py` — headless Blender renderer (AD-721i).
`_blender/` — non-package directory, scripts run only inside Blender's subprocess Python.
"""
