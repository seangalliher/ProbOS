# ProbOS — Progress Tracker

## Current Status: BF-138 CLOSED (Sovereign ID Completion — Fixed 10 remaining slot ID leak sites across 7 files. D1: drift_detector _get_crew_agent_ids() now uses resolve_sovereign_id(). D2: All 6 memory probes use _resolve_probe_agent_id() DRY helper for sovereign ID resolution at seed AND recall. D3: HXI agent chat episode creation + chat history recall use sovereign_id. D4: CLI SessionManager session entry, recall, and episode creation use sovereign_id via _sovereign_id attribute. D5: FeedbackEngine accepts identity_registry, _extract_agent_ids() resolves slot IDs to sovereign IDs via resolve_sovereign_id_from_slot(). D6: Recall exception logging elevated from debug to warning with BF-138 prefix. 8 files modified, 15 new tests. Build prompt: bf-138-sovereign-id-completion.md. Issue #141). BF-137 CLOSED (Stale stasis duration after partial boot — shutdown() had _started guard before session record write, partial boots never updated session_last.json. Fix: moved session record write before guard. 1 file modified. Issue #140). AD-584c COMPLETE (Recall Scoring Rebalance — Rebalanced composite scoring weights for QA-trained embeddings + convergence bonus for multi-channel evidence. Weight changes: keyword 0.10→0.20, trust 0.15→0.10, hebbian 0.10→0.05, recency 0.20→0.15, anchor 0.10→0.15, semantic stays 0.35. Convergence bonus: +0.10 when episode found by BOTH semantic AND keyword channels (spreading activation). Config: recall_convergence_bonus field added to MemoryConfig. Full call chain wiring: score_recall() → recall_weighted() → cognitive_agent.py. Clamped to max(0.0, bonus) for defense in depth. 3 files modified, 20 new tests. Build prompt: ad-584c-scoring-rebalance.md). AD-584a/b COMPLETE (Recall Pipeline Q→A Fix — Embedding model swap to multi-qa-MiniLM-L6-cos-v1 + template-based query reformulation + BF-029 prefix removal. Root cause: all-MiniLM-L6-v2 is STS-trained not QA-trained, cosine 0.10–0.35 for Q→A pairs. Tier 1: SentenceTransformerEmbeddingFunction with QA-trained model, 2-tier fallback chain (DefaultEmbeddingFunction → keyword overlap), collection migration on model mismatch. Tier 2: reformulate_query() regex-based question→declarative templates (10 patterns), dual-query in recall_for_agent_scored() merges results across variants taking best distance per episode, BF-029 "Ward Room {callsign}" prefix removed from DM recall queries. Config: embedding_model, query_reformulation_enabled. Migration: episodic, semantic (5 collections), procedure_store. sentence-transformers added to pyproject.toml. 10 files modified, 37 new + 2 updated tests. Build prompt: ad-584-recall-qa-fix.md. Issue #138). AD-584 PLANNED (Recall Pipeline Q→A Fix — Embedding model swap multi-qa-MiniLM-L6-cos-v1 + template-based query reformulation + BF-029 prefix removal. Root cause: all-MiniLM-L6-v2 is STS-trained not QA-trained, cosine 0.10–0.35 for Q→A pairs. 4-tier fix, build prompt ad-584-recall-qa-fix.md covers Tiers 1+2. Research: recall-pipeline-research-synthesis.md. Issue #138). BF-135 CLOSED (Session record write races shutdown timeout — moved session.record_write() before async operations in shutdown sequence. 1 file modified). BF-134 CLOSED (Recall pipeline semantic threshold + FTS scoring + watch section filtering — three interrelated defects: (1) agent_recall_threshold configurable default 0.15 replaces hardcoded 0.3, (2) fts_keyword_floor 0.2 for keyword-only hits, (3) anchor_watch_section promoted to ChromaDB metadata + recall_by_anchor() watch_section param + _try_anchor_recall() wiring + migration backfill. 5 files, 10 new + 3 updated tests). BF-063 CLOSED (Naming ceremony conditional logging — warning for LLM empty/oversized/invalid, info for valid choice. 3 new + 2 updated tests). BF-080 CLOSED (DM Channel Conversation Viewer — selectDmChannel action, dm-detail view state, click-to-navigate from DM Log, back header, reuses WardRoomThreadList/ThreadDetail. Satisfies AD-523a. 3 new tests). AD-583f/583g COMPLETE (Observable State Verification + Convergence Source Tracing — Ward Room echo chamber gap closure. 583g: ThreadEchoAnalyzer traces amplification chains to "Patient Zero" in Ward Room threads via temporal post ordering + Jaccard similarity + anchor independence. 583f: ObservableStateVerifier with pluggable StateProvider protocol (Recreation/Trust/Health) verifies agent claims against system ground truth. Satisfies AD-569d deferral — populates convergence_correctness_rate in BehavioralSnapshot. New events: WARD_ROOM_ECHO_DETECTED, OBSERVABLE_STATE_MISMATCH. Counselor subscriptions + therapeutic DMs. 4 new files, 10 modified, 42 tests. Build prompt: prompts/ad-583fg-observable-state-source-tracing.md. Issue #117). BF-133 CLOSED (Qualification probe anchor gate failure + unrealistic content framing — Two issues: (1) _make_test_episode() created episodes with anchors=None, scoring 0.0 in compute_anchor_confidence(). recall_weighted() anchor_confidence_gate (default 0.3) silently filtered ALL test episodes. Fix: always populate AnchorFrame with realistic defaults (department="qualification", channel="probe", watch_section="first", trigger_type="direct_message") → confidence ≈ 0.44. (2) Episode content lacked production Ward Room framing. BF-029 prepends "Ward Room {callsign}" to recall queries, biasing embeddings toward "[Ward Room]" prefixed content. Seeded episodes had bare facts, reducing semantic similarity. Fix: _ward_room_content() helper wraps all episode content in production "[Ward Room] {channel} — probe: {text}" format. 6 probes updated. 2 files modified, 1 test updated). AD-583 COMPLETE (Wrong Convergence Detection — Echo chamber vs independent collaborative insight. Wires AD-567f anchor independence scoring into AD-554 convergence pathway. 5 sub-ADs: (a) convergence independence scoring in check_cross_agent_convergence() return dict via compute_anchor_independence(), (b) WRONG_CONVERGENCE_DETECTED event + ALERT-severity bridge alert, (c) real-time integration in proactive.py, (d) Counselor subscription + therapeutic DM on extreme cases + groupthink handler upgrade, (e) dream Step 7g wrong convergence flagging + Step 9 EmergenceSnapshot.provenance_independence population. Case study motivation: Chapel tic-tac-toe echo chamber. 8 files modified, 28 new tests). AD-582 COMPLETE (Memory Competency Probes — LongMemEval-Inspired. 6 probes in cognitive/memory_probes.py: SeededRecallProbe (Tier 2, seeds 5 episodes with known facts, scores recall against ground truth via check_faithfulness() + LLM), KnowledgeUpdateProbe (Tier 2, seeds contradictory episodes at different timestamps, tests temporal recency), TemporalReasoningProbe (Tier 2, seeds episodes across watch sections, tests anchor-based temporal filtering), CrossAgentSynthesisProbe (Tier 3, seeds episodes in different agent shards, tests OracleService cross-shard synthesis via global recall()), MemoryAbstentionProbe (Tier 2, seeds topic A episodes then asks about topic B, tests confabulation resistance with dual LLM+keyword scoring), RetrievalAccuracyBenchmark (Tier 1 threshold 0.0, infrastructure precision@k/recall@k measurement). Three shared helpers: _seed_test_episodes() using seed() to bypass rate limiting/dedup, _cleanup_test_episodes() via evict_by_ids() in finally blocks, _make_test_episode() with AnchorFrame construction. Registered in runtime.py after behavioral probes. 3 files modified/created, 24 new tests). AD-580 COMPLETE (Alert Resolution Feedback Loop — Crew-driven alert acknowledgment and suppression. Three modes: Dismiss (time-bounded, default 4h), Resolve (clean-period-gated re-fire, 1h), Mute (indefinite). BridgeAlertService: dismiss_alert(), resolve_alert(), mute_alert(), unmute_alert(), list_suppressed(), _is_suppressed(), _last_detected tracking in _should_emit() independent of emission. BridgeAlertConfig + 2 fields (resolve_clean_period, default_dismiss_duration). Startup wiring in communication.py. 5 API endpoints (POST dismiss/resolve/mute/unmute, GET suppressed). Shell /alert command (commands_alert.py + shell.py registration) with pattern-prefix matching for human-readable dedup keys. Ward Room acknowledgment posting at caller level. 7 files modified, 21 new tests). BF-124 CLOSED. AD-570c COMPLETE. AD-568a/b/c COMPLETE (Adaptive Source Governance — Dynamic episodic vs parametric memory weighting with three capabilities: (1) Task-Type Retrieval Router — RetrievalStrategy enum (NONE/SHALLOW/DEEP), classify_retrieval_strategy() maps intent types to strategies, confabulation safety gate downgrades DEEP→SHALLOW at >0.3 confab rate, Oracle Service wired for ORACLE+DEEP tiers. (2) Adaptive Budget Scaling — BudgetAdjustment dataclass, compute_adaptive_budget() scales context budget from anchor confidence/episode count/score distribution, floor 500/ceiling 12000, wires previously dead cross_department_anchors config. (3) Source Priority Framing — SourceAuthority enum (HIGH/MODERATE/LOW), SourceFraming dataclass, compute_source_framing() produces confidence-calibrated framing (AUTHORITATIVE/SUPPLEMENTARY/PERIPHERAL), _format_memory_section() integration across all 3 cognitive paths. New file: cognitive/source_governance.py. 5 files modified, 30 new tests + 187 regression = 217 passed). AD-462c/d/e COMPLETE (Memory Architecture Extensions — Variable Recall Tiers: RecallTier enum parallels AgencyLevel with trust-gated memory depth (BASIC/ENHANCED/FULL/ORACLE), resolve_recall_tier_params() DRY helper wired into _recall_relevant_memories() and _gather_context(). Oracle Service: OracleService aggregates all 3 knowledge tiers (EpisodicMemory + RecordsStore + KnowledgeStore) with normalized scoring and source provenance. Social Memory: SocialMemoryService "does anyone remember?" protocol via Ward Room thread_mode="memory_query", agents detect and respond from sovereign shard. Ward Room wiring in runtime.py due to startup phase ordering. AD-462f concept graphs remain DEFERRED. 4 new files, 5 modified, 29 new tests). AD-569 COMPLETE (Observation-Grounded Crew Intelligence Metrics — BehavioralMetricsEngine with 5 content-level behavioral metrics: Analytical Frame Diversity, Synthesis Detection, Cross-Department Trigger Rate, Convergence Correctness, Anchor-Grounded Emergence. Dream Step 13 integration. 5 Tier 3 qualification probes. API: /behavioral-metrics, /behavioral-metrics/history. BehavioralMetricsConfig. BEHAVIORAL_METRICS_UPDATED event. 11 files modified, 36 new tests). BF-126 CLOSED (Post-stasis cooperation cluster false positives — crew-identified by Forge + Reyes. Synchronized agent startup after stasis creates correlated Hebbian activity that cooperation cluster detector misinterprets as emergent cooperation. Extended set_cold_start_suppression() to also suppress clusters during 300-second window. 2 files modified, 2 new tests). BF-125 CLOSED (Working memory game engagement desync — crew-identified by Horizon/Forge/Reyes. Four defects: opponent WM orphaned on game completion, reply-path and DM-path MOVE had no WM cleanup, GAME_COMPLETED event had zero subscribers. Fix: centralized GAME_COMPLETED event subscriber in finalize.py cleans both players' WM. Replaced 3 inline cleanup blocks. Existing restore revalidation retained as defense-in-depth. 3 files modified, 1 new test file, 8 new tests) (Working memory not resynced after stasis recovery — crew-identified by Horizon via tic-tac-toe game state drift). BF-123 CLOSED (Ward Room router response path doesn't parse CHALLENGE/MOVE — root cause of recreation system never triggering. When Captain posts in Ward Room, agents respond through `ward_room_router.handle_event()` → intent_bus, which only extracted endorsements and DMs. CHALLENGE/MOVE tags were posted as raw text, bypassing `_extract_and_execute_actions()` in proactive.py entirely. Fix: `_extract_recreation_commands()` method on WardRoomRouter with full CHALLENGE/MOVE extraction, rank gating, Recreation channel thread creation, game lifecycle management, working memory engagement. Also removed DIAG-REC diagnostic logging from proactive.py. 3 files modified, 5 new tests). BF-121 CLOSED (CHALLENGE/MOVE tags inside REPLY blocks posted as text but never parsed as commands — agents wrap output in `[REPLY thread_id]...[/REPLY]`, reply extraction consumes the block before challenge regex runs. Fix: `_extract_commands_from_reply()` method parses CHALLENGE/MOVE from reply body, creates games/makes moves, strips tags before posting. Also applies BF-120 markdown stripping to reply body). BF-122 CLOSED (`callsign` undefined in `_extract_and_execute_actions()` recreation block — relied on variable leaked from notebook `for` loop. If agent produced `[CHALLENGE]` without `[NOTEBOOK]`, NameError crashed silently. Fix: resolve callsign from `callsign_registry` at recreation block entry). 1 file modified, 6 new tests. BF-120 CLOSED (Markdown formatting in LLM output breaks structured tag parsing — `**[CHALLENGE ...]**` or `` `[CHALLENGE ...]` `` not matched by regex. Two-line preprocessing in `_extract_and_execute_actions()` strips `*` and `` ` `` around brackets before all regex patterns. Same fix applied to DM path in `routers/agents.py`. Inline `import re` cleaned up to module-level. 2 files modified, 8 new tests). AD-576 COMPLETE (LLM Unavailability Awareness — EPS Power Brownout Protocol. Three-layer infrastructure awareness: (1) LLM status state machine in proactive loop — operational/degraded/offline states with transition events + Bridge Alerts (ALERT for offline, ADVISORY for degraded, INFO for restored). (2) Infrastructure context injection into `_gather_context()` → rendered as `[INFRASTRUCTURE NOTE]` in `_build_user_message()` before self-monitoring section — tells agents "this is infrastructure, not cognitive." (3) Circuit breaker infra-correlation tagging — `CognitiveEvent.infrastructure_degraded` flag filters brownout events from velocity/similarity computation, preventing false trips. Counselor clinical gating on `_on_self_monitoring_concern()` suppresses infrastructure-correlated amber concerns. Wires previously dead `LLM_HEALTH_CHANGED` event via `LlmHealthChangedEvent` dataclass. Also fixes BF-116 (dead `circuit_breaker_redirect` removal + AD-567g `orientation_supplement` rendering completion) and BF-117 (convergence/divergence bridge alert delivery — `rt.bridge_alerts` + `rt.ward_room_router`). 7 files modified, 1 new test file, 29 new tests). BF-118 CLOSED (OrientationService startup crash — `runtime` not in scope in `cognitive_services.py`. One-line fix: pass `ontology` parameter directly). BF-119 CLOSED (DM game challenge parsing — `[CHALLENGE @callsign game_type]` only parsed in proactive loop, silently ignored in DM responses. Added CHALLENGE parsing to HXI Profile DM path in `routers/agents.py` alongside existing AD-572 MOVE parsing. Recreation channel thread creation, game creation, working memory engagement registration. Standing orders updated with recreation syntax). AD-575 COMPLETE (Unified Self-Awareness — Cross-Context Identity Recognition. `_detect_self_in_content()` method on CognitiveAgent scans Ward Room thread content for the agent's own callsign using word-boundary regex. Self-recognition cue injected in WR branch of `_build_user_message()` — agent told "references to '{callsign}' refer to YOU, respond as a participant not an observer." Cross-context engagement binding: when self-mentioned AND has active game engagement via AgentWorkingMemory, injects participatory awareness ("Spectators are watching your game"). Generalizes BF-102 commissioning awareness pattern. 1 file modified, 1 new test file, 10 new tests). AD-513 Phase 1 COMPLETE (Ship's Crew Manifest — Queryable Crew Roster. Three-layer delivery: (1) `get_crew_manifest()` on VesselOntologyService — query-time assembly from ontology/trust/callsign subsystems with dependency-injected enrichment. (2) Anti-confabulation cognitive grounding via `_build_crew_complement()` in `_build_temporal_context()` — department-grouped "SHIP'S COMPLEMENT" block injected into ALL prompt pathways (DM, WR, proactive). OrientationContext `crew_names` for cold/warm boot. (3) HXI CrewRosterPanel — draggable floating panel with dept filter chips, rank badges, trust bars, click-to-profile. REST endpoint `/api/ontology/crew-manifest`. LoD fix: `get_all_assignments()` public method. 10 files modified, 14 new tests). AD-574 COMPLETE (DM Reply Agent Notification — Captain posts in DM channels now notify the target agent. Two independent gaps fixed: (1) find_targets() in WardRoomRouter added "dm" channel type case matching agent.id[:8] against channel name, no Earned Agency gating. (2) get_unread_dms() query rewritten from "agent has zero posts" to "last author is not the agent" via LEFT JOIN subquery with COALESCE — catches Captain replies in agent-initiated threads. 2 files modified, 9 new tests). BF-114b CLOSED (Remove Dead `proactive_extends_idle` Code Path — dead code cleanup following BF-114. Removed: `proactive_extends_idle` from DreamingConfig + system.yaml, `record_proactive_activity()` + `is_proactively_busy` + `_last_proactive_time` from DreamScheduler, idle calculation simplification, proactive.py caller, startup wiring, DreamAdapter busy guard. 9-test TestDreamSchedulerProactiveAwareness class removed, 1 focused emergent detector test added. Net -8 tests. 7 files modified). AD-573 COMPLETE (Unified Agent Working Memory — Cognitive Continuity Layer. Per-agent AgentWorkingMemory maintaining active situation model across all cognitive pathways. Ring-buffered entries (actions, observations, conversations, events) with token-budget-aware render_context() and 6-priority eviction. ActiveEngagement tracking for games/tasks/collaborations. All pathways write: handle_intent() action recording, proactive observation recording, DM conversation recording, [CHALLENGE]/[MOVE] game engagement management, cognitive state sync, active game sync. All pathways read: render_context() injected into DM/WR/proactive _build_user_message() branches. SQLite-backed WorkingMemoryStore (ConnectionFactory, start()/stop(), BEGIN IMMEDIATE + asyncio.Lock). Freeze on shutdown, restore on stasis recovery with stale pruning + game revalidation. System prompt _has_active_game() replaced with working_memory.has_engagement("game"). WorkingMemoryConfig in SystemConfig. 2 new files, 7 modified, 2 new test files, 35 new tests). AD-572 COMPLETE (Captain Engagement Priority — Active State Awareness in DM Path. Active game state injection into DM _build_user_message() via _build_active_game_context(). DM system prompt augmentation with [MOVE] instruction when game active. [MOVE pos] parsing from DM responses in agents router with move execution against RecreationService. get_game_by_player() DRY method on RecreationService replaces 3 instances of iterate-and-match in proactive.py. Ward Room board update posting from DM path. gameMoveExecuted flag in REST response. 4 files modified, 3 new test files, 18 new tests). AD-526b COMPLETE (HXI Tic-Tac-Toe — Captain vs Crew Game Panel. REST router /api/recreation/ (challenge, move, active, forfeit) with Depends() DI. Floating GamePanel.tsx (CSS Grid board, piece-pop animations, win-line glow, pulse-dim waiting). GAME_UPDATE event emission from RecreationService. Game rehydration on refresh. Challenge button in ProfileInfoTab. forfeit_game() service method. Zustand store game state slice + WebSocket handler. 2 new files, 7 modified, 16 new tests). BF-110 CLOSED (Game Board Invisible to Agents — get_recent_activity() only returns top-level threads, not replies where board updates are posted. Fixed: inject active game state (board, valid moves, turn indicator) directly from RecreationService into proactive context. 2 files modified, 0 new tests). AD-526a COMPLETE (Social Channels + Tic-Tac-Toe — agent recreation framework. GameEngine protocol + TicTacToeEngine. RecreationService with game lifecycle, Ship's Records integration, GAME_COMPLETED event emission. Recreation + Creative default Ward Room channels with auto-subscription. Proactive integration: Recreation channel context gathering, [CHALLENGE @callsign game_type] and [MOVE position] action extraction with rank gating (Lieutenant+). cognitive_agent.py challenge/move instructions. RecreationService wired in finalize.py with ward_room + records_store + emit_event. 10 files modified, 47 tests). AD-570 COMPLETE (Anchor-Indexed Episodic Recall — Structured AnchorFrame Queries. Promotes 4 anchor fields (department, channel, trigger_type, trigger_agent) to top-level ChromaDB metadata for native where-clause filtering. One-time migration backfills existing episodes. recall_by_anchor() API with two modes: enumeration (structured filters, no embedding) and semantic re-ranking (structured + vector similarity). Post-retrieval agent_id filtering, activation tracking, hash verification. 2 files modified, 23 tests). AD-570b COMPLETE (Participant Index Sidecar — SQLite junction table indexing episode participants (sovereign IDs + callsigns) for O(1) lookups. 20 tests). BF-109 CLOSED (Qualification Probe Param Key Mismatch. _send_probe() sent params={"message":...} but perceive() reads params.get("text"). All Tier 1/2 probe results were unreliable — agents never received the question. One-line fix: "message" → "text". First real qualification run: 130/131 pass (99.2%), 15 agents, 130 baselines established. Only failure: Security Officer mti_temperament at 0.000). BF-108 CLOSED (LLM Unreachable — No Runtime Visibility. MockLLMClient.get_health_status() now returns overall:"mock" with all tiers offline. runtime.llm_is_mock property. Chat endpoint returns explicit offline message instead of triggering self-mod. /system/services correctly reports LLM as offline. 4 files, 0 new tests — covered by existing 82 tests). AD-567g COMPLETE (Cognitive Re-Localization — final AD in Memory Anchoring lineage 567a→g. Structured orientation at boot: cold start full orientation (identity + cognitive grounding + first duty), warm boot stasis recovery, diminishing proactive supplement. Anchor field gap fixes: watch_section, event_log_window, Ward Room department. OrientationService, OrientationContext, derive_watch_section(). Subsumes BF-034 cold-start note. 28 tests). AD-567f COMPLETE (Social Verification Protocol — absorbs AD-462d. Cross-agent claim verification, corroboration scoring, cascade confabulation detection. Privacy-preserving: agents see metadata not content. Anchor independence discriminates corroboration from cascade. Ward Room integration after AD-506b peer similarity. Bridge Alerts on medium/high risk. Counselor therapeutic DMs on high risk. 28 tests). AD-567d COMPLETE (Anchor-Preserving Dream Consolidation + Active Forgetting — provenance composition through dream pipeline (absorbs AD-559), ACT-R activation-based memory lifecycle (absorbs AD-462b), ActivationTracker with SQLite access log, dream Step 12 activation pruning, micro-dream replay reinforcement, recall access recording, 31 tests). AD-567c COMPLETE (Anchor Quality & Integrity — Johnson-weighted confidence scoring, RPMS confidence gating, per-agent AnchorProfile for Counselor diagnostics, SIF check_anchor_integrity(), drift classification (specialization/concerning/unclassified), absorbs AD-567e). AD-567b COMPLETE (Anchor-Aware Recall + Salience-Weighted Retrieval — RecallScore composite scoring, FTS5 keyword search sidecar, anchor context headers in memory formatting, SECONDHAND source wiring, recall_weighted() API with budget enforcement. Absorbs AD-462a). AD-567a COMPLETE (Episode Anchor Metadata — AnchorFrame dataclass with 10 fields across 5 dimensions, all 15 episode creation sites wired, serialization round-trip through ChromaDB, content hash exclusion, backwards compatible). BF-104 CLOSED (Display Crew Agent Count — registry.crew_count(), shell prompt shows crew not total, status panel/API/working memory updated). AD-566f COMPLETE (/qualify Shell Command — manual trigger and inspection, 5 subcommands: status/run/agent/baselines). AD-566e COMPLETE (Tier 3 Collective Tests — 5 crew-wide probes: CoordinationBreakevenProbe, ScaffoldDecompositionProbe, CollectiveIntelligenceProbe, ConvergenceRateProbe, EmergenceCapacityProbe + QualificationHarness.run_collective() + DriftScheduler collective integration). AD-566d COMPLETE (Tier 2 Domain Tests — 5 department-gated probes: TheoryOfMindProbe, CompartmentalizationProbe, DiagnosticReasoningProbe, AnalyticalSynthesisProbe, CodeQualityProbe + DriftScheduler tier generalization). AD-566c COMPLETE (Drift Detection Pipeline — DriftDetector z-score engine, DriftScheduler periodic runner, Counselor/VitalsMonitor/BridgeAlerts integration). AD-566b COMPLETE (Tier 1 Baseline Tests — 4 psychometric probes: PersonalityProbe, EpisodicRecallProbe, ConfaculationProbe, TemperamentProbe). AD-566a COMPLETE (Qualification Test Harness Infrastructure — psychometric protocol, store, harness engine). AD-541f COMPLETE (Eviction Audit Trail — Append-Only Accountability). AD-541 Lineage CLOSED (6/6 pillars complete). AD-541e COMPLETE (Content Hashing — Cryptographic Episode Integrity). BF-103 CLOSED (Episodic Memory Agent ID Mismatch — sovereign ID normalization + migration). AD-541d COMPLETE (Guided Reminiscence — Therapeutic Memory Sessions). AD-541c COMPLETE (Spaced Retrieval Therapy — Active Recall Practice). AD-541b COMPLETE (Reconsolidation Protection — Read-Only Memory Framing). AD-555 COMPLETE (Notebook Quality Metrics & Dashboarding). AD-554 COMPLETE (Real-Time Cross-Agent Convergence & Divergence Detection). AD-553 COMPLETE (Quantitative Baseline Auto-Capture). AD-552 COMPLETE (Notebook Self-Repetition Detection). AD-551 COMPLETE (Notebook Consolidation — Dream Step 7g). AD-550 COMPLETE. BF-069 CLOSED. BF-101/102 COMPLETE. AD-560 COMPLETE. AD-557 COMPLETE. AD-558 COMPLETE. BF-099 CLOSED. Cognitive JIT pipeline COMPLETE (9/9). 618 Cognitive JIT tests. 6,893 tests (6,714 pytest + 179 vitest).

---

## Development Eras

| Era | Phases | Codename | Status |
|-----|--------|----------|--------|
| [**Era I: Genesis**](progress-era-1-genesis.md) | 1-9 | Building the Ship | Complete |
| [**Era II: Emergence**](progress-era-2-emergence.md) | 10-21 | The Ship Comes Alive | Complete |
| [**Era III: Product**](progress-era-3-product.md) | 22-29 | The Ship Sets Sail | Complete |
| [**Era IV: Evolution**](DECISIONS.md) | 30 | The Ship Evolves | Active |
| Era V: Civilization | 31-36 | The Ship Becomes a Society | Planned |

## Release Tagging Policy

Tags use the format `v{major}.{minor}.{patch}-phase{N}` (e.g., `v0.4.0-phase29c`).

| Event | Tag? | Example |
|-------|------|---------|
| Phase completed | Yes — bump minor version | `v0.5.0-phase30` |
| Critical bug fix between phases | Yes — bump patch version | `v0.4.1-phase29c` |
| Roadmap/docs-only changes | No | — |
| Mid-phase work (partial features) | No | — |
| First production-ready release | Yes — `v1.0.0` | After security hardening (Phase 31) |

**Current tags:**

| Tag | Commit | Date | Notes |
|-----|--------|------|-------|
| `v0.1.0-phase12` | — | — | End of Era I: Genesis |
| `v0.4.0-phase29c` | `13d52a7` | 2026-03-17 | End of Phase 29c: Codebase Knowledge |

## Design Principles

See [design-principles.md](docs/development/design-principles.md) for full design principles:
- "Brains are Brains" (Nooplex core principle)
- Agent Development Model (Communication + Simulation)
- HXI Self-Sufficiency, Agent-First Design, Cockpit View
- Probabilistic Agents, Consensus Governance
- Agent Classification Framework (Core / Utility / Domain)
- Foundational Governance Axioms (Safety Budget, Reversibility, Minimal Authority)
- "Cooperate, Don't Compete" (Federation philosophy)
- Visiting Officer Subordination
- Extension-First Architecture
- Markdown is Code

---

## Environment

- **Platform:** Windows 11 Pro (10.0.26200)
- **Python:** 3.12.13 (installed via uv)
- **Toolchain:** uv 0.10.9
- **Key deps:** pydantic 2.12.5, pyyaml 6.0.3, aiosqlite 0.22.1, httpx 0.28+, rich 13.0+, chromadb 1.5.4, pytest 9.0.2, pytest-asyncio 1.3.0, pytest-xdist 3.8.0, pytest-timeout 2.4.0
- **LLM endpoints:** Fast tier: Ollama at `http://127.0.0.1:11434/v1`, Standard/Deep tier: VS Code Copilot proxy at `http://127.0.0.1:8080/v1`
- **LLM models:** fast=qwen3.5:35b (local Ollama), standard=claude-sonnet-4.6 (Copilot proxy), deep=claude-opus-4.6 (Copilot proxy)
- **Run tests:** `uv run pytest tests/ -v` (sequential) or `uv run pytest -n auto` (parallel, 13x faster)
- **Run fast tests:** `uv run pytest -n auto -m "not slow"` (skip 65 sleep-heavy tests)
- **Run demo:** `uv run python demo.py`
- **Run interactive:** `uv run python -m probos`
