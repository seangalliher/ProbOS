AD-679 Selective Disclosure Routing — PLANNED. Formal message classification layer (public/department/private/captain-only) enforced by the messaging infrastructure rather than left to agent judgment. Ward Room posts, DMs, and event payloads tagged with disclosure level; routing layer prevents PII or sensitive operational data from reaching public channels. Extends the paper's selective disclosure principle to ProbOS's virtual messaging fabric.

AD-678 Memory Transparency Mechanism — PLANNED. Extends Westworld Principle: agents can explain what they know, where they learned it, and why they believe it. Captain and crew can query an agent's knowledge provenance on request. Agents transparently report their own epistemic state — the inverse of the Counselor Minority Report principle (voluntary disclosure vs covert extraction). Supports epistemic degradation diagnosis.

AD-677 Context Provenance Metadata — PLANNED. Every context element flowing through NATS and working memory tagged with structured provenance: {source, confidence, sensitivity, timestamp, layer}. Enables consumers to assess claim quality, supports Signal Visibility feedback, and provides the epistemic grounding for AD-678 transparency queries. Directly addresses epistemic degradation concern — agents can distinguish high-provenance facts from low-confidence inferences.

AD-676 Action Risk Tiers — PLANNED. Formal risk classification on actions (not agents). Three tiers: autonomous (info retrieval, analysis, Ward Room posts) → confirm (proposals, duty log entries, trust-affecting observations) → dual-control (trust modifications, standing order changes, system config, Captain-level operations). Orthogonal to agent trust — a trusted agent still needs confirmation for high-risk actions. Formalizes the HXI Cockpit View / Captain's stick principle into enforceable policy.

AD-675 Uncertainty-Calibrated Initiative — PLANNED. Confidence scores from AD-444 confidence tracker modulate agent assertiveness. Low confidence = hint at most. High confidence = suggest or act. Medium = suggest and wait. Wires confidence_tracker output to the initiative scale (AD-674), creating a principled model for "how assertive should I be right now?" rather than binary on/off proactivity.

AD-674 Graduated Initiative Scale — PLANNED. Five-level agent initiative continuum: silent → hint → suggest → offer → act. Orthogonal to self-regulation zones (GREEN/AMBER/RED/CRITICAL govern restraint; initiative governs assertiveness). Trust level sets the agent's maximum initiative ceiling. Duty cycle modulates baseline: off-duty agents default to silent, on-duty agents graduate based on context confidence and trust. Absorbed from Ambient Intelligence paper (Chen et al. 2026).

AD-444 Knowledge Confidence Scoring — CLOSED. Added in-memory ConfidenceTracker for Ship's Records entries with confirm/contradict scoring, auto-supersede checks, presentation tiers, RecordsStore delegation, ConfidenceConfig, knowledge confidence event types, startup wiring, and Dream Step 10 quality cross-reference. 12 new tests; focused AD-444 gate passed. Full xdist gate reported 9688 passing/14 skipped with xdist worker-crash artifacts triaged serially; proactive stagger nodeids passed with raised timeout due AD-636 delay.

AD-563 Knowledge Linting — CLOSED. Added keyword-based KnowledgeLinter for Ship's Records notebooks with inconsistency detection, department coverage gaps, same-topic cross-reference suggestions, LintConfig, DreamReport lint fields, startup wiring, and Dream Step 10 reporting. 12 new tests; focused AD-563 gate passed. Targeted dreaming/config regression passed 93 tests. Full xdist gate reported 9706 passing/14 skipped with worker-crash artifacts serial-triaged; proactive stagger nodeids passed with raised timeout due AD-636 delay.

AD-564 Quality-Triggered Forced Consolidation — CLOSED. Added QualityConsolidationTrigger with low-quality, stale-rate, and repetition-rate thresholds, cooldown and daily-limit gates, forced-consolidation event emission, QualityTriggerConfig, DreamReport forced_consolidations, startup wiring, and Dream Step 10 trigger checks. 10 new tests; focused AD-564 gate passed. Targeted dreaming/config regression passed 105 tests. Full xdist gate reported 9668 passing/14 skipped with worker-crash artifacts; the only assertion-style failure passed serially and focused serial crash triage passed.

AD-565 Quality-Informed Routing — CLOSED. Added QualityRouter with neutral-by-default per-agent quality weights, QualityRouterConfig, QUALITY_CONCERN event emission, Counselor diagnostic API, startup wiring, DreamingResult exposure, and Dream Step 10 per-agent quality updates from AD-555 snapshots. 10 new tests; focused AD-565 gate passed. Targeted notebook/dream regression passed 103 tests. Full xdist gate reported 9701 passing/14 skipped with worker-crash/resource-pressure artifacts; direct non-worker-looking failures passed serially, proactive stagger nodeids passed with raised timeout, and 53 exact non-proactive xdist-failed nodeids passed serially before extended triage was stopped for duration.

AD-571 Agent Tier Trust Separation — CLOSED. Added AgentTierRegistry and AgentTierConfig, tier-aware TrustNetwork score filtering and CORE trust-record skip, crew-only cascade counting, crew-only EmergenceMetrics author/pair filtering, HebbianRouter crew-only reporting, and finalize-time registry population. 15 new tests; focused AD-571 gate passed. Full xdist gate reported 9631 passing/14 skipped; 145 xdist worker-crash nodeids passed serial triage. Test 15 uses TrustNetwork.get_record() and get_events_for_agent() because the original outcomes_for() assertion referenced a phantom API.

AD-572 Episodic-Procedural Bridge — CLOSED. Added EpisodicProceduralBridge as Dream Step 7h to scan dream clusters against existing procedures for novel cross-cycle patterns, with BridgeConfig, DreamReport bridged_procedures reporting, startup wiring, and procedure-store persistence for newly bridged procedures. 10 new tests; focused AD-572 gate passed. Full xdist gate reported 9539 passing/14 skipped; 138 xdist worker-crash nodeids passed serial triage.

AD-574 Episodic Decay & Reconsolidation Scheduling — CLOSED. Added ReconsolidationScheduler with importance-scaled Ebbinghaus review intervals, ReconsolidationConfig, high-importance store-time scheduling in EpisodicMemory, Dream Step 11b due-review processing, and startup wiring. 12 new tests; focused AD-574 gate passed. Targeted dreaming regression passed 40 tests. Full xdist gate reported 9839 passing/14 skipped; 118 xdist worker-crash nodeids passed serial triage.

AD-579c Validity-Aware Dream Consolidation — CLOSED. Added temporal validity spans to EpisodeCluster, compute_cluster_validity(), cluster validity propagation, EpisodicMemory.update_episode_validity(), and dream procedure-evolution expiry for superseded source episodes. 8 new tests; focused AD-579c gate passed. Targeted clustering/dreaming regression passed 80 tests. Full xdist gate reported 9821 passing/14 skipped; one assertion-style failure and 136 worker-crash nodeids passed serial triage.

AD-594 Crew Consultation Protocol — CLOSED. Added ConsultationProtocol request/response lifecycle with directed and expert-selected routing, CapabilityRegistry/BilletRegistry/TrustNetwork weighted scoring hooks, rate limiting, timeout handling, typed consultation events, ConsultationConfig, CognitiveAgent consultation handler registration, startup/result/finalize wiring, and 24 focused tests. Targeted gates passed; full xdist suite stopped on unrelated worker-crash infrastructure with no AD-594-specific failures.

AD-600 Transactive Memory — CLOSED. Added in-memory ExpertiseDirectory with agent topic profiles, confidence decay, dream-cycle cluster profile updates, ExpertiseConfig, OracleService expertise routing across top-k agent shards, and startup/runtime wiring into cognitive services and dreaming. 14 new tests; focused AD-600 gate passed. Oracle regression suite passed 29 tests. Full xdist gate reported 9865 passing/14 skipped; 115 worker-crash nodeids passed serial triage.

BF-245 CLOSED. NATS/xdist stream isolation - module-level `os.environ.setdefault("PROBOS_NATS_ENABLED", "false")` in conftest.py prevents real NATS connections during tests. NatsConfig.enabled field_validator with validate_default=True honors env override. Eliminates xdist worker crashes from JetStream stream name collisions. 8 tests; full xdist run had no NATS/JetStream collision signatures, with remaining worker crashes classified outside BF-245.

AD-603 COMPLETE. Anchor Recall Composite Scoring — added recall_by_anchor_scored() wrapper that applies score_recall() to anchor-retrieved episodes with trust, Hebbian, recency, keyword, temporal, importance, and structural anchor bonus signals. CognitiveAgent merge now combines scored anchor and semantic results by composite_score, preserving BF-155 watch filtering and legacy unscored fallback. 18 new tests; 53 focused/regression tests passed. Full-suite xdist timing failure classified as waived infrastructure after serial rerun passed.

AD-585 COMPLETE. Tiered Knowledge Loading — three-tier ambient/contextual/on-demand knowledge loader with cache invalidation, token budgets, trust summaries, typed KNOWLEDGE_TIER_LOADED events, CognitiveAgent context injection, and finalize-time wiring across crew agents. Focused AD-585 gates passed; full-suite NATS/xdist collision failures waived by Captain instruction on 2026-04-27. 34 tests.

BF-244 CLOSED. Ontology callsign desync after naming ceremony. Startup ordering: onboarding runs with ontology=None, so update_assignment_callsign() silently skips. Fix: callsign reconciliation loop in finalize.py syncs CallsignRegistry → ontology assignments after ontology is wired. Fixed identity context, peer, and superior callsign display in get_crew_context(). 8 tests. Issue #357.

AD-672 CLOSED. Agent Concurrency Management — added ConcurrencyManager with per-agent max_concurrent ceiling, priority-ordered bounded queue, capacity-approaching event emission, resource arbitration, diagnostic snapshot, ConcurrencyConfig role overrides, CognitiveAgent handle_intent slot wrapping, and startup finalization wiring. 18 new tests; 77 focused/config/CognitiveAgent regression tests passed. Full-suite NATS/xdist collision failures waived by Captain instruction. Issue #353.

BF-243 CLOSED. __new__ test pattern AttributeError after AD-601/494/595e build wave. Tests using ClassName.__new__(ClassName) bypass __init__, crash on new attributes (_tcm, _trait_adaptive_enabled, _qualification_standing, _novelty_gate). Fix: getattr(self, '_attr', default) guards at access sites in episodic.py, proactive.py, cognitive_agent.py. Source-side fix — no test changes needed.

AD-492 COMPLETE. Cognitive Correlation IDs — cross-layer trace threading. 12-char hex ID generated at perceive() time, threaded through observation dict, working memory, CognitiveJournal (schema + index), Episode, Ward Room post pipeline logging, and event payloads (SELF_MODEL_DRIFT, TASK_EXECUTION_COMPLETE, PROCEDURE_FALLBACK_LEARNING). Transient — not persisted in to_dict(). Cleared after lifecycle; next perceive() overwrites stale IDs. 21 tests.

AD-493 COMPLETE. Novelty Gate — Semantic observation dedup. Per-agent embedding fingerprints with cosine similarity (MiniLM, threshold 0.82), in-memory ring buffer (50/agent), 24h time decay. Three-layer dedup stack: BF-032 Jaccard (word-level) → AD-493 NoveltyGate (semantic/topic-level) → AD-632e Evaluate (LLM/thread-level). Wired into proactive loop and Ward Room post pipeline. Fail-open on embedding failures. 21 tests.

AD-494 COMPLETE. Trait-Adaptive Circuit Breaker — Per-agent circuit breaker thresholds from Big Five personality scores. Openness → velocity tolerance, Neuroticism → similarity sensitivity (inverted), Conscientiousness → cooldown duration (inverted), Extraversion → amber zone sensitivity. Pure deterministic compute_trait_thresholds() function. TraitAdaptiveThresholds frozen dataclass. Lazy registration in proactive loop. Safe clamping bounds. Backward-compatible (no traits = uniform thresholds). TraitAdaptiveConfig in SystemConfig. 28 tests.

AD-671 CLOSED. Dream-Working Memory Integration — added DreamWorkingMemoryBridge for pre-dream WM session-summary flush episodes and post-dream WM priming from dream insights, DreamWMConfig, DreamReport WM counters, DreamingEngine optional bridge hooks, startup wiring, and __new__-safe getattr guards. 13 new tests; 65 dream/config regression tests passed. Full-suite NATS/JetStream collision failures waived by Captain instruction. Issue #352.

AD-670 CLOSED. Working Memory Metabolism — added stateless MemoryMetabolism with TRIAGE, DECAY, AUDIT, and FORGET operations, MetabolismConfig, optional AgentWorkingMemory injection, live buffer exposure, run_cycle hook, and inline triage gates for all five record methods. 26 new tests; 69 focused working-memory/metabolism regression tests passed. Full-suite AD-580 timing failure passed serially and was classified as waived timing/load noise. Issue #351.

AD-669 CLOSED. Cross-Thread Conclusion Sharing — added ConclusionType/ConclusionEntry and a capped ConclusionLog in AgentWorkingMemory, TTL-based sibling conclusion rendering at priority 6, serialization, correlation ID attachment, lifecycle conclusion recording after act/report, and sibling conclusion injection before decide(). 16 new tests; 56 CognitiveAgent and 57 working-memory regression tests passed. Full-suite NATS/xdist worker crashes waived; debounce timing failure passed serially. Issue #350.

AD-668 CLOSED. Salience Filter — added pure SalienceFilter scoring with relevance, recency, novelty, urgency, and social dimensions; configurable SalienceConfig; BackgroundStream for sub-threshold entries; and optional AgentWorkingMemory promotion gating. Default AgentWorkingMemory behavior remains unchanged when no filter is configured. 35 new tests; 57 working-memory regression tests passed. Full-suite AD-585 timing failure waived after retry classification. Issue #349.

AD-667 COMPLETE. Named Working Memory Buffers — added Duty/Social/Ship/Engagement named buffers as a parallel index alongside legacy AgentWorkingMemory ring buffers, per-buffer token budgets, dual-write routing, selective render_buffers(), named buffer serialization, and legacy from_dict compatibility. render_context() remains unchanged. 15 new tests; 43 focused working-memory tests passed. Full-suite NATS/xdist collision failures waived by Captain instruction on 2026-04-27. Issue #348.

AD-666 CLOSED. Agent Sensorium Formalization — formalized CognitiveAgent context injections as the Agent Sensorium with SensoriumLayer, SENSORIUM_REGISTRY, SensoriumConfig, SENSORIUM_BUDGET_EXCEEDED event/dataclass, aggregate char-budget tracking in the chain path, and injection ordering audit in _build_user_message. Phase 1 completion of Ambient Awareness roadmap. 14 tests. Issue #347.

BF-241 CLOSED. NATS JetStream reconnect resilience — extracted `_recover_jetstream()` from `set_subject_prefix()`, added `_on_reconnected()` instance method, MockNATSBus parity. Completes NATS resilience stack (BF-229/230/231/232/241). 8 new tests.

BF-242 CLOSED. JetStream liveness probe — consecutive failure counter triggers recovery + suspension. Suspended JetStream bypasses to core NATS (no timeout penalty). Single-flight guard on recovery task. Probe verifies recovery success via stream_info(). MockNATSBus parity. Reconnect auto-resumes. 16 new tests.

BF-041 CLOSED. HXI SVG icon system — replaced all Unicode text glyphs across 18 component files with 25 shared SVG glyph components in `Glyphs.tsx`. STEP_ICONS string maps → STEP_ICON_COMPONENTS React component maps. IntentSurface FeedbackStatus.confirmText refactored string→ReactNode. 68 new glyph tests. Zero remaining Unicode icon glyphs (grep-verified).

BF-240 CLOSED. LLM Health Dwell-Time Criterion — configurable `min_consecutive_healthy` threshold (default 3) before diagnostic holds release. Prevents premature recovery on single healthy check amid instability. 10 new tests. Issue #344.

AD-665 COMPLETE. Corroboration Source Validation — graded provenance independence weights in compute_anchor_independence(), replacing AD-662 binary veto with version-aware scoring. Same-origin-different-version pairs get configurable independence weight (default 0.7). ProvenanceValidationResult report for diagnostics. Privacy-preserving ancestry_details (ids + reason only). Transitive ancestry deferred to future AD. 16 new tests. Issue #343.

AD-664 COMPLETE. EventLog Diagnostic Infrastructure — structured payload (data JSON column), correlation_id, parent_event_id tracing columns. Schema migration for existing DBs. query_structured() and get_event_chain() methods. _row_to_dict() DRY helper. Retrofitted dream/emergent, mesh, and QA events with structured payloads + correlation IDs. Engineering diagnostic query capability declared (capability + _handled_intents + LLM instructions); programmatic handler deferred to follow-up AD. 17 tests. Issue #337.

AD-663 COMPLETE. Provenance Producer Wiring — populate source_origin_id and artifact_version on AnchorFrame at 4 episode producer sites (WR reply, WR thread, proactive thought, cognitive agent action). Dream reflections deferred (deterministic IDs already provide dedup; provenance fields would encode same hash, no independent signal). Enables AD-662 consumer-side ancestry detection and AD-665 graded scoring. WR posts use post/thread ID as origin, SHA-256 body hash as version. Proactive uses observed WR post IDs. Cognitive agent uses correlation_id. anomaly_window_id deferred (no automated detector). 13 tests. Issue #355.

AD-618e COMPLETE. Cognitive JIT Bridge — Bill step completions → T3 skill acquisition via SkillBridge. StepSkillMapping with 3-level priority resolution (exact > bill-scoped > global). 5 default mappings (cognitive_skill→duty_execution, tool→tool_operation, post_to_channel/send_dm→communication, sub_bill→coordination). Dual exercise path: catalog entry → SkillBridge, no entry → AgentSkillService direct with auto-acquire at FOLLOW. Log-and-degrade. 22 tests. Issue #204.

AD-618d COMPLETE. HXI Bill Dashboard — definition registry on BillRuntime (register/list/get), FastAPI router (6 endpoints), React dashboard (catalog + instance timeline + role roster + step progression), WebSocket refetch-on-event, ViewSwitcher Bills tab. 15 backend + 3 frontend tests. Issue #204.

BF-239 CLOSED. Ward Room thread engagement tracking via working memory — agent-level cognitive dedup using ActiveEngagement pattern. Prevents duplicate replies when dispatched twice for the same thread. Cognitive lifecycle extracted to `_run_cognitive_lifecycle` helper; try/finally at call site ensures engagement cleanup on all exit paths. 19 tests. Issue #342.

BF-237 CLOSED. Pipeline-level post budget (N+1 post problem). LLM emits multiple [REPLY]/[MOVE] blocks → each fires create_post in the extractor loop → Step 7 fires another create_post for cleaned text. Fix: PostBudget dataclass threaded from process_and_post() → extract_and_execute_actions() → _extract_and_execute_replies(). Budget spent on first create_post; subsequent [REPLY]/[MOVE] blocks and Step 7 main post suppressed. Telemetry event on suppression. Four-layer dedup stack now complete: BF-234 (transport) → BF-236 (dispatch round) → BF-237 (pipeline invocation) → BF-197 (content similarity). 10 new tests. Issue #340.

AD-618c COMPLETE. Built-in Bills — 4 default YAML bills (General Quarters, Research Consultation, Incident Response, Daily Operations Brief) + loader functions (builtin discovery/parse, custom loading). XOR gateway with dual-input convergence in incident_response. 24 tests. Issue #204.

AD-618b COMPLETE. Bill Instance + Runtime — BillInstance/StepState/RoleAssignment dataclasses with InstanceStatus/StepStatus enums. BillRuntime service: activate() with WQSB role assignment (BilletRegistry + qualification filtering), step lifecycle (start/complete/fail/skip), cancel(), query API (get_instance, list_instances, get_agent_assignments). 8 EventType entries. BillConfig (max_concurrent_instances, default_step_timeout, allow_partial_assignment). Late-bound event callback + BilletRegistry setter. 30 new tests. Issue #204.

AD-618a CLOSED. Bill Schema + Parser — BillDefinition/BillStep/BillRole/BillActivation dataclasses with GatewayType (sequential/parallel/XOR/OR) and StepAction enums. YAML parser with schema validation (role references, branch targets, step ID uniqueness, action types, gateway-branch consistency, condition step references). Ship's Records gains `bills/` subdirectory with raw-YAML `write_bill()` (bypasses frontmatter wrapping) and `list_bills()` (globs `*.bill.yaml`, not `*.md`). New `src/probos/sop/` package. 50 new tests (46 schema/parser + 4 RecordsStore integration) including write→read round-trip. Foundation for AD-618b (runtime) and AD-618c (built-in bills).

AD-664 PLANNED. EventLog Diagnostic Infrastructure — structured payloads + query authority. Two gaps: (1) EventLog events emit bare string labels with no structured payload, correlation ID, or parent_event_id — root-cause tracing impossible. (2) No agent holds confirmed EventLog query authority — diagnostic relay chains dead-end. Crew-originated (Forge + Anvil, 5 proposals). Issue #337.
BF-235 CLOSED. Stale identity block in agent system prompt rendering across stasis boundaries. Root cause: @lru_cache on _build_personality_block() and _load_file() in standing_orders.py never cleared on stasis resume. Fix: unconditional clear_cache() + evict_cache_for_type() on stasis_recovery lifecycle event in finalize.py (not gated behind warm_boot_orientation). Defensive clear on all startups. Diagnostic logging. Crew-originated (Vance, Chapel, Anvil, Reed). 7 new tests. Issue #338.
BF-236 CLOSED. Semantic duplicate dispatch — round-scoped post tracker. `_route_to_agents()` had no check for "agent already posted in this conversational round." Back-to-back `route_event()` calls dispatched same agent for same thread with different intent IDs. Fix: `_posted_in_round` tracker on WardRoomRouter, keyed on `(agent_id, thread_id)`, cleared on Captain repost. Recorded by WardRoomPostPipeline after `create_post`. 120s eviction. Gate in Phase 1 after Layer 3, respects `is_direct_target` bypass. 13 new tests.
BF-234 CLOSED. Duplicate ward room responses — consumer-side dedup. 4 of 12 agents posted duplicate replies due to transport-layer duplicate delivery (JetStream redelivery or js_publish timeout-then-succeed). Fix: `_seen_intents` dict in `IntentBus._on_dispatch()` — 300s sliding-window keyed by `intent.id`, filtered to `ward_room_notification` only. WARN log + counter + telemetry event. `msg.ack()` on hit. BF-198 remains semantic round-tracking only. 11 new tests.
AD-556 CLOSED. Adaptive trust anomaly detection — per-agent z-score thresholding. Rolling window of per-agent trust score history (default 30 snapshots). Z-score computed against agent's personal delta baseline. Debounce requires consecutive anomalous cycles (default 2) before escalation. Dual-gate: population sigma AND personal z-score must both fire. New agents without history fall back to population-only detection. Four new config parameters on EmergentDetectorConfig. 26 new tests. Crew-originated design (Forge + Reyes, 2026-04-01).
BF-233 CLOSED. BF-204 grounding check false positives — entity IDs from ward room params (thread_id, channel_id, author_id) and agent identity (_agent_id, intent_id) not included in grounding source, causing legitimate ward room replies to be suppressed as confabulations. Fix: expand grounding source to include all entity IDs the agent was explicitly given in its input context. Fabricated hex IDs still caught. Known limitation: cross-agent post UUID references (not in agent's own params) may still trigger if agent uses full UUID instead of truncated 8-char form. 11 new tests.
BF-232 CLOSED. ensure_stream() inherits stale subject filters from previous boot — update_stream() silently fails to change subjects (BF-231 finding), causing persistent "no response from stream" warnings on every JetStream publish. Fix: new recreate_stream() method uses delete-then-create; ensure_stream() unchanged (non-destructive). Phase 2 startup and set_subject_prefix() now use recreate_stream(). _delete_stream() failure logging promoted from DEBUG to WARNING for real failures (not-found stays DEBUG). Completes BF-229/230/231 NATS resilience trilogy. 8 new tests.
AD-599 CLOSED. Reflection as Recallable Episodes — Dream Step 15 promotes consolidation insights to [Reflection] episodes in EpisodicMemory. Four reflection sources: convergence reports, emergence snapshots, notebook consolidations, dominant cluster patterns. MemorySource.REFLECTION enum value. Deterministic content-hash IDs for cross-cycle deduplication. Rate-limited (default 3/cycle). No LLM calls. 18 new tests. Depends on AD-551 + AD-567d.
AD-601 CLOSED. TCM Temporal Context Vectors — Howard & Kahana (2002) Temporal Context Model for episodic memory. Drifting context vector (d=16, rho=0.95) stored alongside each episode. Cosine similarity between current and stored context provides smooth temporal proximity gradient, replacing binary watch_section matching for new episodes. Legacy episodes fall back to BF-147/BF-155 logic. Pure Python, no numpy/scipy. 23 tests.
AD-595e CLOSED. Qualification gate enforcement at three pipeline points: BillRuntime step start, proactive duty dispatch, and CognitiveAgent context injection. Two-flag config (enforcement_enabled + enforcement_log_only) enables shadow mode rollout. All gates default ALLOW (graceful degradation). BillRuntime.start_step() now async. BilletRegistry gains get_qualification_standing() and check_role_qualifications() methods. CognitiveAgent caches standing with 5-min TTL. QUALIFICATION_GATE_BLOCKED event type. 16 new tests. Depends on AD-595d.
AD-595d CLOSED. Qualification-aware billet assignment. Post gains required_qualifications field. BilletRegistry gains check_qualifications() async method with allow_untested cold-start tolerance, assign_qualified() async gated assignment, and set_qualification_store() public setter. assign() unchanged — stays sync and unconditional. Startup wiring connects QualificationStore → BilletRegistry via public setter. Data model + check API only — production gate deferred to AD-595e. 22 new tests. Depends on AD-595a + AD-539.
AD-595c CLOSED. Standing orders billet templating. compose_instructions() now resolves {Billet Title} patterns to current callsigns via BilletRegistry: {Chief Engineer} → LaForge (Chief Engineer). Vacant billets render as Title (vacant). Code blocks and inline backticks excluded from processing. 18 new tests. Depends on AD-595a.
AD-595b CLOSED. Naming ceremony → BilletRegistry integration. BilletRegistry.assign() method added (validate post + emit BILLET_ASSIGNED, falls back to stored callsign when caller passes empty). Single billet assignment point after identity issuance covers cold-start, warm-boot, and non-crew paths. OrientationContext gains billet_title field. 13 new tests. Depends on AD-595a.
AD-595a CLOSED. BilletRegistry — read-side facade over DepartmentService adding title-based resolution (case-insensitive), roster snapshots (Watch Bill), and BILLET_ASSIGNED/BILLET_VACATED event types. BilletHolder frozen dataclass. Eagerly initialized in VesselOntologyService.initialize(). Mutators (assign/vacate) deferred to AD-595b. 17 new tests. Issue #165.
BF-231 CLOSED. JetStream stale DID stream fix — set_subject_prefix() now deletes and recreates streams instead of updating subject filters. Prevents "no response from stream" after probos reset generates a new ship DID. 5 new tests.
AD-584d CLOSED. Enriched embedding document — _prepare_document() now includes reflection alongside user_input (aligning ChromaDB with FTS5) plus heuristic question seeding for elaborative encoding. Intent, outcome, and department question templates. Reflection NOT templated into questions (grammar issues). No LLM call. 15 new tests. Completes AD-584 recall improvement wave.
BF-230 CLOSED. JetStream publish silently drops events under CPU load — no retry, no fallback to core NATS. Fix: js_publish() retries once (0.5s backoff), then falls back to core NATS publish, only ERROR if both fail. Configurable timeout (default 5s) via NatsConfig.js_publish_timeout. 6 new tests. Issue #335.
BF-106 CLOSED. DreamingEngine late-init dependencies monkey-patched in finalize.py — `_ward_room`, `_records_store`, `_get_department` set via private attrs. Fix: `records_store` forwarded through init_dreaming() constructor (available Phase 4→5). `ward_room` and `get_department` get public setter methods (genuinely unavailable until Phase 7). finalize.py now calls setters instead of reaching into private attrs. 8 new tests. Issue #19.
BF-229 CLOSED. NATS subject prefix rejects colons from ship DID — JetStream streams stranded with probos.local.* filters after Phase 7 commissioning sets prefix to probos.did:probos:<uuid>. All js_publish() calls fail with "no response from stream". Fix: NATSBus.set_subject_prefix() sanitizes NATS-unsafe chars to underscores (boundary enforcement); ensure_stream() re-raises after logging; stream update failure promoted to ERROR. 5 new tests + 1 existing test updated. Issue #334.
BF-228 CLOSED. Communications Array Degraded/Restored flapping — proactive loop transitions to "degraded" on 1 failure, resets on next success, flooding All Hands with Bridge Alerts. Three fixes: (1) raised degraded threshold from >=1 to >=3 (hysteresis, matches LLM client _UNREACHABLE_THRESHOLD), (2) only count actual LLM errors (timeout/connection/rate-limit) not generic agent failures, (3) decay failure count on success instead of hard-reset to 0. Issue #332.
AD-662 COMPLETE. Corroboration Source Provenance Validation — extend SocialVerificationService (AD-567f) with source provenance metadata. Three new AnchorFrame fields (source_origin_id, artifact_version, anomaly_window_id), ancestry-based independence veto, anomaly window discount scoring. Infrastructure-only (consumer-side); AD-663 wires producers. 13 new tests. First crew improvement proposal from this instance (Reed/Science, corroborated by Ezri/Bridge and Sentinel/Security). Issue #331.
AD-654 IN PROGRESS. Universal Agent Activation Architecture (UAAA) — event-driven activation for multi-agent systems. 5 sub-ADs: a=Async Ward Room Dispatch (JetStream publish, not request/reply), b=Agent Cognitive Queue (priority mailbox), c=TaskEvent Protocol & Dispatcher, d=Internal Event Emitters (RecreationService, WardRoom, Kanban), e=External Integration (MCP Apps, deferred). a-d COMPLETE, e deferred. Research: universal-agent-activation-research.md.
AD-654d COMPLETE. Internal Event Emitters — 4 emitters wired into TaskEvent/Dispatcher: (1) RecreationService move_required with callsign→agent_id resolution, game-over suppression; (2) delegation tags [ASSIGN] (Lieutenant+ rank gate) / [HANDOFF] (no rank gate) with self-assignment guard; (3) WorkItemStore work_item_assigned on assignment (not claim); (4) WardRoom @mention on post/thread creation with self-exclusion. Late-binding via attach_dispatcher() pattern (Law of Demeter). All emitters degrade gracefully when dispatcher=None. 25 tests.
AD-654c COMPLETE. TaskEvent Protocol & Dispatcher — universal TaskEvent dataclass (frozen, 10 fields: source_type, source_id, event_type, priority, target, payload, thread_id, deadline, id, created_at). AgentTarget with exactly-one validation (agent_id/capability/department_id/broadcast). Dispatcher routes TaskEvents to cognitive queues via target resolution; callback injection pattern (get_queue, dispatch_async_fn) avoids LoD violation. DispatcherProtocol. DispatchResult with dispatch_ms timing. 3-tier fallback: queue → dispatch_async → create_task. New package: src/probos/activation/. 26 tests. Issue #325.
AD-654b COMPLETE. Agent Cognitive Queue (Priority Mailbox) — Actor Model mailbox interposes between JetStream consumers and agent.handle_intent(). AgentCognitiveQueue with priority ordering (CRITICAL→NORMAL→LOW), overflow shedding, dequeue-time circuit breaker guard, JetStream ack semantics (ack/term/nak). handler.__self__ Law of Demeter violation replaced with injected callback. max_deliver=10 bounds nak redelivery. AgentCognitiveQueueProtocol in protocols.py. 25 tests + 29 AD-654a regression green. Issue #324.
AD-654a COMPLETE. Async Ward Room Dispatch — ward room router publishes to JetStream fire-and-forget; agents consume via durable consumer and self-post via WardRoomPostPipeline. IntentBus.dispatch_async() with fallback to direct dispatch. NATSMessage.term() + manual_ack on js_subscribe(). Per-agent consumer cleanup in unsubscribe(). 29 tests. Issue #323.
BF-222 CLOSED. Stream subjects not updated on NATS prefix change. set_subject_prefix() now updates stream configs. EventType moved to str Enum. Issue #327.
BF-223 CLOSED. JetStream durable consumer filter_subject stale after prefix change — zero agent dispatch delivery. Two fixes: (1) NATSBus.set_subject_prefix() deletes+recreates durable consumers, (2) IntentBus defers dispatch consumers until finalize.py after ship commissioning. Issue #328.
BF-224 CLOSED. Proactive context renders thread title only, drops body — agents can't read ward room posts during proactive scan. Fixed all three context builders (dept/All Hands/Recreation) to render title+body. Issue #329.
BF-225 CLOSED. Scout classification instructions stripped — duty-triggered proactive_think not renamed in observation dict. _decide_via_llm() treated it as conversation, excluded ===SCOUT_REPORT=== format. Fixed perceive() to write intent back. Issue #330.
BF-226 CLOSED. Two bugs: (1) Proposal extraction broken — BF-203 catch-all bracket strip destroys [PROPOSAL] before _extract_and_post_proposal runs. Fix: moved proposal extraction into _extract_and_execute_actions before BF-203 catch-all, deleted old method. (2) All Hands unread count never resets — update_last_seen only fires inside conditional requiring new non-inform activity. Fix: unconditional update_last_seen after channel check. Also fixed Recreation last_seen and agent_last_seen NameError. 11 tests (9 proposal, 2 last_seen).
BF-227 CLOSED. All Hands channel lookup matches any ship channel, not "All Hands". `all_hands_ch` set by `channel_type == "ship"` iteration — lands on last ship channel (Creative or Recreation), never actual All Hands. BF-226's update_last_seen fix was correct but operating on wrong channel. Fix: match by `ch.name == "All Hands"` instead of `ch.channel_type == "ship"`. 1-line fix.
AD-653 COMPLETE. Dynamic Communication Register — "speak freely" protocol (Layer 1). ANALYZE extended with speak_freely intended_action for thread_analysis + situation_review (not DM). COMPOSE trust-gates authorization: ≥0.7 auto-granted, 0.4-0.7 flagged to Counselor, <0.4 denied. Billet instruction pattern (AD-651a) injects framing. Events: REGISTER_SHIFT_GRANTED/DENIED. Counselor subscribes + dispatches. 14 tests. Layers 2-3 deferred (register taxonomy enum, Big Five defaults, analytics). Issue #303.
AD-652 DESIGN PRINCIPLE (adopted). Cognitive Code-Switching — unified pipeline with contextual modulation. One chain, not parallel pipelines. Field/tenor/mode parameters modulate behavior. Structured format overlays for institutional outputs. Variable chain depth. Character-driven self-monitoring range. Research: cognitive-code-switching-research.md. Issue #302.
AD-651 CLOSED. Standing Order Decomposition — step-specific instruction routing for cognitive chain. StepInstructionRouter slices composed standing orders by category markers; analyze/compose handlers use get_step_instructions(); startup wires router via finalize.py. Backward-compatible full-text fallback when disabled or unmarked. 20 new tests, 144 focused integration tests passing. Issue #299.
AD-651a CLOSED. Compose billet instructions — proposal format injected when analyze requests "proposal" in intended_actions, duty compose framing replaced with structured Findings/Assessment/Recommendation report, [NO_RESPONSE] removed from duty cycles. 10 tests. Issue #301.
BF-217 CLOSED. Post-batch test regressions + DRY cleanup — 3 test regressions fixed (max_tokens 1024→1536, key counts 6→7/5→6), 2 DRY extractions (AD646B_DEDICATED_KEYS frozenset in sub_tasks/__init__.py, derive_communication_context() module-level function in cognitive_agent.py), 1 dead test file deleted (test_bf194, 371 lines). 151 tests green. Issue #306.
BF-216 FIXED (direct). Ward Room thread body not scrollable — long duty reports push posts/reply off-screen. Moved thread body into scrollable content area, title/author stays fixed. Issue #305.
BF-215 FIXED (direct). Empty observation posts after tag stripping — added empty guard after BF-203/BF-174 action extraction in proactive.py. Issue #304.
BF-214 CLOSED. Scout marks repos as seen before classification — deferred seen marking from perceive to act, gated on classification success. 10 tests. Issue #300.
BF-213 CLOSED (rewritten). Chain proactive analyze suppresses structured actions — three compounding issues: (1) analyze "Silence is professionalism" bias defaults to `intended_actions: ["silent"]`, (2) compose short-circuit skips LLM call, (3) compose framing "2-4 sentences" discourages structured output. Fix: rebalance analyze framing, broaden compose framing, revert BF-213 duplicate action vocabulary. ~10 lines across 2 files, 12 tests. Issue #298.
BF-212 CLOSED. Crew-vs-crew game moves never reach opponent — added `@{next_player}` to in-progress board post body in both MOVE paths (ward_room_router.py + proactive.py). Existing @mention resolution routes to opponent regardless of channel type. 4 tests. Issue #297.
BF-211 CLOSED. Proactive loop dies silently — moved `asyncio.sleep(self._interval)` inside try/except in `_think_loop()`. Sleep in both happy path and error recovery branch. CancelledError still re-raises for clean shutdown. 2 tests. Issue #296.
BF-210 CLOSED. Ward Room DM recipient never wired — wired `author_callsign` → `_dm_recipient` in cognitive_agent.py, named peer in compose private_conversation branch, changed `_build_dm_compose_prompt()` default from "the Captain" to "a crew member". 8 tests, 34 regression green. Issue #295.
AD-650 COMPLETE. Analytical Depth Enhancement — added `analytical_reasoning` narrative prose field to composition brief (all 3 ANALYZE modes), reframed brief as "analytical reasoning and composition plan", bold-header suppression for all Ward Room branches, depth instructions ("Don't just summarize — interpret") in ward_room/DM/proactive COMPOSE prompts. 12 tests, 71 regression green. Issue #294.
AD-649 COMPLETE. Communication Context Awareness for Cognitive Chain — add channel-type register adaptation to COMPOSE, prescriptive tone guidance in ANALYZE, voice parity with one-shot. 14 tests. Issue #293.
AD-648 COMPLETE. Post Capability Profiles — per-post capability declarations + negative grounding in ontology. PostCapability dataclass, loader parsing, service methods (get_post_capabilities, get_agent_capabilities, get_post_negative_grounding), get_crew_context() extension, _build_cognitive_baseline() prompt injection. All 18 posts profiled in organization.yaml. Prevents conceptual confabulation. 14 tests. Issue #292.
AD-647 SCOPED. Process-Oriented Cognitive Chains — scout report as first case. Depends on AD-618 (Bills), AD-595 (Billets), AD-641g (NATS). Issue #291.
BF-209 CLOSED. Scout duty report chain bypass — ScoutAgent._should_activate_chain() override routes duty-triggered scout_report through decide()→act(). 4 tests. Issue #290.
BF-208 CLOSED. Scout duty report always returns 0 findings — perceive() only recognizes scout_search/scout_report intents, duty scheduler sends proactive_think. Fix: 5-line duty-trigger check in perceive(). 5 tests. Issue #286.
BF-207 CLOSED. Shutdown race condition causes episodic memory hash mismatch. Issue #282.
BF-206 CLOSED. Enforce evaluate suppress + confabulation feedback loop. Issue #281.
AD-643b COMPLETE. Skill Trigger Learning — adaptive trigger discovery + post-hoc re-reflect feedback loop. Trigger awareness in ANALYZE, post-hoc re-reflection when action → no trigger match. Phase 3 (graduation) deferred. Issue #284.
AD-643  COMPLETE. Intent-Driven Skill Activation & Thought Process Catalog — trigger registry, weighted matching, chain-aware dispatch. Issue #283.
AD-637z COMPLETE. NATS Migration Cleanup + BF-221 Lift — prefix re-subscription (NATSBus tracks _active_subs, async set_subject_prefix re-subscribes all), BF-221 lifted (IntentBus.send() restored to NATS request/reply when connected), double event subscription fix (_nats_events_wired gate), ensure_future→create_task task leak fix, duplicate ensure_stream removal. NATSBus owns full subscription lifecycle. 7 files modified, 13 new tests (88/88 NATS regression pass). Issue #321.
AD-637f COMPLETE. Priority Model Formalization — Priority(StrEnum) with CRITICAL/NORMAL/LOW + classify() single source of truth. Captain @mentions now get interactive LLM slots (behavioral change). X-Priority headers on ward room + system event JetStream publishes. LOW is observability-only label (same background semaphore as NORMAL). 5 files modified, 13 new tests. Issue #312.
AD-637  COMPLETE. NATS Event Bus — standardize all messaging on NATS. Sub-ADs: a=Foundation, b=IntentBus, c=WardRoom, d=SystemEvents, e=Federation, f=Priority, z=Cleanup — all complete. Issue #270.
AD-637d COMPLETE. System Event Migration (EventEmitter → NATS) — _emit_event() publishes to SYSTEM_EVENTS JetStream stream when NATS connected, falls back to _emit_event_local() when disconnected. add_event_listener() creates ephemeral NATS subscriptions with subject-level type filtering. Night Orders escalation preserved as synchronous local check (runs before dispatch). Bulk wiring of early-startup listeners via _setup_nats_event_subscriptions(). 2 files modified (runtime.py, finalize.py), 10 new tests. Issue #308.
AD-637c COMPLETE. Ward Room NATS JetStream Emission — fire-and-forget create_task dispatch replaced with JetStream publish→durable consumer. max_ack_pending=10 + ack_wait=120s replaces Semaphore(10). Graceful degradation to create_task fallback. No-dual-delivery enforced structurally. 5 files modified, 11 new tests. Issue #307.
AD-637b COMPLETE. IntentBus NATS Migration — send() uses NATS request/reply with direct-call fallback, NATS subscribe/unsubscribe lifecycle, publish() alias (fixes latent watch intent bug), serialization helpers, set_nats_bus() wiring, federation handler cleanup. broadcast() unchanged (deferred to AD-637c). 4 files modified, 19 tests (13 new). Issue #270.
AD-637a COMPLETE. NATS Foundation Layer — nats-py dependency, NATSBus abstraction (graceful degradation), NatsConfig, MockNATSBus, NATSBusProtocol, startup Phase 1b wiring, shutdown drain, pre-flight check. 10 files (3 new, 7 modified), 24 tests. Issue #270.
AD-637  COMPLETE. NATS Event Bus — standardize all messaging on NATS. Sub-ADs: a=Foundation, b=IntentBus, c=WardRoom, d=SystemEvents, e=Federation, f=Priority, z=Cleanup — all complete. Issue #270.
AD-646b COMPLETE. Chain Cognitive Parity — close remaining one-shot gaps. Two QUERY operations (self_monitoring, introspective_telemetry) + three baseline enhancements (cold-start, rich attribution, self-recognition) + prompt consumption (oracle, telemetry). ~150 lines across 4 files. Issue #289.
AD-646  COMPLETE. Universal Cognitive Baseline — split _build_cognitive_state() into baseline (agent-intrinsic, all chains) + extensions (context_parts-dependent). Fixes ward_room data starvation. ~100 lines across 3 files. Research doc complete. Issue #288.
AD-645  Phase 1-3 Complete. Composition briefs replace routing slips in ANALYZE→COMPOSE handoff. COMPOSE gets structured brief + 7 SA keys. 15 tests. Issue #287.
AD-644  Phase 1-4 Complete. 23/23 parity. Chain path has full perceptual equivalence with _build_user_message(). Phase 5 (deprecation) design.

BF-203 CLOSED (Strip hallucinated bracket command tags from agent output. Issue #278) | BF-201 CLOSED (Simplify thread caps — remove per-agent & dept gate, add 50-post cap. Issue #276) | BF-106 OPEN (DreamingEngine late-init dependencies monkey-patched in finalize.py. Issue #19) | BF-200 CLOSED (Thread Cap Awareness & DM Cap Raise — system notification on cap hit, DM limit 5→40, DM bypasses reply cap. Issue #275) | BF-199 CLOSED (Ward Room JSON Leak Guard — sanitize chain JSON at posting boundary, shared helper for proactive+router. Issue #274) | AD-640 COMPLETE (Tiered Trust Initialization — role-based trust priors: Bridge 0.82, Chiefs 0.75, Crew 0.50. 6 academic domains validate. Issue #273) | AD-638 COMPLETE (Cold-Start Boot Camp — 4-phase onboarding, quality gate relaxation, graduation criteria. 1 new + 8 modified, 35 tests. Issue #272) | AD-639 COMPLETE (Chain Personality Tuning — trust-band adaptive chain: low trust skips evaluate/reflect, mid trust injects personality block, high trust unchanged. 4 modified + 1 test file, 38 tests. Issue #272) | BF-204 CLOSED (Evaluate grounding criterion — reorder defense chain safety>obligation, deterministic hex ID pre-check, Grounding criterion in all 3 eval prompts, reflect suppress above social obligation. 2 modified + 1 test file, 24 tests. Issue #280) | ## Current Status: BF-193 CLOSED (Parallel Captain dispatch — 3-phase _route_to_agents: pre-filter, concurrent dispatch via asyncio.gather for Captain, sequential result processing. 1 source file, 7 tests. Issue #264) | BF-190/191 CLOSED (Route now NameError + Evaluate raw JSON pass-through — removed dead now variable, cleaned up import time as _time, added deterministic JSON rejection in evaluate before LLM judge. 2 source files, 8 tests. Issue #261) | BF-189 CLOSED (Chain pipeline memory context gaps — pre-format memories via _format_memory_section() in chain context, fix thread_analysis raw list repr, add memory to dm_comprehension/situation_review/compose. 3 source files, 12 tests. Issue #257) | BF-187/188 CLOSED (DM social obligation + Captain delivery coordination — _is_dm flag at all 4 chain gates, asyncio.Event for Captain delivery, reflect suppress reorder. 5 source files, 18 tests. Issue #255) | BF-186 CLOSED (Compose standing orders regression — removed 80+ lines hardcoded action vocab, inject rank/skill_profile/crew_manifest into chain context, social obligation bypass at compose layer, analyze enriched with standing orders. 4 source files + 1 config, 20 tests. Issue #251) | BF-185 CLOSED (Reflect social bypass — Captain/@mention skip self-critique, 0 tokens. 1 file, 8 tests. Issue #250) | BF-184 CLOSED (Evaluate social bypass — Captain/@mention auto-approve, 0 LLM tokens. 2 files, 11 tests. Issue #248) | BF-183 CLOSED (Budget-aware retry in SubTaskExecutor. 13 tests. Issue #246) | BF-184 CLOSED (Evaluate social bypass — Captain/@mention auto-approve, 0 LLM tokens. 2 files, 11 tests. Issue #248) | BF-183 CLOSED (Sub-Task Chain Budget-Aware Retry — timeout step checks remaining chain time budget, retries once with 2s backoff if sufficient, falls back immediately if not. 1 file, 13 tests. Issue #246) | AD-636 COMPLETE | BF-179/180/181/182 CLOSED (AD-632 runtime activation bugs — four fixes enabling sub-task chains in production. BF-179: finalize.py used getattr(agent, 'is_crew', False) which always returned False — executor never wired onto crew agents. Fix: is_crew_agent(). BF-180: duplicate augmentation skill loading — AD-632f added _load_augmentation_skills() in decide() but _decide_via_llm() already called it. Fix: guard with observation key check. BF-181: asyncio not defined in chain error handler — _execute_sub_task_chain() except block referenced asyncio.TimeoutError without import. Fix: local import. BF-182: query handler missing agent_id/thread_id — context filtering stripped _-prefixed identity keys and QUERY steps got filtered context. Fix: pass _-prefixed keys through filter, bypass filtering for QUERY steps, _ctx() helper resolves from nested params dict. 3 source files (finalize.py, cognitive_agent.py, sub_task.py), 1 handler file (query.py). Issue #243) | AD-633 SCOPED (Predictive Cognitive Branching — umbrella AD enabling crew agents to engage in forward-looking thought during idle cycles. Three functions: (1) Pre-Computation (CPU branch prediction analog — speculate on likely sub-tasks, serve pre-computed results), (2) Anticipatory Reasoning (Prospection/DMN analog — forward-looking thought producing new awareness, skill gap identification, PROPOSAL generation), (3) Goal Origination (Constructive Episodic Simulation/Preplay analog — agents generate novel goals from observation). Four speculation tiers: Zero-Cost deterministic, Cheap fast-tier >70%, Standard >85%, Anticipatory self-originated idle. Separate speculative token budget (not competing with operational). Prediction accuracy feedback loop (flush rate >30% = budget reduction). Earned Agency gates budgets by trust tier. Preplay dream Step 13 (forward simulation + implementation intentions). Prediction errors feed AD-557 EmergenceMetrics as novelty signal. Repeated predictions compile into Cognitive JIT procedures. Nine sub-ADs: 633a prediction engine, 633b executor+cache, 633c budget management, 633d pipeline integration, 633e accuracy tracking, 633f anticipatory reasoning, 633g preplay dream step, 633h emergence signal, 633i Cognitive JIT compilation. Research: docs/research/predictive-cognitive-branching.md (14 sources: Seligman, Schacter, DMN, Dragoi, Friston, Gollwitzer, Ha/Schmidhuber). Depends: AD-632, AD-531-539, AD-557, AD-573, AD-357. Issue #228) | AD-632 COMPLETE (Cognitive Sub-Task Protocol — umbrella AD enabling crew agents to decompose complex reasoning into focused sub-steps. Three-level cognitive escalation: Level 1 Cognitive JIT replay (0 calls), Level 2 single-call reasoning (1 call, current), Level 3 sub-task protocol (2-4 focused calls). Five sub-task types: Query (deterministic), Analyze (LLM narrow), Compose (LLM with skill), Evaluate (criteria), Reflect (self-critic). Selective activation via skill annotation, complexity heuristic, or quality fallback — not constant cost. Eight sub-ADs: 632a foundation, 632b query, 632c analyze, 632d compose, 632e evaluate/reflect, 632f triggers, 632g Cognitive JIT integration, 632h parallel dispatch. SOAR impasse-driven subgoaling + chunking model. Sub-tasks are intra-agent only — no identity, no trust, no episodic memory. Research: docs/research/cognitive-sub-task-protocol.md (13 sources: SOAR, ACT-R, ReAct, DECOMP, Inner Monologue, ToT, Reflexion, LATS, MRKL, Claude Code, Kahneman). Depends: AD-631, AD-625, AD-626, AD-596a-e, AD-531-539. Issue #227) | AD-632a COMPLETE (Cognitive Sub-Task Protocol Foundation — infrastructure layer for Level 3 cognitive escalation enabling intra-agent sub-task decomposition. SubTaskType 5-value str enum (QUERY/ANALYZE/COMPOSE/EVALUATE/REFLECT), SubTaskSpec/SubTaskResult frozen dataclasses, SubTaskChain mutable chain container, SubTaskHandler @runtime_checkable Protocol with async __call__(spec, context, prior_results), SubTaskExecutor Open/Closed handler registry with sequential execution + per-step and chain-level timeouts via asyncio.wait_for() + journal recording with structured dag_node_id (st:{chain_id}:{step_index}:{type}) populating dead AD-432 column + SUB_TASK_CHAIN_COMPLETED event emission. SubTaskError/SubTaskChainError/SubTaskStepError exception hierarchy. CognitiveAgent integration: _sub_task_executor + _pending_sub_task_chain attributes, set_sub_task_executor() setter, _execute_sub_task_chain() method, decide() integration between Level 1 (procedural memory) and Level 2 (single-call LLM) with consume-once semantics and single-call fallback on failure. SubTaskConfig (enabled=False, chain_timeout_ms=30000, step_timeout_ms=15000, max_chain_steps=6) in SystemConfig. Six invariants enforced: token attribution to parent agent_id, no episodic memory storage, no trust attribution, no circuit breaker events, journal recording with dag_node_id, no nesting (max depth 1). QUERY steps excluded from journal (0 LLM calls). Foundation only — no handlers (632b-e), no triggers (632f), no JIT integration (632g), no parallelism (632h). 1 new file (cognitive/sub_task.py), 3 modified files (cognitive_agent.py, config.py, events.py). 41 tests across 9 classes. Build prompt: prompts/ad-632a-sub-task-foundation.md. Depends: AD-432, AD-534, AD-504. Issue #230) | AD-632e COMPLETE (Evaluate & Reflect Sub-Task Handlers — quality gates on Compose output completing 5-step chain Q→A→C→E→R. EvaluateHandler criteria-based scoring (3 modes: ward_room_quality absorbing AD-631 Pre-Submit Check, proactive_quality, notebook_quality absorbing AD-634 Pre-Write Verification Gate). Structured verdict: pass/fail, score, per-criterion, recommendation (approve/revise/suppress). ReflectHandler self-critique and revision (3 modes: ward_room_reflection, proactive_reflection, general_reflection). Suppress short-circuit when Evaluate recommends suppress — skips LLM, returns [NO_RESPONSE] with tokens_used=0. Skill instruction injection from _augmentation_skill_instructions into Reflect system prompt. Decision extractor prefers REFLECT > COMPOSE > concatenated fallback. Chain expansion 3→5 steps in _build_chain_for_intent(), both EVALUATE and REFLECT steps with required=False for graceful degradation. Fail-open design: Evaluate parse failure → pass-by-default, Reflect LLM failure → returns original Compose output unchanged. extract_json() raises ValueError not returns None — wrapped in try/except. Reflexion (Shinn et al., NeurIPS 2023). 2 new files (sub_tasks/evaluate.py, sub_tasks/reflect.py), 3 modified files (sub_tasks/__init__.py, startup/finalize.py, cognitive_agent.py), 1 new test file (57 tests across 12 classes), 2 AD-632f test assertions updated (3→5 step counts). Build prompt: prompts/ad-632e-evaluate-reflect-handlers.md. Depends: AD-632a, AD-632d. Issue #240) | AD-632h COMPLETE (Parallel Sub-Task Dispatch — wave-based concurrent execution of independent sub-task steps within chains. Adds depends_on: tuple[str, ...] to SubTaskSpec for explicit dependency declaration. Steps with satisfied dependencies dispatch concurrently via asyncio.gather(return_exceptions=True). Current chains gain EVALUATE‖REFLECT parallelism (both depend only on COMPOSE output). validate_chain() catches invalid references, cycles, self-references (fail-open). _get_ready_steps() computes per-wave readiness with backward-compatible sequential default. _execute_single_step() extracted for reuse. Follows Transporter/TaskDAG wave pattern. Closes AD-632 umbrella — all 8/8 sub-ADs complete. 2 modified files (sub_task.py, cognitive_agent.py), 1 new test file (34 tests across 7 classes). Build prompt: prompts/ad-632h-parallel-dispatch.md. Depends: AD-632a, AD-632e, AD-632f, AD-617. Issue #243) | AD-632g COMPLETE (Cognitive JIT Integration — Chain Pattern Learning. Closes learning loop between Level 3 sub-task chains (2-4 LLM calls) and Level 1 procedural replay (0 LLM calls). SOAR chunking analog. Chain-dominant episode clusters (>60% threshold) use deterministic extract_chain_procedure() (0 LLM calls) instead of LLM-based extraction. Chain procedures start at Dreyfus Level 2 (Guided) — higher than episode-derived (Level 1). learned_via="chain_compiled" (fourth value alongside direct/observational/taught). Decision dict gains chain_source + chain_steps metadata, propagated through observation._chain_metadata → episode outcomes[0]. Dream Step 7 chain-aware pre-check before LLM extraction. DreamReport gains chain_procedures_extracted counter. 4 modified files (procedures.py, cognitive_agent.py, dreaming.py, types.py), 1 new test file (29 tests across 6 classes). Build prompt: prompts/ad-632g-cognitive-jit-integration.md. Depends: AD-632a, AD-632f, AD-531-539. Issue #242) | BF-178 CLOSED (Consolidation anomaly false positives during micro-stasis recovery — stale dream history causes false consolidation_anomaly events after short stasis. Cold-start suppression only fires on true cold starts (empty trust+memory). Fix: clear_dream_history() on EmergentDetector + stasis recovery branch in startup/dreaming.py (60s suppression). Lineage: BF-166→BF-175→BF-178. 2 source files, 6 tests. Issue #241) | AD-632f COMPLETE (Sub-Task Chain Activation Triggers — inline chain construction in decide() with intent-type trigger heuristics. _should_activate_chain() three-gate evaluation: global enable check via SubTaskExecutor.enabled property (Law of Demeter), intent type filter (_CHAIN_ELIGIBLE_INTENTS: ward_room_notification + proactive_think), executor readiness. _build_chain_for_intent() constructs SubTaskChain per intent: WR chain (QUERY thread_metadata+credibility → ANALYZE thread_analysis → COMPOSE ward_room_response), proactive chain (QUERY unread_counts+trust_score → ANALYZE situation_review → COMPOSE proactive_observation). Skill preloading before chain check ensures _augmentation_skill_instructions available for Compose handler. SubTaskConfig.enabled flipped True→default. External _pending_sub_task_chain preserved as escape hatch. Config: sub_task section in system.yaml. 4 modified files (cognitive_agent.py, sub_task.py, config.py, system.yaml), 1 new test file (34 tests across 8 classes). Build prompt: prompts/ad-632f-activation-triggers.md. Depends: AD-632a, AD-632b, AD-632c, AD-632d. Issue #238) | AD-632d COMPLETE (Compose Sub-Task Handler — skill-augmented response composition for Level 3 cognitive escalation. Final LLM call in MVP chain (Query→Analyze→Compose). ComposeHandler implementing SubTaskHandler protocol. Three composition modes via Open/Closed dispatch: ward_room_response (WR posting rules + ENDORSE/NO_RESPONSE), dm_response (conversational 1:1, no action tags), proactive_observation (full action vocabulary: REPLY/ENDORSE/NOTEBOOK/PROPOSAL/DM/CHALLENGE/MOVE). Full system prompt via compose_instructions() with standing orders + personality (unlike Analyze's narrow identity). Skill instruction injection via XML <active_skill> framing with proficiency tier support. SILENT short-circuit: skips LLM call entirely when prior Analyze has contribution_assessment=SILENT or should_respond=false, returns [NO_RESPONSE] with tokens_used=0. LLM params: temperature=0.3, max_tokens=2048, tier=spec.tier. Result: {"output": llm_text} consumed by _execute_sub_task_chain() at cognitive_agent.py:1488. Three-tier error handling: LLM unavailable, LLM exception, empty response. Does NOT flip SubTaskConfig.enabled (follow-up AD). 1 new file (sub_tasks/compose.py), 2 modified files (sub_tasks/__init__.py, startup/finalize.py), 1 new test file (40 tests across 11 classes). Build prompt: prompts/ad-632d-compose-handler.md. Depends: AD-632a, AD-632c. Issue #236) | BF-177 CLOSED (Scout reports stopped — BF-024 proactive_think guard in ScoutAgent.act() blocks duty-triggered report generation. duty dict not propagated from observation to decision dict. Fix: propagate duty to decision in cognitive_agent.py, exempt duty-triggered proactive_think from guard in scout.py. 2 source files. Issue #234) | AD-632b COMPLETE (Query Sub-Task Handler — deterministic data retrieval handler for Level 3 cognitive escalation. First concrete SubTaskHandler implementation for SubTaskType.QUERY with zero LLM calls. Multiplexer pattern: spec.context_keys determines which service queries to dispatch via Open/Closed _QUERY_OPERATIONS dispatch table. 9 query operations: thread_metadata, thread_activity, comm_stats, credibility, unread_counts, unread_dms, trust_score, trust_summary, posts_by_author. Constructor-injected runtime reference (DIP). Smart context_keys separation: operation keys dispatched to query functions, data keys passed through for executor context filtering. Partial failure support: multi-key queries return merged results with per-key error reporting. _ServiceUnavailableError internal exception for defensive service access. WardRoomCredibility dataclass→dict conversion via asdict(). TrustNetwork sync methods called without await. Always tokens_used=0, tier_used="". Does NOT flip SubTaskConfig.enabled (needs Analyze/Compose handlers AD-632c/d). Startup wiring in finalize.py: SubTaskExecutor created with QueryHandler registered, wired onto all crew agents via set_sub_task_executor(). 2 new files (cognitive/sub_tasks/__init__.py, cognitive/sub_tasks/query.py), 1 modified file (startup/finalize.py), 1 new test file (31 tests across 11 classes). Build prompt: prompts/ad-632b-query-handler.md. Depends: AD-632a. Issue #232) | AD-632c COMPLETE (Analyze Sub-Task Handler — focused LLM comprehension handler for Level 3 cognitive escalation. First LLM-calling SubTaskHandler implementation for SubTaskType.ANALYZE with exactly one LLM call per invocation. Three analysis modes via Open/Closed dispatch table: thread_analysis (Ward Room thread comprehension — topics_covered, novel_posts, gaps, endorsement_candidates, contribution_assessment), situation_review (proactive think cycle — active_threads, pending_actions, priority_topics, department_relevance), dm_comprehension (DM understanding — sender_intent, key_questions, required_actions, emotional_tone). Mode selected by spec.prompt_template with thread_analysis as default fallback. Narrow system prompt: agent identity (callsign/department) for perspective, NO standing orders, NO skill instructions, NO action vocabulary. LLM call via BaseLLMClient.complete() with tier=spec.tier, temperature=0.0, max_tokens=1024. Response parsed via extract_json() utility. Prior QUERY results incorporated as factual context in user prompt. Agent identity injection: _execute_sub_task_chain() in cognitive_agent.py now injects _agent_id, _agent_type, _callsign, _department into observation dict for all handlers (Open/Closed benefit). Three-tier error handling: LLM unavailable, LLM exception, JSON parse failure — all return SubTaskResult(success=False) without exceptions. Error content truncated to 200 chars. Token accounting from LLMResponse.tokens_used. Does NOT flip SubTaskConfig.enabled (needs Compose handler AD-632d). 1 new file (cognitive/sub_tasks/analyze.py), 3 modified files (sub_tasks/__init__.py, cognitive_agent.py, startup/finalize.py), 1 new test file (39 tests across 11 classes). Build prompt: prompts/ad-632c-analyze-handler.md. Depends: AD-632a, AD-632b. Issue #233) | AD-635 SCOPED (Medical Diagnostic Data Access — Clinical Telemetry Query Interface. Clearance-gated read-only query service enabling Medical (Chapel, FULL clearance) and Counselor (Echo, ORACLE clearance) to perform cross-agent clinical diagnostics. Four data domains: dream cycle history (EmergentDetector._dream_history), consolidation anomaly audit trail, cross-agent cognitive journal queries, circuit breaker state history. ClinicalTelemetryService query facade, proactive context injection for clinical agents, shell command (/clinical or /medbay), REST API endpoints gated by clearance. Access gates on billet clearance FULL+ AND clinical role. AD-588 extended from self-query to cross-agent clinical query. Origin: crew proposal from Chapel — proposal rejected but diagnostic access gap is genuine. Depends: AD-588, AD-620/621/622. Issue #231) | AD-634 COMPLETE (Notebook Analytical Quality Skill — config-only augmentation skill teaching analytical quality for notebook entries. One new file: config/skills/notebook-quality/SKILL.md. No code changes. Teaches: Analytical Purpose Gate, Finding-First Structure (Minto Pyramid), Temporal Threading, Data vs Analysis, Ward Room Differentiation, Anti-Patterns, Pre-Write Verification Gate. Co-activates with communication-discipline on proactive_think intent. 7-level Dreyfus proficiency (FOLLOW→SHAPE). probos-min-rank: ensign (all crew). 21 tests across 4 classes. Depends: AD-631, AD-625, AD-626, AD-596a-e. Issue #229) | AD-631 COMPLETE (Skill Effectiveness Improvements — eight structural fixes for why crew agents ignore communication-discipline skill. (1) DRY deduplication: removed Theory of Mind, Communication Etiquette, Reply Quality Standard from federation.md — absorbed into SKILL.md as single source of truth; federation.md retains channel descriptions + format mechanics only. (2) XML tag injection framing: `_frame_task_with_skill()` rewritten from plain-text `---` delimiters to `<active_skill>`, `<skill_instructions>`, `<proficiency_tier>`, `<skill_context>` XML tags per Anthropic prompt engineering guidance. (3) Self-verification gate: Pre-Submit Check section in SKILL.md — verify novelty, opening sentence structure, endorsement criteria before finalizing. (4) Positive framing with reasoning: Safety Rules→Contribution Standard rewrite using "do X because Y" instead of "Never X"; zero "Never"/"Do not" sentence starters. (5) Tier 7 XML: `<available_skills>`/`<skill>` tags replace markdown bullets in compose_instructions() system prompt. (6) Proficiency consolidation: removed standalone `_get_comm_proficiency_guidance()` injection from `_decide_via_llm()`, flow through `<proficiency_tier>` in augmentation skill frame; 4 injection points→2. (7) Anti-pattern addressing: operating sequence step 7 detects "Looking at..."/"I notice..."/"I can confirm..." openers with deletion instruction. (8) Self-monitoring XML (BF-174 root cause): bracket markers `[COGNITIVE ZONE:]`/`--- Recent Activity ---` replaced with `<cognitive_zone>`/`<recent_activity>`/`<notebook>`/`<source_awareness>` XML tags; `_strip_bracket_markers()` retained as defense-in-depth. Files: federation.md, SKILL.md, cognitive_agent.py, standing_orders.py. Tests: 23 new (test_ad631_skill_effectiveness.py) + 7 regression fixes in AD-625/626 tests. Absorbs: BF-174 root cause. Issue #226) | AD-630 COMPLETE (Leadership Developmental Feedback — Department Chiefs observe subordinate communication patterns and provide developmental mentoring via DMs. Five components: (1) Per-agent communication stats on WardRoomService layer — count_all_posts_by_author() cross-thread aggregation on ThreadManager, count_endorsements_by_voter()/count_endorsements_for_author() on MessageStore, get_agent_comm_stats() facade returning posts_total/endorsements_given/endorsements_received/credibility_score. (2) Ontology reverse lookup — get_agents_for_post() on DepartmentService, get_subordinate_agent_types() on VesselOntologyService using Post.authority_over chain. (3) Chief proactive context injection — _gather_context() conditionally injects subordinate_stats dict for agents with authority_over in ontology, minimum 3-post observation threshold, windowed since session start. (4) XML rendering — <subordinate_activity> tags in _build_user_message() proactive_think branch, one line per subordinate with all 4 stats. (5) Leadership-feedback augmentation skill — SKILL.md with pattern recognition (corrective: zero endorsements + many posts, high posts + low credibility; reinforcing: endorsements > posts, high credibility + moderate posts), DM feedback structure (observation/impact/guidance), frequency limits, pre-send verification, 7-level Dreyfus proficiency. Federation standing orders updated with Leadership and Mentorship section. Feedback flows through existing DM extraction/delivery/episodic memory/dream consolidation pipeline. Navy CPO mentorship model. 7 modified files (threads.py, messages.py, service.py, departments.py, ontology/service.py, proactive.py, cognitive_agent.py), 2 new files (SKILL.md, federation.md section), 1 new test file (28 tests across 8 classes). Issue #225) | AD-629 COMPLETE (Ward Room Reply Gate Enforcement + Post ID Context — four structural fixes for Ward Room echo-chamber pile-on. (1) Unified reply cap: `check_and_increment_reply_cap()` single enforcement point on WardRoomRouter, called by both `route_event()` and proactive `_extract_and_execute_replies()`. (2) @mention cap enforcement: `is_direct_target` bypass limited to cooldown timing only, NOT per-thread cap — @mentioned agents still capped. (3) Per-department reply gate: max one reply per department per thread via ontology department lookup, first-responder-wins (no `is_chief` field on Post). `_dept_thread_responses` dict tracks departments, cleaned up alongside `_agent_thread_responses`. (4) Post IDs in thread context: `[{id[:8]}] callsign: body` format enables `[ENDORSE post_id UP/DOWN]` (previously impossible — IDs never shown to agents). Post IDs also prefixed in proactive Ward Room activity body field across dept/All Hands/Recreation channels. Inline cap check at route_event() replaced with unified method call. Old increment block removed — counter incremented atomically inside `check_and_increment_reply_cap()`. 2 source files (ward_room_router.py, proactive.py), 1 new test file (test_ad629_reply_gate.py, 14 tests). Build prompt: prompts/ad-629-ward-room-reply-gate.md. Issue #224) | AD-628 SCOPED (Crew Skill Readiness Monitoring + Training Officer Role — Medical + Counselor skill performance monitoring with structured skill telemetry events. New Training Officer (TRAINO) crew agent for qualification tracking, training coordination, new agent onboarding, and Holodeck simulation management. Naval model: PQS + COSC + IMR + Training Team + CDB + C-Ratings. Seven sub-ADs: 628a skill telemetry events, 628b agent skill readiness profile, 628c TRAINO agent + standing orders, 628d Holodeck integration, 628e onboarding coordination, 628f readiness reporting, 628g LIMDU protocol. Depends: AD-596a–e, AD-625/626/627, AD-535, AD-506a, AD-566a–f, AD-477, AD-486, AD-539b) | AD-627 COMPLETE (Communication Skill Research Enrichment — content-only update to SKILL.md incorporating 630 lines of framework research into actionable agent instructions. 8 frameworks synthesized: Shannon information delta, Minto Pyramid answer-first, Canale & Swain communicative act typing, Robert's Rules consent-by-silence, Dreyfus 7-level proficiency mapping, ACH diagnosticity, Delphi independent analysis, military net discipline. Expanded from 52-line basic checklist to structured 5-gate pre-composition protocol with action selection matrix, thread discipline section, 10 anti-patterns with explanations, and full proficiency progression table. No code changes — config/content only. Research source: docs/research/communication-discipline-skill-research.md) | AD-626 COMPLETE (Dual-Mode Skill Activation — Discovery + Augmentation. `probos-activation` YAML frontmatter field on SKILL.md: "discovery" (default, backward-compatible), "augmentation" (supplementary instructions for handled intents), or "both". `find_augmentation_skills()` on CognitiveSkillCatalog — query method with department/rank filtering. `_load_augmentation_skills()` on CognitiveAgent — loads matching augmentation/both skills as "## Skill Guidance:" sections in _decide_via_llm() after compose_instructions(). Proficiency-gated via check_proficiency_gate(). Exercise recording for augmentation skills (fire-and-forget asyncio.create_task). `find_by_intent()` updated to exclude augmentation-only skills from discovery path. Intent parser enhanced: handles both comma-separated and space-separated probos-intents. Communication-discipline SKILL.md updated: `probos-activation: augmentation`, `ward_room_notification` added to intents. 2 modified source files (skill_catalog.py, cognitive_agent.py), 1 modified config (SKILL.md), 1 new test file (test_ad626_skill_activation.py, 46 tests). Build prompt: prompts/ad-626-dual-mode-skill-activation.md) | AD-625 COMPLETE (Communication Discipline Skill — Tier 2 cognitive skill teaching agents Ward Room reply self-evaluation with proficiency-gated system gate modulation. CommTier enum (NOVICE/COMPETENT/PROFICIENT/EXPERT) mapped from ProficiencyLevel via 4-band mapping. Gate overrides: max_responses_per_thread (1→3→4→5) and reply_cooldown_seconds (180→120→90→60) scale with proficiency. Exercise recording: every successful Ward Room post triggers record_exercise(agent_id, "communication"). Cognitive checklist in SKILL.md: Thread Awareness, Novelty Test, Action Selection, Brevity Check, Anti-Patterns. Prompt guidance injection via _get_comm_proficiency_guidance() into proactive_think composed instructions. Profile caching: runtime._comm_profiles dict pre-populated at startup from SkillService. get_descriptions() 3-tuple change: (name, description, skill_id) for proficiency label display in standing orders Tier 7. format_proficiency_label() renders tier tag for compose_instructions(). Reuses existing "communication" PCC (skill_id), not a new skill. Config overlay pattern (SOLID-O): read config then apply proficiency overrides at decision time, no default mutation. 1 new file (comm_proficiency.py), 7 modified files (SKILL.md, skill_catalog.py, standing_orders.py, cognitive_agent.py, ward_room_router.py, proactive.py, finalize.py), 1 new test file (test_ad625_comm_discipline.py, 52 tests). Build prompt: prompts/ad-625-communication-discipline-skill.md. Issue #219) | BF-176 CLOSED (TC_N always 0.000 despite Ward Room collaboration — compute_tc_n() only read REL_INTENT Hebbian weights but Ward Room records REL_SOCIAL weights. Fix: expanded to include both intent routing (REL_INTENT) and social cross-pool collaboration (REL_SOCIAL). Combined formula: (intent_multi + social_multi) / (intent_total + social_total). 1 source file (emergent_detector.py), 3 new tests) | AD-596e COMPLETE (Skill Validation + Instruction Linting — three-layer validation pipeline for cognitive skills. Layer 1: AgentSkills.io structural validation — native implementation of 5 spec rules (name format regex, name length ≤64, name-directory match, description length ≤1024, compatibility length ≤500). Layer 2: ProbOS metadata cross-references — department against _AGENT_DEPARTMENTS, rank against _RANK_ORDER, min_proficiency 1-5 range, skill_id against SkillRegistry (warning not error). Layer 3: Callsign linting (BF-146 class) — word-boundary regex case-insensitive scan of instruction body for hardcoded agent callsigns from CallsignRegistry (warnings). SkillValidationResult dataclass (skill_name, valid, errors, warnings, to_dict()). validation_context dict injected by caller — catalog never imports runtime/CallsignRegistry/SkillRegistry (Law of Demeter). validate_skill() and validate_all() on CognitiveSkillCatalog. enrich_skill() gains optional validation_context for post-enrichment advisory validation. _build_validation_context() helper in routers/skills.py + commands_skill.py. Two REST endpoints: GET /catalog/{name}/validate, GET /catalog/validate. /skill validate [name] shell subcommand with Rich table output. Parser vs Validator separation: parse_skill_file() permissive (Postel's Law), validate_skill() strict. 3 existing files modified (skill_catalog.py, routers/skills.py, commands_skill.py), 1 new test file (35 tests). Build prompt: prompts/ad-596e-skill-validation-linting.md. Issue #166) | AD-596d COMPLETE (External Skill Import — AgentSkills.io-compatible external skill consumption. Four new methods on CognitiveSkillCatalog: import_skill() validates SKILL.md + copies into config/skills/ + overrides origin="external" + registers; discover_package_skills() walks site-packages for .agents/skills/*/SKILL.md convention (discovery-only, no auto-import, respects Captain's authority); enrich_skill() updates ProbOS metadata (department, skill_id, min_proficiency, min_rank, intents) + rewrites SKILL.md frontmatter preserving body and non-ProbOS fields; remove_skill() guards origin=="external" + deletes from cache + SQLite + filesystem. Three REST endpoints: POST /catalog/import, GET /catalog/discover, PUT /catalog/{name}/enrich. /skill shell command with 6 subcommands (list, discover, import, info, enrich, remove) — flag-based enrichment (--dept, --skill-id, --intents, --min-prof, --min-rank). Origin tracking: parse_skill_file() defaults "internal", import_skill() overrides to "external" (fixes D2 latent bug). 1 new file (commands_skill.py), 3 existing files modified (skill_catalog.py, routers/skills.py, shell.py), 1 new test file (23 tests). Build prompt: prompts/ad-596d-external-skill-import.md. Issue #166) | AD-596c COMPLETE (Skill-Registry Bridge — Stateless coordinator connecting CognitiveSkillCatalog (T2 instruction-defined skills) and SkillRegistry/AgentSkillService (T3 proficiency-tracked skills). SkillBridge class with 4 methods: validate_and_sync() startup validation, check_proficiency_gate() trust-gated skill access, record_skill_exercise() fire-and-forget exercise tracking with auto-acquire at FOLLOW, resolve_skill_for_gap() enhanced intent→skill mapping (replaces Law of Demeter violation on registry._skills). CognitiveAgent proficiency gate: silent self-deselect when agent lacks proficiency for governed skill. Exercise recording via asyncio.create_task() after faithfulness blocks. Gap predictor enhanced with optional skill_bridge parameter — public API path + legacy fallback. Agent onboarding wires _skill_bridge + cached SkillProfile on crew agents. Procedure dataclass gains source_skill_id for T2→T3 provenance chain. ProcedureStep.to_dict() serialization fix for required_tools field. BF-596b ordering fix: set_skill_catalog() relocated from Phase 6→Phase 7 (was always no-op due to None cognitive_skill_catalog). Startup wiring in runtime.py + finalize.py, shutdown cleanup in shutdown.py. 1 new file (skill_bridge.py), 8 existing files modified, 1 new test file (24 tests), 1 existing test file updated. Build prompt: prompts/ad-596c-skill-registry-bridge-build.md. Issue #166) | BF-175 CLOSED (Consolidation Anomaly False Positives — Absolute Floor Thresholds. 604 false positive consolidation_anomaly events over 8 days (563 "unusual trust adjustments" + 41 "unusual pruning"). Root cause: BF-166's 2x historical average threshold has no absolute floor — when baseline avg=4, a count of 9 triggers anomaly despite being normal for a 55-agent crew. Fix: added configurable minimum absolute floor thresholds (dream_anomaly_min_strengthened=10, dream_anomaly_min_pruned=5, dream_anomaly_min_trust_adj=10) as AND conditions alongside existing 2x multiplier check. Crew-identified by Vega → Meridian → Captain. 4 source files modified (config.py, emergent_detector.py, dreaming.py, test_emergent_detector.py), 5 new tests. Relates to: BF-166, BF-126, BF-165) | AD-596b COMPLETE (Intent Discovery + compose_instructions() Integration — Wires cognitive skill catalog into all four cognitive pathways. Tier 7 skill descriptions in `compose_instructions()` via `get_descriptions(department, min_rank)` — progressive disclosure (~100 tokens). `handle_intent()` cognitive skill fallback on unmatched intents via `find_by_intent()` + full SKILL.md instruction injection. `_decide_via_llm()` skill catalog injection. `_gather_context()` Section 7 cognitive skills in proactive loop. Agent onboarding `wire_agent()` wires `_cognitive_skill_catalog` + IntentBus subscription. `set_skill_catalog()` / `_skill_catalog` module-level setter in `standing_orders.py` (matches `set_directive_store()` pattern). Startup wiring in `runtime.py`, shutdown cleanup in `shutdown.py`. Bug fix: removed undefined `runtime` reference in `structural_services.py` — relocated `set_skill_catalog()` to `runtime.py`. 8 source files modified (standing_orders.py, cognitive_agent.py, proactive.py, agent_onboarding.py, finalize.py, structural_services.py, runtime.py, shutdown.py), 1 new test file (21 tests across 6 classes). Build prompt: `prompts/ad-596b-intent-discovery-integration.md`. Issue #166) | AD-596a COMPLETE (Cognitive Skill File Format + Loader — AgentSkills.io `SKILL.md` format adoption with ProbOS metadata extensions. `parse_skill_file()` extracts YAML frontmatter (name, description, license, compatibility + ProbOS metadata: department, skill_id, min_proficiency, min_rank, intents). `get_skill_body()` returns markdown content below frontmatter. `CognitiveSkillCatalog` Ship's Computer service: `start()`/`stop()` lifecycle, `scan_and_register()` discovers `config/skills/*/SKILL.md`, `register()` persists to SQLite + cache, `get_entry()`/`list_entries(department, min_rank)` with department scoping + rank filtering via `_RANK_ORDER`, `get_descriptions()` progressive disclosure (name + description tuples), `get_instructions()` on-demand body loading, `get_intents()`/`find_by_intent()` reverse lookup. `CognitiveSkillEntry` dataclass with origin tracking (internal/external). ConnectionFactory pattern matching SkillRegistry. REST API: `GET /catalog` (filtered list), `GET /catalog/{name}` (full entry + instructions), `POST /catalog/rescan` (re-scan trigger). Startup wiring in `communication.py`, shutdown in `shutdown.py`, runtime attribute. Example skill: `config/skills/communication-discipline/SKILL.md` (placeholder for AD-625). 1 new module (`cognitive/skill_catalog.py`), 1 new skill dir, 5 existing files modified (results.py, communication.py, shutdown.py, runtime.py, routers/skills.py), 1 new test file (27 tests across 5 classes). Build prompt: `prompts/ad-596a-cognitive-skill-loader.md`. Issue #166) | AD-423c COMPLETE (ToolContext + Role-Based Tool Assignment — ToolContext scoped permission-filtered view of ToolRegistry per agent (dataclass with agent identity snapshot: agent_id, agent_rank, agent_department, agent_types). Wraps shared ToolRegistry, delegates permission checks via resolve_permission(). Methods: available_tools() (permission-filtered, optional tool_type filter), has_tool(), invoke() (delegates to check_and_invoke() with merged agent context), get_permission(), refresh() (identity re-snapshot on rank/department change), to_dict(). Constructed at onboarding via wire_agent() — sovereign_id resolution, rank from Rank.from_trust(), department from ontology or standing_orders.get_department(). Late-bound via set_tool_registry() public setter (LoD). DutyDefinition.required_skills informational field (Duty→Skill link). ProcedureStep.required_tools declarative field (Cognitive JIT tool dependencies). CognitiveAgent.tool_context attribute. TOOL_CONTEXT_CREATED event emitted during onboarding. Startup wiring in finalize.py follows set_orientation_service() pattern. 1 new file (tools/context.py), 7 existing files modified (protocol.py, agent_onboarding.py, finalize.py, procedures.py, config.py, cognitive_agent.py, events.py), 2 new test files (27 tests across 7 classes). Build prompt: prompts/ad-423c-toolcontext-onboarding.md. Issue #146) | AD-423b COMPLETE (Tool Permissions & Scoping — CRUD+O permission model with 5 additive levels (NONE < OBSERVE < READ < WRITE < FULL), five-layer permission resolution chain (enabled → department scope → restricted_to allowlist → rank gate via default_permissions matrix → Captain override grants), LOTO (Lock-Out/Tag-Out) exclusive access with timeout auto-expire + Captain break_lock, ToolPermissionStore (SQLite-backed, WAL mode, in-memory cache for sync reads, issue/revoke/list grants), ToolPermissionDenied exception with rich context (held vs required), check_and_invoke() permission-checked tool invocation, /tool-access shell command (grant/restrict/revoke/break-lock/list/check subcommands), 3 event types (TOOL_PERMISSION_DENIED, TOOL_LOCKED, TOOL_UNLOCKED). Deny-by-default. 4 new files (tools/permissions.py, experience/commands/commands_tool_access.py + 2 modified tool files), 7 existing files modified (shell.py, events.py, results.py, communication.py, runtime.py, shutdown.py, config.py), 1 new test file (28 tests across 7 classes). Build prompt: prompts/ad-423b-tool-permissions.md. Issue #145) | AD-423a COMPLETE (Tool Foundation — unified Tool protocol, ToolRegistry service, 3 adapter implementations, SkillDefinition.preferred_tools. Tool protocol (typing.Protocol, runtime-checkable) with ToolType 9-value enum, ToolResult frozen dataclass, ToolRegistration metadata wrapper (domain/department/tags/provider/enabled), ToolPreference skill→tool link. ToolRegistry in-memory catalog with register/unregister/get/list_tools filtering by type/domain/department/tag/enabled. Three adapters: InfraServiceAdapter (intent bus broadcast), DirectServiceAdapter (async handler), DeterministicFunctionAdapter (sync callable). Ontology seeding: 7 ToolCapability entries from resources.yaml → DirectServiceAdapter with noop handlers (AD-423c replaces with real bindings). SkillDefinition gains preferred_tools field with SQLite migration. Absorbs AD-483 (Tool Layer — Instruments). 4 new files (tools/__init__.py, tools/protocol.py, tools/registry.py, tools/adapters.py), 4 existing files modified (skill_framework.py, results.py, communication.py, runtime.py), 1 new test file (24 tests across 7 classes). Build prompt: prompts/ad-423a-tool-foundation.md. Issue #144, closes #77) | AD-623 COMPLETE (DM Convergence Gate + DM Self-Monitoring — structural fix for DM conversation loops. Two mechanisms: (1) DM Convergence Gate in WardRoomRouter — check_dm_convergence() detects mutual agreement via cross-author Jaccard similarity (threshold 0.55) over last 3 exchange pairs, emits DM_CONVERGENCE_DETECTED event and returns early (stops routing). Thread-level check before per-agent loop. (2) DM Self-Monitoring — _build_dm_self_monitoring() on CognitiveAgent injects self-similarity warning (>=0.4) into ward_room_notification DM response path, closing the gap where self-monitoring only ran in proactive loop. Also: Counselor handler for DM_CONVERGENCE_DETECTED event, get_posts_by_author() gains optional thread_id filter, check_dm_convergence() facade on WardRoomService. Companion to BF-168 (dm_exchange_limit 6→3). 7 source files modified (threads.py, service.py, ward_room_router.py, cognitive_agent.py, counselor.py, events.py, models.py), 1 new test file (18 tests). Build prompt: prompts/ad-623-dm-convergence-gate.md. Issue #212) | BF-168 CLOSED (DM exchange limit too generous — Atlas-Kira DM thread showed 12+ repetitive exchanges despite dm_exchange_limit=6. Root cause: 6 exchanges = 3 back-and-forth rounds was too generous for typical DM conversations that converge within 2-3 exchanges. Fix: lowered dm_exchange_limit from 6 to 3 in config.py. One-line change. Relates to: AD-614, AD-623) | BF-167 CLOSED (get_embeddings() numpy array truthiness + MemoryGraph3D missing dimensions — AD-611 Memory Graph showed black canvas with no semantic edges. Two root causes: (1) episodic.get_embeddings() used bare `not result["embeddings"]` and `if emb and` checks on ChromaDB return values which are numpy arrays — numpy arrays don't support bare truthiness. Fix: `is None` checks. Latent since AD-531, never triggered until AD-611. (2) ForceGraph3D rendered 0×0 canvas — react-force-graph-3d doesn't auto-detect container size from CSS flex layout. Fix: ResizeObserver measures container, passes explicit width/height props. 2 source files modified (episodic.py, MemoryGraph3D.tsx), 0 new tests) | AD-622 COMPLETE (Special Access Grants (ClearanceGrant) — Captain-issued, time-limited, scoped, revocable elevated access (SAP analog). (1) ClearanceGrant frozen dataclass in earned_agency.py with id/target_agent_id/recall_tier/scope/reason/issued_by/issued_at/expires_at/revoked/revoked_at. (2) effective_recall_tier() gains grants parameter — max(rank, billet, grants). (3) resolve_active_grants() Law of Demeter helper — fail-open on None store or exception. (4) ClearanceGrantStore in clearance_grants.py — SQLite-backed with WAL mode + busy_timeout(5000) + synchronous=NORMAL, in-memory cache populated at startup and updated on issue/revoke, get_active_grants_sync() zero-I/O reads from cache. (5) Shell /grant command (issue/revoke/list) in commands_clearance.py — callsign resolution, sovereign ID targeting, prefix-match revocation, Rich table output. (6) Wired in startup pipeline (results.py, communication.py, runtime.py, shutdown.py). (7) Grant resolution at both tier resolution call sites (cognitive_agent.py, proactive.py) using sovereign_id with slot ID fallback. 2 new files (clearance_grants.py, commands_clearance.py), 8 existing files modified, 1 new test file (22 tests across 6 classes). Build prompt: prompts/ad-622-clearance-grants.md. Issue #208) | AD-621 COMPLETE (Billet-Driven Channel Visibility — Subscription-based routing replaces department matching. (1) WardRoomRouter.find_targets()/find_targets_for_agent() use membership cache (_channel_members dict) instead of home department comparison — agents must be subscribed to receive department channel events. (2) Membership cache populated at startup via populate_membership_cache() from WardRoomService.get_all_channel_members(). (3) communication.py subscription refined from AD-620 FULL+ clearance to ontology-driven: reports_to: captain (First Officer + Counselor only) get all department channels. (4) list_channels(agent_id) INNER JOIN with memberships table for subscription-filtered queries. (5) Earned Agency same_department flag preserved: cross-dept subscribers get same_department=False (higher threshold), home-dept agents get True. (6) @mention overrides subscription — mentioned agents always receive events. (7) Ship-wide and DM routing unchanged. 7 source files modified, 1 new test file (18 tests). Design: docs/research/clearance-system-design.md. Issue #207) | AD-620 COMPLETE (Clearance Model Foundation — Separation of Rank and Access Eligibility. Billet-based clearance replaces AD-619 ship-wide authority hack. (1) `clearance` field on Post dataclass + organization.yaml — Bridge=ORACLE, Chiefs=FULL, Officers=ENHANCED. (2) `_TIER_ORDER` dict, `effective_recall_tier(rank, billet_clearance)` = max(rank-tier, billet-tier), `resolve_billet_clearance()` Law of Demeter helper — all in earned_agency.py. (3) cognitive_agent.py + proactive.py tier resolution replaced with effective_recall_tier(). (4) Oracle gate simplified — ORACLE clearance alone, strategy requirement removed. (5) communication.py FULL+ billet clearance → all department channel subscriptions. (6) Removed `has_ship_wide_authority()` + `_SHIP_WIDE_AUTHORITY_TYPES` from crew_utils.py. (7) Migrated test_ad619 to clearance model. 7 source files modified, 1 config file modified, 1 test file migrated, 1 new test file (27 tests). Design: docs/research/clearance-system-design.md. Issue #206) | AD-619 COMPLETE (Counselor Cross-Department Awareness — Ship-wide authority agents get all department channel subscriptions at startup + Oracle recall tier override for cross-agent clinical awareness. New `has_ship_wide_authority()` helper in crew_utils.py (DRY/Open-Closed — extensible set, not hardcoded agent type checks). Standing orders updated with Cross-Department Awareness section. 3 source files modified, 1 new test file. Build prompt: prompts/ad-619-counselor-cross-department-awareness.md. Issue #205. NOTE: To be superseded by AD-620/621/622 clearance system) | AD-618 SCOPED (Bill System — Standard Operating Procedures. Declarative YAML-based multi-agent SOPs with role-based assignment, BPMN-vocabulary decision points, qualification gates, Cognitive JIT learning bridge. Missing middle layer between Standing Orders (T1, policy) and Cognitive JIT (T3, learned). Navy model: Bills + MRCs + WQSB. Five sub-ADs: AD-618a schema+parser, AD-618b instance+runtime, AD-618c built-in Bills, AD-618d HXI dashboard, AD-618e Cognitive JIT bridge. OSS: framework + runtime + built-in Bills. Commercial: SOP marketplace, visual designer, enterprise governance. Research: docs/research/standard-operating-procedures.md. Issue #204) | BF-166 CLOSED (Consolidation Anomaly False Positives After Stasis — detect_consolidation_anomalies() fires against stale dream history with only 1-2 reports as baseline, no cold-start suppression, unbounded _dream_history list. Fix: (A) minimum history gate raised from 2 to configurable dream_min_history (default 5), (B) cold-start suppression via _suppress_dreams_until in set_cold_start_suppression(), (C) _dream_history bounded with deque(maxlen=max_history), (D) dream_min_history config in EmergentDetectorConfig. 3 source files modified, 1 new test file (8 tests), 2 existing tests updated) | BF-165 CLOSED (Cooperation Cluster False Positives During Stasis — detect_cooperation_clusters() fires against stale Hebbian weights during zero cognitive activity. BF-126 time-bounded suppression expires after 5 minutes while stasis continues. Fix: cognitive activity gate — record_activity() called from runtime on Hebbian record_interaction(), detect_cooperation_clusters() checks if any interaction occurred within configurable cluster_activity_window (default 900s), skips detection if stale. Config: cluster_activity_window in EmergentDetectorConfig (0 = disabled). 4 source files, 8 tests) | BF-164 CLOSED (Stale Unread DM Notification Loop — AD-614's exchange limit blocked agents from responding to old flood threads, but `get_unread_dms()` kept returning those threads — causing infinite BF-082 notification cycles every ~2 minutes. Fix: `get_unread_dms()` accepts `exchange_limit` parameter, SQL subquery excludes threads where agent already has >= exchange_limit posts. `_check_unread_dms()` reads `dm_exchange_limit` from config and passes to query. Log accuracy fix: BF-082 log now reports `routed` count, not raw query count. 3 source files modified (messages.py, service.py, proactive.py), 1 new test file (8 tests). Issue #202). BF-163 CLOSED (DM Send Flood — Agent-to-Agent Feedback Loop. `_extract_and_execute_dms()` had zero rate limiting on the sending side. BF-156's `is_direct_target` bypass only exempted DM recipients from cooldown, not senders. Forge sent 40+ DMs to Chapel in ~2 minutes — feedback loop: Agent A DMs Agent B → B gets BF-082 notification → B responds → A gets notified → repeat, overwhelming LLM Copilot Proxy with HTTP 500 errors. Fix: per-agent per-target 60-second cooldown using `{agent.id}:{target_callsign.lower()}` composite key and `time.monotonic()` timestamps. First DM always allowed; subsequent DMs to same target throttled for 60s. 1 source file (proactive.py), 1 new test file (9 tests). Issue #197). AD-614 COMPLETE (DM Conversation Termination — three-layer defense against DM conversation flooding. Layer 1: Standing order instructions — conversation closure guidance in federation.md + DM self-monitoring in ship.md. Layer 2: DM send self-similarity gate — Jaccard word-set similarity (>= 0.6) against last DM body per agent-target pair in `_extract_and_execute_dms()`. Layer 3: DM thread exchange limit — per-agent post count cap (default 6) per DM thread in `ward_room_router.py`, applies even to `is_direct_target` DMs. New `count_posts_by_author()` on ThreadManager/WardRoomService. Config: `dm_exchange_limit`, `dm_similarity_threshold` in WardRoomConfig. 7 source files modified (federation.md, ship.md, proactive.py, ward_room_router.py, config.py, threads.py, service.py), 1 new test file (17 tests). Issue #198). AD-615 COMPLETE (Ward Room Database Performance Hardening — WAL mode + busy_timeout(5000) + synchronous=NORMAL in ward_room/service.py start(). Matches BF-099 canonical pattern from trust.py/routing.py. WAL verification log at startup with log-and-degrade on network filesystem failure. Ward Room was the only ProbOS database without WAL mode. 1 source file modified, 1 new test file (5 tests). Issue #199). AD-616 COMPLETE (Ward Room Router Hot Path Optimization — channel cache usage, event dispatch semaphore, backend event coalescing. Router calls list_channels() 4 times via DB queries when ChannelManager already has an in-memory cache. Fire-and-forget asyncio.create_task() on route_event() has zero backpressure — thousands of concurrent tasks under flood. Fix: expose get_channel_by_id() using cache, add asyncio.Semaphore(10) to route_event dispatch, add 200ms per-thread event coalescing. Config: router_concurrency_limit, event_coalesce_ms in WardRoomConfig. 3 source files). AD-617b COMPLETE (Per-Agent Hourly Token Budget — per-agent fairness enforcement at proactive loop gate. CognitiveJournal.get_token_usage_since() hourly window query + _is_over_token_budget() gate after circuit breaker + per_agent_hourly_token_cap config field on LLMRateConfig (default 0 = disabled). Budget exhaustion = skip proactive thinks, DMs/WR/Captain directives bypass. 60s exhaustion cache per agent. token_budget_exhausted event for Counselor. 3 source files (journal.py, proactive.py, config.py), 1 new test file (13 tests across 3 classes). Build prompt: prompts/ad-617b-per-agent-token-budget.md. Issue #210). AD-617 COMPLETE (LLM Rate Governance — token bucket rate limiter per tier (fast=60/standard=30/deep=15 RPM sliding window), HTTP 429 exponential backoff with Retry-After header respect and inner retry loop (cap 5 retries, backoff 2/4/8s), LRU cache eviction via OrderedDict (500 entries default). Config: LLMRateConfig in SystemConfig with rpm_fast/rpm_standard/rpm_deep/max_wait_seconds/cache_max_entries. Rate limit exceeded returns cached response or error. 429 counter resets on success. 3 source files modified (llm_client.py, config.py, __main__.py), 1 new test file (13 tests across 4 classes). Issue #201) | AD-617b COMPLETE (Per-Agent Hourly Token Budget — fairness enforcement at proactive loop gate. Budget-exhausted agents skip proactive thinks but still receive DMs/WR notifications/Captain directives. CognitiveJournal.get_token_usage_since() queries hourly sliding window (excludes cached). ProactiveCognitiveLoop._is_over_token_budget() with 60-second exhaustion cache. Gate ordering: after circuit breaker, before _think_for_agent(). Event emission: token_budget_exhausted for Counselor awareness. Config: per_agent_hourly_token_cap on LLMRateConfig (default 0 = disabled). 3 source files modified (journal.py, proactive.py, config.py), 1 new test file (13 tests across 3 classes). Issue #201). AD-613 COMPLETE (Ward Room HXI Performance — Query Batching, Event Debouncing, Caching. Six fixes across 5 files + 1 new test file (10 tests): (1) WebSocket event debouncing — 300ms flag-based coalescing in useStore.ts, module-level timer with per-flag batching. (2) N+1 DM query elimination — `count_threads()` SQL COUNT(*) on ThreadManager/WardRoomService replaces `len(list_threads(100))`. (3) Per-channel thread cache — Map-based 30s TTL in Zustand store. (4) Post pagination — `get_thread()` keyword-only `post_limit=100` + `total_post_count`, DESC LIMIT + reverse. (5) Conditional DM poll — `isOpen` guard on WardRoomPanel useEffect. (6) Composite SQL indexes — `idx_threads_channel_activity`, `idx_threads_channel_archived` (in service.py after ALTER TABLE migration), `idx_posts_thread_created`. 5 source files modified (useStore.ts, threads.py, service.py, models.py, WardRoomPanel.tsx), 1 API router updated (wardroom.py). Issue #196). BF-162 CLOSED (Introspective Faithfulness False Positives — AD-589's `_MANIFEST_CONTRADICTIONS` regex rules fired on conversational idioms ("my intuition suggests", "gut feeling about", "subconsciously noticed", "continuous awareness of"). Uniform `claims=1, contradictions=1` across Lynx/Reyes/Forge = systematic false positive, not genuine confabulation. Downstream: inflated `confabulation_rate` via EMA (alpha=0.1), degraded DEEP→SHALLOW retrieval at >0.3, emitted false SELF_MODEL_DRIFT events, corrupted episode metadata. Fix: `_IDIOM_EXEMPTIONS` pattern list in source_governance.py checked only when contradiction already matched — exemption wins for conversational constructions. 5 exemption patterns: intuition suggests/tells, instinctively/intuitively adverb, gut feeling about, subconsciously + past/gerund verb, continuous awareness/monitoring of. 1 source file (source_governance.py), 1 test file (test_ad589), 12 new tests + 2 updated. 50/50 tests pass. Issue #195). BF-161 CLOSED (Cognitive zone UNKNOWN for GREEN agents — `_build_self_monitoring_context()` only set `cognitive_zone` for non-green zones, so working memory never received the green value. `IntrospectiveTelemetryService.get_cognitive_state()` read from working memory and got nothing → downstream renders showed "UNKNOWN" for healthy agents. Fix: always include zone in self-monitoring context (removed `if zone != "green":` guard), added `"green"` default in telemetry service. Crew-identified by Horizon. 2 source files, 1 test file modified. Issue #194). AD-612 COMPLETE (DM Rendering + Thread Depth + DM Tag Robustness — three related Ward Room communication quality fixes. (A) Hardened DM regex in proactive.py — replaced newline-requiring pattern with two-tier extraction: closed DMs tolerate any whitespace (multiline/single-line/inline), unclosed DMs capture to next [DM tag or end-of-string. Negative lookahead prevents cross-DM-tag consumption. (B) Flat IM rendering for DM channels — WardRoomPostItem gains flat/allPosts/parentCallsign props, DM detail view renders chronological flat messages with "replying to @callsign" back-references instead of threaded tree. WardRoomThreadDetail detects dm-detail from store, flattenPosts() helper. (C) Thread depth flattening — MAX_THREAD_DEPTH constant lowered from 4 to 2, depth>=2 shows parentCallsign back-reference. 3 source files modified (proactive.py, WardRoomPostItem.tsx, WardRoomThreadDetail.tsx), 2 new test files (12 pytest + 7 vitest = 19 tests). Issue #193). AD-611 COMPLETE (3D Memory Graph Visualization — interactive force-directed graph of agent episodic memory in HXI. Backend: new memory_graph router with three-tier node selection (recency 70%/importance 20%/activation 10%), four edge types (semantic via HNSW cosine, thread co-occurrence, temporal proximity 5min, participant Jaccard ≥0.3). Frontend: react-force-graph-3d in new Memory tab on agent profile panel with node color by channel/department, importance-driven size, activation-driven opacity (sigmoid normalization), click-to-detail, edge type legend. Ship-wide toggle merges across crew agents. 200 node default, 500 cap, 2000 edge cap. 1 new backend router (memory_graph.py), 3 new frontend components (memoryGraphTypes.ts, MemoryGraph3D.tsx, ProfileMemoryTab.tsx), AgentProfilePanel.tsx modified, api.py wired. 11 new pytest tests. Build prompt: prompts/ad-611-memory-graph-3d.md. Issue #192). BF-156/157 CLOSED (DM Delivery + @Mention Response Guarantee — five changes across 3 source files fixing two communication reliability bugs. BF-156: Agents don't read DMs — _check_unread_dms() sat after Ensign gate in proactive loop (Ensigns never reached it), per-agent cooldown suppressed DM notifications, thread depth cap truncated DM conversations. Fix: moved _check_unread_dms() before Ensign gate, DM recipients bypass cooldown/round/per-thread caps via is_direct_target flag, DM channels bypass thread depth cap (check moved after channel lookup). BF-157: @mention doesn't guarantee response — mentions used for routing only, never forwarded to agent; LLM prompt offered [NO_RESPONSE] even for @mentioned agents. Fix: mentioned_agent_ids set built from _callsign_registry.resolve(), was_mentioned flag passed in IntentMessage params, @mentioned agents bypass cooldown/caps, prompt says "You were directly @mentioned — a response is expected" without [NO_RESPONSE] option. 3 source files modified (ward_room_router.py, cognitive_agent.py, proactive.py), 2 new test files (7 tests), 3 existing tests updated. Issues #187, #188). BF-159/160 CLOSED (Qualification Probe Fix Wave — 5/221 test failures resolved. BF-159: AUTHORITATIVE source framing suppressed temporal preference — agents with 3,500+ real episodes hit AUTHORITATIVE tier during probes, losing "prefer most recent observation" instruction. AGM Belief Revision is a logical principle, not a quality concern — applied universally across all tiers. BF-160: CrossAgentSynthesisProbe false failure at CREW level — BF-150 redesigned from cross-shard to sovereign-shard (per-agent) but tier remained 3, causing `run_collective()` to invoke with `__crew__` → agent not found → score=0.0. Added skip guard returning `_make_skip_result`. 2 source files modified (cognitive_agent.py, memory_probes.py), 2 test files modified (test_bf148, test_bf150), 2 new tests + 1 modified test. Issues #190, #191). AD-598 COMPLETE (Importance Scoring at Encoding — rule-based 1-10 importance scoring computed at episode creation time via `compute_importance()` in new `importance_scorer.py` module. Three scoring signals: (1) trigger_type mapping from AnchorFrame (captain_dm→8, circuit_breaker_trip→9, proactive_thought→3), (2) content boosts (Captain mentions, Ward Room), (3) outcome adjustments (failures boost, no-response degrades). Importance modifies: activation decay rate via `compute_activation_with_importance()` (importance/5.0 factor — importance=10 decays 2x slower), pruning threshold via `find_low_activation_episodes_with_importance()` (adjusted_threshold = threshold − (importance−5)×0.2), and recall composite scoring (normalized 0-1, weight 0.05 tiebreaker). Episode dataclass gains `importance: int = 5` field. Backward compatible — existing episodes default to 5 (neutral). Dream Step 12 standard + aggressive tiers use importance-aware pruning with fallback. 5 source files modified (types.py, importance_scorer.py NEW, episodic.py, activation_tracker.py, dreaming.py), 1 new test file (19 tests across 6 classes). Issue #172). AD-605 COMPLETE (Enhanced Embedding — Content + Anchor Metadata Concatenation. `_prepare_document()` static method concatenates non-empty AnchorFrame fields as bracketed prefixes `[department] [channel] [watch_section] [trigger_type]` into document text before ChromaDB embedding. 3 write sites updated (store, seed, _force_update). Original `user_input` preserved in metadata for clean Episode reconstruction via `metadata.get("user_input", document)` fallback. One-time idempotent migration `migrate_enriched_embedding()` re-embeds existing episodes and populates user_input metadata. Wired in startup/cognitive_services.py after `migrate_embedding_model()`. 2 source files modified (episodic.py, cognitive_services.py), 1 new test file (15 tests across 4 classes). Issue #181). AD-606/607/608/609/610 SCOPED (Temporal recall merge contamination — wrong-watch episodes outscore correct watch. Three changes: (A) pre-merge watch filtering in cognitive_agent.py excludes wrong-watch semantic episodes at merge boundary when _query_watch_section set, (B) temporal mismatch suppression penalty −0.15 in score_recall() when query has temporal intent but episode watch doesn't match (clamped to 0.0), (C) temporal_match_weight 0.10→0.25 in MemoryConfig + match/mismatch mutually exclusive (0.40 composite swing). 3 source files modified (cognitive_agent.py, episodic.py, config.py), 1 new test file (14 tests across 4 classes). Deep work deferred: AD-601/602/603/604. Issue #176). AD-601/602/603/604 SCOPED (Deep Temporal Discrimination Enhancements. AD-601: TCM temporal context vectors — Howard & Kahana 2002, continuous context gradient replaces binary watch match. AD-602: question-adaptive retrieval routing — classify_retrieval_strategy() gains question-type dimension, TEMPORAL/SOCIAL/FACTUAL use different weight profiles. AD-603: anchor recall composite scoring — recall_by_anchor() returns RecallScore not raw Episodes, enables scored merge. AD-604: spreading activation multi-hop — two-hop associative recall for causal/narrative queries. Priority: AD-603→AD-601→AD-602→AD-604). BF-154/154b CLOSED (TemperamentProbe + SeededRecallProbe timeout — sequential LLM calls exceeded 60s harness timeout. asyncio.gather() parallelizes axes/questions. TemperamentProbe: 4 axes parallel. SeededRecallProbe: 5 questions parallel, eliminated 10% timeout rate (17/173 runs). 2 source files, 1 new test file (10 tests)). BF-153 CLOSED (Shell directive commands use seed callsign instead of runtime callsign. Crew-identified by Reyes. get_callsign() only read YAML seed profile, ignoring CallsignRegistry after naming ceremony. Fix: runtime.callsign_registry checked first, falls back to seed profile, then formatted agent_type. 4 call sites + shell proxy wired. 2 source files, 1 new test file (10 tests)). BF-152 CLOSED (Temporal reasoning probe — keyword scoring gap for JSON agents. Same root cause as BF-151: check_faithfulness() false-zero + LLM scorer variance. Probe computed correct_found via _distinctive_keywords() but never used as score signal. Fix: key_values on _TEMPORAL_EPISODES + keyword_score as 3rd signal max(faithfulness, llm, keyword) with -0.3 wrong-watch penalty + "ship memory" nudge. 1 source file, 1 new test file (18 tests)). BF-151/151b CLOSED (Seeded recall probe + knowledge update probe JSON scoring mismatch. key_values 3-tuple on _RECALL_FACTS + keyword_score signal + old_aliases/new_aliases on _UPDATE_PAIRS + bare value aliases. 1 source file, 1 test file (33 tests). Issue #175). BF-147/148/149/150 CLOSED (Qualification Probe Hardening Wave — 4 bug fixes for memory probe failures. BF-147: watch section vocabulary fix ("first_watch"→"first", "second_watch"→"second_dog") + temporal_match scoring in score_recall()/recall_weighted() + parse_anchor_query "recently" regex fix. BF-148: timestamp calculation fix (no future dates) + temporal preference instruction in _confabulation_guard() for SUPPLEMENTARY/PERIPHERAL tiers. BF-149: empty-response retry-once across all 4 memory probes + empty_response diagnostic field. BF-150: sovereign shard synthesis redesign — all episodes seeded in tested agent's own shard with department attribution, probe_pathway metadata. 7 source files modified, 4 new test files, 28 new tests. Build prompt: bf-147-148-149-150-probe-hardening-wave.md). AD-597 SCOPED (MCP App Host Infrastructure + Interactive Games. ProbOS becomes MCP App host — interactive HTML apps render inside HXI chat via sandboxed iframes + JSON-RPC postMessage bridge. MCP Apps standard covers both Anthropic and OpenAI ecosystems. 6 sub-ADs: AD-597a (HXI AppBridge), AD-597b (game MCP server), AD-597c (chess engine), AD-597d (chess UI), AD-597e (tic-tac-toe migration), AD-597f (external app consumption). Issue #167). AD-596 SCOPED (Cognitive Skill Registry — AgentSkills.io-Compatible Skill Library. Four-tier capability model: T1 Standing Orders, T2 Cognitive Skills (SKILL.md), T3 Executable Skills (Cognitive JIT), T4 Tool Skills (CapabilityDescriptor). Adopts AgentSkills.io open standard for T2 format — interoperable with 30+ tools. ProbOS metadata extensions as additive optional fields. T2-to-T3 self-improvement pathway via existing Cognitive JIT pipeline. Absorbs skills-ref for validation. 5 sub-ADs: AD-596a (format+loader), AD-596b (intent discovery+compose_instructions), AD-596c (skill-registry bridge), AD-596d (external import), AD-596e (validation+linting). crew-capability-architecture.md updated with four-tier model. Issue #166). AD-595 SCOPED (Watch Bill / Billet Registry — Programmatic role-to-agent resolution. Issue #165). AD-589 COMPLETE (Introspective Faithfulness Verification — Layer 3 of Metacognitive Architecture wave (AD-587→588→589), wave COMPLETE. Extends AD-568e faithfulness verification to the self-referential domain. 6 self-referential claim patterns in `_SELF_REFERENTIAL_PATTERNS` detect introspective claims (feelings, memory/cognition ownership, stasis processing, emotional anchors, subconscious). 8 manifest contradiction rules in `_MANIFEST_CONTRADICTIONS` derived from CognitiveArchitectureManifest (AD-587): selective clarity, stasis processing, stasis dreaming, stasis memory evolution, continuous consciousness, emotional subsystem, subconscious, intuition. `extract_self_referential_claims()` splits response into sentences and filters by patterns. `check_introspective_faithfulness()` pure function — no LLM, no I/O — scores claims against manifest contradictions, returns frozen `IntrospectiveFaithfulnessResult`. Score = 1.0 − (contradictions/claims). Graceful degradation: manifest=None skips manifest checks, telemetry=None skips telemetry, both=None assumes good faith. `_check_introspective_faithfulness()` on CognitiveAgent follows AD-568e fire-and-forget pattern. Post-decision pipeline: check → log → SELF_MODEL_DRIFT event → Counselor `record_faithfulness_event()` → episode metadata (score, grounded, contradiction count). Telemetry snapshot caching: DM/WR AD-588 injection paths cache `_snapshot` on AgentWorkingMemory via `set_telemetry_snapshot()` for post-decision cross-check without async call. SELF_MODEL_DRIFT event type added to events.py. 3 source files modified (cognitive_agent.py, source_governance.py, agent_working_memory.py), 1 event file modified (events.py), 1 new test file (38 tests across 6 classes), 0 existing test files changed. Build prompt: ad-589-introspective-faithfulness.md. Issue #153). BF-146 CLOSED (Standing orders hardcode callsigns — agents contradict each other about crew identity. Standing order .md files referenced crew by hardcoded callsigns instead of billet/role titles. When agents self-name during naming ceremony, cross-references become stale. Fix: replaced all hardcoded callsigns with role titles across 15 files following Navy Watch Bill conventions — "reference the billet, never the person." No code changes. Issue #164). AD-590 COMPLETE (Composite Score Floor — Recall Quality Gate. Configurable `composite_score_floor` parameter on `recall_weighted()` filters marginal episodes by minimum composite score threshold. Default 0.0 (disabled/backward-compatible). Per-tier config: basic=0.0, enhanced=0.35, full=0.35, oracle=0.0. DEEP retrieval strategy relaxes floor by −0.10 (clamped to 0.0). Follows anchor_confidence_gate pattern — 3-line list comprehension at step 3c between anchor gate and sort. Wired in cognitive_agent.py + proactive.py. Oracle service unchanged (inherits 0.0 default). 4 source files modified, 1 new test file, 17 new tests. Build prompt: ad-590-composite-score-floor.md). AD-592 COMPLETE (Confabulation Guard Instructions — authority-calibrated anti-fabrication framing in _format_memory_section(). Three tiers: AUTHORITATIVE (light — fabrication guard only), SUPPLEMENTARY/fallback (standard — + orientation priority), PERIPHERAL (strong — + uncertainty mandate). _confabulation_guard() static method on CognitiveAgent. Guard text between instruction and Markers. 1 source file, 2 test files modified, 7 new tests + 1 updated. Build prompt: ad-592-confabulation-guard-instructions.md. Issue #154). AD-591 COMPLETE (Quality-Aware Budget Enforcement — replaces character-count-only budget loop with three stop conditions: (a) character budget (existing), (b) max_recall_episodes hard cap (default k*2), (c) quality degradation stop — stops adding when next episode would drop running mean composite below recall_quality_floor threshold. Config: max_recall_episodes default 0 (uses k*2), recall_quality_floor default 0.40. Per-tier: basic=0.0/0 (disabled), enhanced=0.40/0, full=0.40/0, oracle=0.0/0 (disabled). DEEP relaxes: max×1.5 (if >0), floor−0.10 (clamped ≥0.0). First episode always included. 4 source files modified, 1 new test file, 22 new tests. Build prompt: ad-591-quality-aware-budget.md). AD-593 COMPLETE (Pruning Acceleration + Similarity Floor Tightening — Two-part fix: Part A: two-tier dream Step 12 pruning. Standard tier (>24h, threshold −2.0, 10% fraction, configurable via DreamingConfig). Aggressive tier (>168h, threshold 0.0, 25% fraction). Episode pool pressure: >5000 episodes multiplies fractions by 1.5x (capped 50%). Distinct eviction reason activation_decay_aggressive for audit trail. Part B: agent_recall_threshold raised from 0.15 to 0.25. 3 source files modified, 1 new test file, 24 new tests. Build prompt: ad-593-pruning-acceleration.md). BF-145 CLOSED (Align pre-existing tests with AD-593 changes — 6 tests broken by AD-593's similarity floor raise and two-tier pruning. Test-only fix, no source changes. 2 assertion updates + 1 docstring in test_ad567b_anchor_recall.py for 0.25 threshold default. 1 fixture change in test_ad567d_dream_provenance.py adding aggressive_prune_enabled=False to isolate standard-tier tests. 2 test files modified, 0 new tests. Build prompt: bf-145-ad593-test-alignment.md). BF-144 CLOSED (Stasis duration confabulation — agents fabricate offline duration instead of citing authoritative value in orientation. Meridian posted "2d 22h" when actual stasis was 6 minutes. Root cause: duration presented as narrative prose — LLM treats as background and confabulates. Fix: restructured to STASIS RECORD (AUTHORITATIVE — cite this, do not estimate) with key-value Duration/Shutdown/Resume fields. Added stasis_shutdown_utc + stasis_resume_utc to OrientationContext. Pass timestamps from runtime._previous_session through finalize.py. 2 source files, 1 test file modified, 7 new tests. Build prompt: bf-144-stasis-duration-confabulation.md). BF-143 CLOSED (Temporal episode semantic gap — seeded episodes invisible to recall. Root cause: episode content has no temporal vocabulary, cosine similarity ~0.15 loses to real memories at ~0.20+. Fix: "During first/second watch:" prefix creates semantic bridge to probe questions. Also adds _PROBE_STOP_WORDS for temporal prefix words, punctuation stripping in _distinctive_keywords(). 1 source file, 2 test files modified, 7 new tests. Build prompt: bf-143-temporal-episode-semantic-gap.md). BF-142 CLOSED (Temporal probe scoring — faithfulness/LLM imbalance + keyword false positives. 15/15 agents still fail temporal_reasoning_probe after BF-139. Three causes: (1) check_faithfulness() token-overlap returns near-zero for paraphrased responses. (2) Averaging formula caps scores below threshold — fix: max(score, llm_score) in 3 probes. (3) Second-watch episode content uses common agent vocabulary causing false positives — fix: distinctive domain content. Also records faithfulness_score + llm_score separately in per_question diagnostics. 1 source file, 2 test files modified, 9 new tests. Build prompt: bf-142-temporal-probe-scoring.md. Issue #150). BF-141 CLOSED (Stale session record — Ctrl+C skips session_last.json write. Root cause: Ctrl+C → CancelledError (BaseException) → not caught by `except Exception` → os._exit(0) → shutdown() never runs → session record never written → next boot sees days-old timestamp → inflated stasis duration. Fix: synchronous session record write in both _boot_and_run and _serve finally blocks BEFORE async shutdown. Belt-and-suspenders with BF-135/137 shutdown.py write. 1 file modified, 5 new tests. Build prompt: bf-141-stale-session-record.md. Issue #149). BF-139 CLOSED (Memory probe scoring failures — recall gap + probe hardening. Three compounding root causes: (1) Missing reformulation patterns for common probe question forms ("what happened during...", "what did...", "tell me about...") — 4 new patterns in _REFORMULATION_PATTERNS. (2) Temporal probe keyword collision — c.lower().split()[:4] included stopwords causing false-positive wrong-watch penalties. Fix: _distinctive_keywords() helper with _STOP_WORDS filtering, all distinctive words checked instead of first 4. (3) No LLM fallback scorer on temporal probe. Fix: LLM fallback averaging (matches SeededRecallProbe pattern). Also fixed keyword collision in _TEMPORAL_EPISODES ("3 agents" → "3 workers"). 3 files modified, 30 new tests. Build prompt: bf-139-140-probe-scoring-hardening.md. Issue #147). BF-140 CLOSED (_send_probe exception handling — _send_probe() had no try/except, any handle_intent() exception propagated silently giving score=0.0. Fix: try/except with WARNING-level BF-140-prefixed logging including agent_type. Diagnostic stage logging added to PersonalityProbe and TemperamentProbe. 1 file modified, included in BF-139 test count. Issue #148). BF-138 CLOSED (Sovereign ID Completion — Fixed 10 remaining slot ID leak sites across 7 files. D1: drift_detector _get_crew_agent_ids() now uses resolve_sovereign_id(). D2: All 6 memory probes use _resolve_probe_agent_id() DRY helper for sovereign ID resolution at seed AND recall. D3: HXI agent chat episode creation + chat history recall use sovereign_id. D4: CLI SessionManager session entry, recall, and episode creation use sovereign_id via _sovereign_id attribute. D5: FeedbackEngine accepts identity_registry, _extract_agent_ids() resolves slot IDs to sovereign IDs via resolve_sovereign_id_from_slot(). D6: Recall exception logging elevated from debug to warning with BF-138 prefix. 8 files modified, 15 new tests. Build prompt: bf-138-sovereign-id-completion.md. Issue #141). BF-137 CLOSED (Stale stasis duration after partial boot — shutdown() had _started guard before session record write, partial boots never updated session_last.json. Fix: moved session record write before guard. 1 file modified. Issue #140). AD-584c COMPLETE (Recall Scoring Rebalance — Rebalanced composite scoring weights for QA-trained embeddings + convergence bonus for multi-channel evidence. Weight changes: keyword 0.10→0.20, trust 0.15→0.10, hebbian 0.10→0.05, recency 0.20→0.15, anchor 0.10→0.15, semantic stays 0.35. Convergence bonus: +0.10 when episode found by BOTH semantic AND keyword channels (spreading activation). Config: recall_convergence_bonus field added to MemoryConfig. Full call chain wiring: score_recall() → recall_weighted() → cognitive_agent.py. Clamped to max(0.0, bonus) for defense in depth. 3 files modified, 20 new tests. Build prompt: ad-584c-scoring-rebalance.md). AD-584a/b COMPLETE (Recall Pipeline Q→A Fix — Embedding model swap to multi-qa-MiniLM-L6-cos-v1 + template-based query reformulation + BF-029 prefix removal. Root cause: all-MiniLM-L6-v2 is STS-trained not QA-trained, cosine 0.10–0.35 for Q→A pairs. Tier 1: SentenceTransformerEmbeddingFunction with QA-trained model, 2-tier fallback chain (DefaultEmbeddingFunction → keyword overlap), collection migration on model mismatch. Tier 2: reformulate_query() regex-based question→declarative templates (10 patterns), dual-query in recall_for_agent_scored() merges results across variants taking best distance per episode, BF-029 "Ward Room {callsign}" prefix removed from DM recall queries. Config: embedding_model, query_reformulation_enabled. Migration: episodic, semantic (5 collections), procedure_store. sentence-transformers added to pyproject.toml. 10 files modified, 37 new + 2 updated tests. Build prompt: ad-584-recall-qa-fix.md. Issue #138). AD-584 PLANNED (Recall Pipeline Q→A Fix — Embedding model swap multi-qa-MiniLM-L6-cos-v1 + template-based query reformulation + BF-029 prefix removal. Root cause: all-MiniLM-L6-v2 is STS-trained not QA-trained, cosine 0.10–0.35 for Q→A pairs. 4-tier fix, build prompt ad-584-recall-qa-fix.md covers Tiers 1+2. Research: recall-pipeline-research-synthesis.md. Issue #138). BF-135 CLOSED (Session record write races shutdown timeout — moved session.record_write() before async operations in shutdown sequence. 1 file modified). BF-134 CLOSED (Recall pipeline semantic threshold + FTS scoring + watch section filtering — three interrelated defects: (1) agent_recall_threshold configurable default 0.15 replaces hardcoded 0.3, (2) fts_keyword_floor 0.2 for keyword-only hits, (3) anchor_watch_section promoted to ChromaDB metadata + recall_by_anchor() watch_section param + _try_anchor_recall() wiring + migration backfill. 5 files, 10 new + 3 updated tests). BF-063 CLOSED (Naming ceremony conditional logging — warning for LLM empty/oversized/invalid, info for valid choice. 3 new + 2 updated tests). BF-080 CLOSED (DM Channel Conversation Viewer — selectDmChannel action, dm-detail view state, click-to-navigate from DM Log, back header, reuses WardRoomThreadList/ThreadDetail. Satisfies AD-523a. 3 new tests). AD-583f/583g COMPLETE (Observable State Verification + Convergence Source Tracing — Ward Room echo chamber gap closure. 583g: ThreadEchoAnalyzer traces amplification chains to "Patient Zero" in Ward Room threads via temporal post ordering + Jaccard similarity + anchor independence. 583f: ObservableStateVerifier with pluggable StateProvider protocol (Recreation/Trust/Health) verifies agent claims against system ground truth. Satisfies AD-569d deferral — populates convergence_correctness_rate in BehavioralSnapshot. New events: WARD_ROOM_ECHO_DETECTED, OBSERVABLE_STATE_MISMATCH. Counselor subscriptions + therapeutic DMs. 4 new files, 10 modified, 42 tests. Build prompt: prompts/ad-583fg-observable-state-source-tracing.md. Issue #117). BF-133 CLOSED (Qualification probe anchor gate failure + unrealistic content framing — Two issues: (1) _make_test_episode() created episodes with anchors=None, scoring 0.0 in compute_anchor_confidence(). recall_weighted() anchor_confidence_gate (default 0.3) silently filtered ALL test episodes. Fix: always populate AnchorFrame with realistic defaults (department="qualification", channel="probe", watch_section="first", trigger_type="direct_message") → confidence ≈ 0.44. (2) Episode content lacked production Ward Room framing. BF-029 prepends "Ward Room {callsign}" to recall queries, biasing embeddings toward "[Ward Room]" prefixed content. Seeded episodes had bare facts, reducing semantic similarity. Fix: _ward_room_content() helper wraps all episode content in production "[Ward Room] {channel} — probe: {text}" format. 6 probes updated. 2 files modified, 1 test updated). AD-583 COMPLETE (Wrong Convergence Detection — Echo chamber vs independent collaborative insight. Wires AD-567f anchor independence scoring into AD-554 convergence pathway. 5 sub-ADs: (a) convergence independence scoring in check_cross_agent_convergence() return dict via compute_anchor_independence(), (b) WRONG_CONVERGENCE_DETECTED event + ALERT-severity bridge alert, (c) real-time integration in proactive.py, (d) Counselor subscription + therapeutic DM on extreme cases + groupthink handler upgrade, (e) dream Step 7g wrong convergence flagging + Step 9 EmergenceSnapshot.provenance_independence population. Case study motivation: Chapel tic-tac-toe echo chamber. 8 files modified, 28 new tests). AD-582 COMPLETE (Memory Competency Probes — LongMemEval-Inspired. 6 probes in cognitive/memory_probes.py: SeededRecallProbe (Tier 2, seeds 5 episodes with known facts, scores recall against ground truth via check_faithfulness() + LLM), KnowledgeUpdateProbe (Tier 2, seeds contradictory episodes at different timestamps, tests temporal recency), TemporalReasoningProbe (Tier 2, seeds episodes across watch sections, tests anchor-based temporal filtering), CrossAgentSynthesisProbe (Tier 3, seeds episodes in different agent shards, tests OracleService cross-shard synthesis via global recall()), MemoryAbstentionProbe (Tier 2, seeds topic A episodes then asks about topic B, tests confabulation resistance with dual LLM+keyword scoring), RetrievalAccuracyBenchmark (Tier 1 threshold 0.0, infrastructure precision@k/recall@k measurement). Three shared helpers: _seed_test_episodes() using seed() to bypass rate limiting/dedup, _cleanup_test_episodes() via evict_by_ids() in finally blocks, _make_test_episode() with AnchorFrame construction. Registered in runtime.py after behavioral probes. 3 files modified/created, 24 new tests). AD-580 COMPLETE (Alert Resolution Feedback Loop — Crew-driven alert acknowledgment and suppression. Three modes: Dismiss (time-bounded, default 4h), Resolve (clean-period-gated re-fire, 1h), Mute (indefinite). BridgeAlertService: dismiss_alert(), resolve_alert(), mute_alert(), unmute_alert(), list_suppressed(), _is_suppressed(), _last_detected tracking in _should_emit() independent of emission. BridgeAlertConfig + 2 fields (resolve_clean_period, default_dismiss_duration). Startup wiring in communication.py. 5 API endpoints (POST dismiss/resolve/mute/unmute, GET suppressed). Shell /alert command (commands_alert.py + shell.py registration) with pattern-prefix matching for human-readable dedup keys. Ward Room acknowledgment posting at caller level. 7 files modified, 21 new tests). BF-124 CLOSED. AD-570c COMPLETE. AD-568a/b/c COMPLETE (Adaptive Source Governance — Dynamic episodic vs parametric memory weighting with three capabilities: (1) Task-Type Retrieval Router — RetrievalStrategy enum (NONE/SHALLOW/DEEP), classify_retrieval_strategy() maps intent types to strategies, confabulation safety gate downgrades DEEP→SHALLOW at >0.3 confab rate, Oracle Service wired for ORACLE+DEEP tiers. (2) Adaptive Budget Scaling — BudgetAdjustment dataclass, compute_adaptive_budget() scales context budget from anchor confidence/episode count/score distribution, floor 500/ceiling 12000, wires previously dead cross_department_anchors config. (3) Source Priority Framing — SourceAuthority enum (HIGH/MODERATE/LOW), SourceFraming dataclass, compute_source_framing() produces confidence-calibrated framing (AUTHORITATIVE/SUPPLEMENTARY/PERIPHERAL), _format_memory_section() integration across all 3 cognitive paths. New file: cognitive/source_governance.py. 5 files modified, 30 new tests + 187 regression = 217 passed). AD-462c/d/e COMPLETE (Memory Architecture Extensions — Variable Recall Tiers: RecallTier enum parallels AgencyLevel with trust-gated memory depth (BASIC/ENHANCED/FULL/ORACLE), resolve_recall_tier_params() DRY helper wired into _recall_relevant_memories() and _gather_context(). Oracle Service: OracleService aggregates all 3 knowledge tiers (EpisodicMemory + RecordsStore + KnowledgeStore) with normalized scoring and source provenance. Social Memory: SocialMemoryService "does anyone remember?" protocol via Ward Room thread_mode="memory_query", agents detect and respond from sovereign shard. Ward Room wiring in runtime.py due to startup phase ordering. AD-462f concept graphs remain DEFERRED. 4 new files, 5 modified, 29 new tests). AD-569 COMPLETE (Observation-Grounded Crew Intelligence Metrics — BehavioralMetricsEngine with 5 content-level behavioral metrics: Analytical Frame Diversity, Synthesis Detection, Cross-Department Trigger Rate, Convergence Correctness, Anchor-Grounded Emergence. Dream Step 13 integration. 5 Tier 3 qualification probes. API: /behavioral-metrics, /behavioral-metrics/history. BehavioralMetricsConfig. BEHAVIORAL_METRICS_UPDATED event. 11 files modified, 36 new tests). BF-126 CLOSED (Post-stasis cooperation cluster false positives — crew-identified by Forge + Reyes. Synchronized agent startup after stasis creates correlated Hebbian activity that cooperation cluster detector misinterprets as emergent cooperation. Extended set_cold_start_suppression() to also suppress clusters during 300-second window. 2 files modified, 2 new tests). BF-125 CLOSED (Working memory game engagement desync — crew-identified by Horizon/Forge/Reyes. Four defects: opponent WM orphaned on game completion, reply-path and DM-path MOVE had no WM cleanup, GAME_COMPLETED event had zero subscribers. Fix: centralized GAME_COMPLETED event subscriber in finalize.py cleans both players' WM. Replaced 3 inline cleanup blocks. Existing restore revalidation retained as defense-in-depth. 3 files modified, 1 new test file, 8 new tests) (Working memory not resynced after stasis recovery — crew-identified by Horizon via tic-tac-toe game state drift). BF-123 CLOSED (Ward Room router response path doesn't parse CHALLENGE/MOVE — root cause of recreation system never triggering. When Captain posts in Ward Room, agents respond through `ward_room_router.handle_event()` → intent_bus, which only extracted endorsements and DMs. CHALLENGE/MOVE tags were posted as raw text, bypassing `_extract_and_execute_actions()` in proactive.py entirely. Fix: `_extract_recreation_commands()` method on WardRoomRouter with full CHALLENGE/MOVE extraction, rank gating, Recreation channel thread creation, game lifecycle management, working memory engagement. Also removed DIAG-REC diagnostic logging from proactive.py. 3 files modified, 5 new tests). BF-121 CLOSED (CHALLENGE/MOVE tags inside REPLY blocks posted as text but never parsed as commands — agents wrap output in `[REPLY thread_id]...[/REPLY]`, reply extraction consumes the block before challenge regex runs. Fix: `_extract_commands_from_reply()` method parses CHALLENGE/MOVE from reply body, creates games/makes moves, strips tags before posting. Also applies BF-120 markdown stripping to reply body). BF-122 CLOSED (`callsign` undefined in `_extract_and_execute_actions()` recreation block — relied on variable leaked from notebook `for` loop. If agent produced `[CHALLENGE]` without `[NOTEBOOK]`, NameError crashed silently. Fix: resolve callsign from `callsign_registry` at recreation block entry). 1 file modified, 6 new tests. BF-120 CLOSED (Markdown formatting in LLM output breaks structured tag parsing — `**[CHALLENGE ...]**` or `` `[CHALLENGE ...]` `` not matched by regex. Two-line preprocessing in `_extract_and_execute_actions()` strips `*` and `` ` `` around brackets before all regex patterns. Same fix applied to DM path in `routers/agents.py`. Inline `import re` cleaned up to module-level. 2 files modified, 8 new tests). AD-576 COMPLETE (LLM Unavailability Awareness — EPS Power Brownout Protocol. Three-layer infrastructure awareness: (1) LLM status state machine in proactive loop — operational/degraded/offline states with transition events + Bridge Alerts (ALERT for offline, ADVISORY for degraded, INFO for restored). (2) Infrastructure context injection into `_gather_context()` → rendered as `[INFRASTRUCTURE NOTE]` in `_build_user_message()` before self-monitoring section — tells agents "this is infrastructure, not cognitive." (3) Circuit breaker infra-correlation tagging — `CognitiveEvent.infrastructure_degraded` flag filters brownout events from velocity/similarity computation, preventing false trips. Counselor clinical gating on `_on_self_monitoring_concern()` suppresses infrastructure-correlated amber concerns. Wires previously dead `LLM_HEALTH_CHANGED` event via `LlmHealthChangedEvent` dataclass. Also fixes BF-116 (dead `circuit_breaker_redirect` removal + AD-567g `orientation_supplement` rendering completion) and BF-117 (convergence/divergence bridge alert delivery — `rt.bridge_alerts` + `rt.ward_room_router`). 7 files modified, 1 new test file, 29 new tests). BF-118 CLOSED (OrientationService startup crash — `runtime` not in scope in `cognitive_services.py`. One-line fix: pass `ontology` parameter directly). BF-119 CLOSED (DM game challenge parsing — `[CHALLENGE @callsign game_type]` only parsed in proactive loop, silently ignored in DM responses. Added CHALLENGE parsing to HXI Profile DM path in `routers/agents.py` alongside existing AD-572 MOVE parsing. Recreation channel thread creation, game creation, working memory engagement registration. Standing orders updated with recreation syntax). AD-575 COMPLETE (Unified Self-Awareness — Cross-Context Identity Recognition. `_detect_self_in_content()` method on CognitiveAgent scans Ward Room thread content for the agent's own callsign using word-boundary regex. Self-recognition cue injected in WR branch of `_build_user_message()` — agent told "references to '{callsign}' refer to YOU, respond as a participant not an observer." Cross-context engagement binding: when self-mentioned AND has active game engagement via AgentWorkingMemory, injects participatory awareness ("Spectators are watching your game"). Generalizes BF-102 commissioning awareness pattern. 1 file modified, 1 new test file, 10 new tests). AD-513 Phase 1 COMPLETE (Ship's Crew Manifest — Queryable Crew Roster. Three-layer delivery: (1) `get_crew_manifest()` on VesselOntologyService — query-time assembly from ontology/trust/callsign subsystems with dependency-injected enrichment. (2) Anti-confabulation cognitive grounding via `_build_crew_complement()` in `_build_temporal_context()` — department-grouped "SHIP'S COMPLEMENT" block injected into ALL prompt pathways (DM, WR, proactive). OrientationContext `crew_names` for cold/warm boot. (3) HXI CrewRosterPanel — draggable floating panel with dept filter chips, rank badges, trust bars, click-to-profile. REST endpoint `/api/ontology/crew-manifest`. LoD fix: `get_all_assignments()` public method. 10 files modified, 14 new tests). AD-574 COMPLETE (DM Reply Agent Notification — Captain posts in DM channels now notify the target agent. Two independent gaps fixed: (1) find_targets() in WardRoomRouter added "dm" channel type case matching agent.id[:8] against channel name, no Earned Agency gating. (2) get_unread_dms() query rewritten from "agent has zero posts" to "last author is not the agent" via LEFT JOIN subquery with COALESCE — catches Captain replies in agent-initiated threads. 2 files modified, 9 new tests). BF-114b CLOSED (Remove Dead `proactive_extends_idle` Code Path — dead code cleanup following BF-114. Removed: `proactive_extends_idle` from DreamingConfig + system.yaml, `record_proactive_activity()` + `is_proactively_busy` + `_last_proactive_time` from DreamScheduler, idle calculation simplification, proactive.py caller, startup wiring, DreamAdapter busy guard. 9-test TestDreamSchedulerProactiveAwareness class removed, 1 focused emergent detector test added. Net -8 tests. 7 files modified). AD-573 COMPLETE (Unified Agent Working Memory — Cognitive Continuity Layer. Per-agent AgentWorkingMemory maintaining active situation model across all cognitive pathways. Ring-buffered entries (actions, observations, conversations, events) with token-budget-aware render_context() and 6-priority eviction. ActiveEngagement tracking for games/tasks/collaborations. All pathways write: handle_intent() action recording, proactive observation recording, DM conversation recording, [CHALLENGE]/[MOVE] game engagement management, cognitive state sync, active game sync. All pathways read: render_context() injected into DM/WR/proactive _build_user_message() branches. SQLite-backed WorkingMemoryStore (ConnectionFactory, start()/stop(), BEGIN IMMEDIATE + asyncio.Lock). Freeze on shutdown, restore on stasis recovery with stale pruning + game revalidation. System prompt _has_active_game() replaced with working_memory.has_engagement("game"). WorkingMemoryConfig in SystemConfig. 2 new files, 7 modified, 2 new test files, 35 new tests). AD-572 COMPLETE (Captain Engagement Priority — Active State Awareness in DM Path. Active game state injection into DM _build_user_message() via _build_active_game_context(). DM system prompt augmentation with [MOVE] instruction when game active. [MOVE pos] parsing from DM responses in agents router with move execution against RecreationService. get_game_by_player() DRY method on RecreationService replaces 3 instances of iterate-and-match in proactive.py. Ward Room board update posting from DM path. gameMoveExecuted flag in REST response. 4 files modified, 3 new test files, 18 new tests). AD-526b COMPLETE (HXI Tic-Tac-Toe — Captain vs Crew Game Panel. REST router /api/recreation/ (challenge, move, active, forfeit) with Depends() DI. Floating GamePanel.tsx (CSS Grid board, piece-pop animations, win-line glow, pulse-dim waiting). GAME_UPDATE event emission from RecreationService. Game rehydration on refresh. Challenge button in ProfileInfoTab. forfeit_game() service method. Zustand store game state slice + WebSocket handler. 2 new files, 7 modified, 16 new tests). BF-110 CLOSED (Game Board Invisible to Agents — get_recent_activity() only returns top-level threads, not replies where board updates are posted. Fixed: inject active game state (board, valid moves, turn indicator) directly from RecreationService into proactive context. 2 files modified, 0 new tests). AD-526a COMPLETE (Social Channels + Tic-Tac-Toe — agent recreation framework. GameEngine protocol + TicTacToeEngine. RecreationService with game lifecycle, Ship's Records integration, GAME_COMPLETED event emission. Recreation + Creative default Ward Room channels with auto-subscription. Proactive integration: Recreation channel context gathering, [CHALLENGE @callsign game_type] and [MOVE position] action extraction with rank gating (Lieutenant+). cognitive_agent.py challenge/move instructions. RecreationService wired in finalize.py with ward_room + records_store + emit_event. 10 files modified, 47 tests). AD-570 COMPLETE (Anchor-Indexed Episodic Recall — Structured AnchorFrame Queries. Promotes 4 anchor fields (department, channel, trigger_type, trigger_agent) to top-level ChromaDB metadata for native where-clause filtering. One-time migration backfills existing episodes. recall_by_anchor() API with two modes: enumeration (structured filters, no embedding) and semantic re-ranking (structured + vector similarity). Post-retrieval agent_id filtering, activation tracking, hash verification. 2 files modified, 23 tests). AD-570b COMPLETE (Participant Index Sidecar — SQLite junction table indexing episode participants (sovereign IDs + callsigns) for O(1) lookups. 20 tests). BF-109 CLOSED (Qualification Probe Param Key Mismatch. _send_probe() sent params={"message":...} but perceive() reads params.get("text"). All Tier 1/2 probe results were unreliable — agents never received the question. One-line fix: "message" → "text". First real qualification run: 130/131 pass (99.2%), 15 agents, 130 baselines established. Only failure: Security Officer mti_temperament at 0.000). BF-108 CLOSED (LLM Unreachable — No Runtime Visibility. MockLLMClient.get_health_status() now returns overall:"mock" with all tiers offline. runtime.llm_is_mock property. Chat endpoint returns explicit offline message instead of triggering self-mod. /system/services correctly reports LLM as offline. 4 files, 0 new tests — covered by existing 82 tests). AD-567g COMPLETE (Cognitive Re-Localization — final AD in Memory Anchoring lineage 567a→g. Structured orientation at boot: cold start full orientation (identity + cognitive grounding + first duty), warm boot stasis recovery, diminishing proactive supplement. Anchor field gap fixes: watch_section, event_log_window, Ward Room department. OrientationService, OrientationContext, derive_watch_section(). Subsumes BF-034 cold-start note. 28 tests). AD-567f COMPLETE (Social Verification Protocol — absorbs AD-462d. Cross-agent claim verification, corroboration scoring, cascade confabulation detection. Privacy-preserving: agents see metadata not content. Anchor independence discriminates corroboration from cascade. Ward Room integration after AD-506b peer similarity. Bridge Alerts on medium/high risk. Counselor therapeutic DMs on high risk. 28 tests). AD-567d COMPLETE (Anchor-Preserving Dream Consolidation + Active Forgetting — provenance composition through dream pipeline (absorbs AD-559), ACT-R activation-based memory lifecycle (absorbs AD-462b), ActivationTracker with SQLite access log, dream Step 12 activation pruning, micro-dream replay reinforcement, recall access recording, 31 tests). AD-567c COMPLETE (Anchor Quality & Integrity — Johnson-weighted confidence scoring, RPMS confidence gating, per-agent AnchorProfile for Counselor diagnostics, SIF check_anchor_integrity(), drift classification (specialization/concerning/unclassified), absorbs AD-567e). AD-567b COMPLETE (Anchor-Aware Recall + Salience-Weighted Retrieval — RecallScore composite scoring, FTS5 keyword search sidecar, anchor context headers in memory formatting, SECONDHAND source wiring, recall_weighted() API with budget enforcement. Absorbs AD-462a). AD-567a COMPLETE (Episode Anchor Metadata — AnchorFrame dataclass with 10 fields across 5 dimensions, all 15 episode creation sites wired, serialization round-trip through ChromaDB, content hash exclusion, backwards compatible). BF-104 CLOSED (Display Crew Agent Count — registry.crew_count(), shell prompt shows crew not total, status panel/API/working memory updated). AD-566f COMPLETE (/qualify Shell Command — manual trigger and inspection, 5 subcommands: status/run/agent/baselines). AD-566e COMPLETE (Tier 3 Collective Tests — 5 crew-wide probes: CoordinationBreakevenProbe, ScaffoldDecompositionProbe, CollectiveIntelligenceProbe, ConvergenceRateProbe, EmergenceCapacityProbe + QualificationHarness.run_collective() + DriftScheduler collective integration). AD-566d COMPLETE (Tier 2 Domain Tests — 5 department-gated probes: TheoryOfMindProbe, CompartmentalizationProbe, DiagnosticReasoningProbe, AnalyticalSynthesisProbe, CodeQualityProbe + DriftScheduler tier generalization). AD-566c COMPLETE (Drift Detection Pipeline — DriftDetector z-score engine, DriftScheduler periodic runner, Counselor/VitalsMonitor/BridgeAlerts integration). AD-566b COMPLETE (Tier 1 Baseline Tests — 4 psychometric probes: PersonalityProbe, EpisodicRecallProbe, ConfaculationProbe, TemperamentProbe). AD-566a COMPLETE (Qualification Test Harness Infrastructure — psychometric protocol, store, harness engine). AD-541f COMPLETE (Eviction Audit Trail — Append-Only Accountability). AD-541 Lineage CLOSED (6/6 pillars complete). AD-541e COMPLETE (Content Hashing — Cryptographic Episode Integrity). BF-103 CLOSED (Episodic Memory Agent ID Mismatch — sovereign ID normalization + migration). AD-541d COMPLETE (Guided Reminiscence — Therapeutic Memory Sessions). AD-541c COMPLETE (Spaced Retrieval Therapy — Active Recall Practice). AD-541b COMPLETE (Reconsolidation Protection — Read-Only Memory Framing). AD-555 COMPLETE (Notebook Quality Metrics & Dashboarding). AD-554 COMPLETE (Real-Time Cross-Agent Convergence & Divergence Detection). AD-553 COMPLETE (Quantitative Baseline Auto-Capture). AD-552 COMPLETE (Notebook Self-Repetition Detection). AD-551 COMPLETE (Notebook Consolidation — Dream Step 7g). AD-550 COMPLETE. BF-069 CLOSED. BF-101/102 COMPLETE. AD-560 COMPLETE. AD-557 COMPLETE. AD-558 COMPLETE. BF-099 CLOSED. Cognitive JIT pipeline COMPLETE (9/9). 618 Cognitive JIT tests. 7,253 tests (7,067 pytest + 186 vitest).

---

## Recent Builder Closures

AD-604 Spreading Activation CLOSED. Added SpreadingActivationConfig, SpreadingActivationEngine, and CognitiveAgent causal-query integration for bounded two-hop recall using anchor metadata, hop decay, and score-preserving deduplication. Fallback gate: 279 targeted tests passed (13 focused AD-604 + 84 CognitiveAgent/config adjacent + 182 recall/episodic). Full-suite gate deferred to AD-673 by Captain decision because it is blocked by the unrelated environmental proactive-timeout in `tests/test_proactive.py::TestPerAgentCooldown::test_per_agent_cooldown_used_in_cycle`.

AD-606 Think-in-Memory CLOSED. Added ThoughtStoreConfig, ThoughtStore, and CognitiveAgent conclusion persistence that stores important working-memory conclusions as REFLECTION episodes anchored to channel="thought" with typed trigger metadata, importance thresholding, per-cycle caps, and evidence provenance. Fallback gate: 277 targeted tests passed (11 focused AD-606 + CognitiveAgent/config adjacent + recall/episodic band). Full-suite gate remains deferred to AD-673 because the unrelated environmental proactive-timeout persists.

AD-573 Memory Budget Accounting CLOSED. Per-cycle MemoryBudgetManager tracks L0/L1/L2/L3 token budgets (150/3000/1000/500), supports allocate/release/remaining/total_remaining/reset with disabled pass-through and unknown-tier warnings, and compress_episodes() truncates RecallScore lists by composite_score within budget. CognitiveAgent accepts optional memory_budget_config via kwargs and creates a local per-cycle manager in decide() only; recall path wiring, _build_user_message(), recall_weighted(), and render_context() unchanged. Tests: 15 focused passed; 73 adjacent cognitive-agent regressions passed; 137 AttributeError regression band passed after constructor-bypass compatibility fix; full xdist gate after fix: 9683 passed, 14 skipped, 140 worker-crash failures classified by serial triage (30 exact nodeids passed, proactive long-delay nodes 2 passed with --timeout=180).

AD-579a Pinned Knowledge Buffer CLOSED. AgentWorkingMemory now supports an optional ephemeral PinnedKnowledgeBuffer with PinnedFact entries, TTL expiry, duplicate refresh, priority/LRU eviction, budgeted rendering at priority 0, disabled no-op behavior, and public pin_knowledge/unpin_knowledge/pinned_knowledge APIs. Added PinnedKnowledgeConfig and KNOWLEDGE_PINNED/KNOWLEDGE_UNPINNED event enum values; no persistence, auto-pinning, cross-agent sharing, API endpoints, or event emission wiring. Tests: 12 focused passed; 43 adjacent working-memory regressions passed; full xdist gate: 9737 passed, 14 skipped, 132 worker-crash failures classified by exact-node serial triage (68 passed).

AD-579b Temporal Validity Windows CLOSED. Episode now has valid_from/valid_until defaults; AnchorFrame has temporal_validity_start/temporal_validity_end defaults; EpisodicMemory metadata round-trips validity timestamps; recall_weighted(valid_at=...) filters expired and not-yet-valid episodes; recall_valid_at() wraps the filtered path. Added TemporalValidityConfig. No automatic inference, UI, retroactive propagation, anchor-recall changes, or dream consolidation changes. Tests: 10 focused passed; 30 adjacent anchor/weighted recall regressions passed; full xdist gate: 9679 passed, 14 skipped, 138 worker-crash failures classified by exact-node serial triage (74 passed).

AD-586 Task-Contextual Standing Orders CLOSED. Added TaskContext, TaskContextConfig, default config/task_orders markdown files (build/analyze/communicate/diagnose plus zero-byte general), Tier 5.5 compose_instructions injection, CognitiveAgent task classification wiring, and startup wiring via _wire_task_context(). Intent mapping is hardcoded for build/analyze/communicate/diagnose/review/general; no LLM classification, per-agent customization, dream evolution, _load_file cache changes, or proactive path-specific rewiring. Tests: 12 focused passed; 90 adjacent standing-orders/cognitive-agent regressions passed; full xdist gate: 9742 passed, 14 skipped, 142 worker-crash failures classified by exact-node serial triage (83 passed).

AD-602 Question-Adaptive Retrieval — CLOSED. Added deterministic QuestionClassifier and RetrievalStrategySelector mapping TEMPORAL/CAUSAL/SOCIAL/FACTUAL queries to tuned recall parameters, QuestionAdaptiveConfig with per-type overrides, and minimal CognitiveAgent recall integration applying strategy k/weight overrides without switching recall methods. No LLM classification, DEEP/ORACLE escalation, fusion, API, or HXI work. Tests: 12 focused passed; 56 adjacent cognitive-agent regressions passed; full xdist gate: 9776 passed, 14 skipped, 130 worker-crash failures classified by exact-node serial triage (92 unique passed).

AD-610 Utility-Based Storage Gating — CLOSED. Added StorageGate and StorageDecision for write-time episode validation, StorageGateConfig, EPISODE_REJECTED event type, EpisodicMemory.set_storage_gate(), pre-BF-039 gate evaluation in store(), and startup wiring in cognitive_services. Gate uses in-memory recent-window Jaccard duplicate detection, deterministic utility scoring, lightweight contradiction logging, and high-importance utility bypass; no LLM dedup, retroactive dedup, cross-agent dedup, BF-039 changes, API, or HXI work. Tests: 14 focused passed; 34 episodic regressions passed; 3 constructor-bypass regressions passed after getattr compatibility fix; full xdist gate after fix: 9793 passed, 14 skipped, 129 reported failures classified by serial triage (1 xdist-only assertion passed serial, 89 unique worker-crash nodes passed serial).

---

## Development Eras

| Era | Phases | Codename | Status |
|-----|--------|----------|--------|
| [**Era I: Genesis**](progress-era-1-genesis.md) | 1-9 | Building the Ship | Complete |
| [**Era II: Emergence**](progress-era-2-emergence.md) | 10-21 | The Ship Comes Alive | Complete |
| [**Era III: Product**](progress-era-3-product.md) | 22-29 | The Ship Sets Sail | Complete |
| [**Era IV: Evolution**](DECISIONS.md) | 30 | The Ship Evolves | Active |
| Era V: Civilization | 31-36 | The Ship Becomes a Society | Planned |

## Release Tagging Policy

Tags use the format `v{major}.{minor}.{patch}-phase{N}` (e.g., `v0.4.0-phase29c`).

| Event | Tag? | Example |
|-------|------|---------|
| Phase completed | Yes — bump minor version | `v0.5.0-phase30` |
| Critical bug fix between phases | Yes — bump patch version | `v0.4.1-phase29c` |
| Roadmap/docs-only changes | No | — |
| Mid-phase work (partial features) | No | — |
| First production-ready release | Yes — `v1.0.0` | After security hardening (Phase 31) |

**Current tags:**

| Tag | Commit | Date | Notes |
|-----|--------|------|-------|
| `v0.1.0-phase12` | — | — | End of Era I: Genesis |
| `v0.4.0-phase29c` | `13d52a7` | 2026-03-17 | End of Phase 29c: Codebase Knowledge |

## Design Principles

See [design-principles.md](docs/development/design-principles.md) for full design principles:
- "Brains are Brains" (Nooplex core principle)
- Agent Development Model (Communication + Simulation)
- HXI Self-Sufficiency, Agent-First Design, Cockpit View
- Probabilistic Agents, Consensus Governance
- Agent Classification Framework (Core / Utility / Domain)
- Foundational Governance Axioms (Safety Budget, Reversibility, Minimal Authority)
- "Cooperate, Don't Compete" (Federation philosophy)
- Visiting Officer Subordination
- Extension-First Architecture
- Markdown is Code

---

## Environment

- **Platform:** Windows 11 Pro (10.0.26200)
- **Python:** 3.12.13 (installed via uv)
- **Toolchain:** uv 0.10.9
- **Key deps:** pydantic 2.12.5, pyyaml 6.0.3, aiosqlite 0.22.1, httpx 0.28+, rich 13.0+, chromadb 1.5.4, pytest 9.0.2, pytest-asyncio 1.3.0, pytest-xdist 3.8.0, pytest-timeout 2.4.0
- **LLM endpoints:** Fast tier: Ollama at `http://127.0.0.1:11434/v1`, Standard/Deep tier: VS Code Copilot proxy at `http://127.0.0.1:8080/v1`
- **LLM models:** fast=qwen3.5:35b (local Ollama), standard=claude-sonnet-4.6 (Copilot proxy), deep=claude-opus-4.6 (Copilot proxy)
- **Run tests:** `uv run pytest tests/ -v` (sequential) or `uv run pytest -n auto` (parallel, 13x faster)
- **Run fast tests:** `uv run pytest -n auto -m "not slow"` (skip 65 sleep-heavy tests)
- **Run demo:** `uv run python demo.py`
- **Run interactive:** `uv run python -m probos`
